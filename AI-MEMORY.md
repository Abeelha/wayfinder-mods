# AI Memory - wayfinder-mods

Per-repo memory. Append-only, concise. Format: `### YYYY-MM-DD - Category - entry`.

---

### 2026-07-04 - TechStack - Wayfinder UE4SS setup facts
- Game dir: `D:/SteamLibrary/steamapps/common/Wayfinder`, exe dir `Atlas/Binaries/Win64` (project name = Atlas)
- EAC is dead post-Echoes: `EasyAntiCheat/Settings.json` points at `Atlas/Binaries/Win64/WayfinderClient.exe` which does not exist. Nexus UE4SS mods run fine.
- UE4SS v3.0.1 (flat layout: dwmapi.dll + UE4SS.dll + Mods/ at exe dir). Wayfinder REQUIRES custom `UE4SS_Signatures/GUObjectArray.lua` AOB sig (shipped by ShowNameplates + SmartSort, identical) AND `bUseUObjectArrayCache = false` in UE4SS-settings.ini.
- Native class prefix `/Script/Wayfinder.` (e.g. `PlayerInventoryComponent:CLIENT_NotifyItemAdded`). Player pawn BP: `/Game/Blueprints/Main/WFPlayerCharacter_Base.WFPlayerCharacter_Base_C`.
- Game uses GAS. Player abilities: `Atlas/Content/Blueprints/Player/GAS/GameplayAbilities/` - weapon families 2H / 2HR / DW / SnS / RangedWeapon. Combo GAs `GA_Player_2H_LightAttack1..4`, parry `GA_Player_2H_Parry{,_Light,_Heavy}`, block `GA_Player_Block{,_2H}`. `GA_Player_Melee_Base` holds `Attack1PressCombo`, `HasComboFollowUps`, `InputTask`, CallFuncs `HandlePlayerInputPress/Hold/ReleaseWithAbility`.
- Char BP input UFunctions: `InpActEvt_Attack1_K2Node_InputActionEvent_36/_37` (M1), `InpActEvt_Attack2_K2Node_InputActionEvent_40/_41` (M2). Pressed vs released edge = TBD at runtime (recon phase).
- Full FModel JSON dump in repo `Atlas/` (gitignored), from Nexus mod 2 ("serialised file").

### 2026-07-04 - Milestone - Runtime recon done (WFRecon session 1), verified APIs
- Input edges CONFIRMED: `InpActEvt_Attack1_..._36` = PRESS, `_37` = RELEASE. Attack2: `_40` = press, `_41` = release. M1 hold produces NO extra events (game has no native auto-chain).
- ALL enemy generic attacks flow through `GA_AI_Base_C:K2_ActivateAbility` (base hook catches Skirmisher/Brawler/Rifleman/etc subclasses). Enemy attack GAs tagged `Ability.Characteristic.Attack` + `.Melee` (verified GA_AI_Atk_Melee defaults).
- Weapon defense model differs per family: SnS = `GA_Player_Block_SNS` (hold), 2H = `GA_Player_Block_2H` + `GA_Player_2H_Parry*`, DW = `GA_Player_DW_Parry` (active parry w/ InvulnEffect + `DW_Parry_Pushback` counter). DW M2 = HeavyAttack_Flourish, NOT block - Block is its own input (`EWFGameplayAbilityInput::Block`, tag `Input.Combat.Block`).
- Player ASC = `WFPlayerAbilitySystemComponent` living on PLAYERSTATE as `UnrealAbilityComponent`, NOT on pawn. Clean access: static lib `/Script/Wayfinder.Default__WFAbilitySystemBlueprintLibrary` -> `GetWFPlayerAbilitySystemComponent(actor)`.
- Key callable natives (UHT dump `Win64/UHTHeaderDump/Wayfinder/Public/`): `WFPlayerAbilitySystemComponent:GetAbilityFromInputTag(FGameplayTag)` (weapon-agnostic block/parry lookup), `AbilitySystemComponent:TryActivateAbilityByClass(class, bool)`, `WFAbilitySystemComponent:AddGameplayTag/RemoveGameplayTag`, `WFAbilitySystemBlueprintLibrary:AUTH_TryGiveAndActivateAbilityOnce(...)`.
- Combo system: melee GA waits on `WFPlayerInputAsyncQuery*`/`WFAbilityTask_HandlePlayerInput*` tasks listening for INPUT TAGS; next ability class per attack in `Attack1PressCombo`/`Attack2PressCombo` properties.
- Sprint: `GA_Player_Sprint_C:K2_ActivateAbility` hook works. `WFPlayerCharacter:IsPlayerSprinting()` pure native. Combat tag `Character.State.Generic.InCombat`.
- GAME CRASHED (EXCEPTION_ACCESS_VIOLATION, UE4SS frames) ~200s into session. Prime suspect: WFRecon's LoopAsync combat probe touching UObjects OFF game thread (probe also never logged = failing silently in pcall). RULE: every LoopAsync body that touches UE objects goes inside ExecuteInGameThread. WFRecon removed from game + repo (git history baf5ab0).
- Session log archived: scratchpad/UE4SS-session1.log. Object dump: Win64/UE4SS_ObjectDump.txt (from GUI Console tab "Dump Objects & Properties" button; Dumpers tab "Generate UHT Compatible Headers" for the SDK).

### 2026-07-04 - Milestone - AutoChain + AutoParry + AutoSprint v1 deployed
- AutoChain (F7): hooks Attack1 press/release edges for held state; LoopAsync 70ms alternates synthetic press/release calls of the char's own `InpActEvt_Attack1_*` UFunctions; `injecting` guard flag prevents self-hook feedback loop.
- AutoParry (F8): hooks `GA_AI_Base_C:K2_ActivateAbility`; filters melee attacks by AbilityTags; range gate 700u; activates `GetAbilityFromInputTag(Input.Combat.Block):GetClass()` via `TryActivateAbilityByClass` = weapon-agnostic (DW parry / SnS block / 2H parry). 0.6s cooldown.
- AutoSprint (F6): LoopAsync 300ms in game thread; gates: not InCombat tag, not IsPlayerSprinting, speed > 100u/s; activates GA_Player_Sprint_C.
- All need in-game test (user). Open Qs: does synthetic InpActEvt call drive combo? does DW parry fire counter (Pushback) on parried hit? does InCombat tag actually flip?

### 2026-07-04 - Milestone - v2: timed AutoParry + fixed AutoSprint (test 1 feedback)
- Test 1 results: AutoChain WORKS (hold M1 chains). AutoParry triggered but at windup START (parry window expired before hit). AutoSprint dead.
- AutoSprint root cause: `pawn:HasMatchingGameplayTag()` is PURE_VIRTUAL C++ interface stub - calling it via UE4SS UFunction dispatch silently fails. Same reason WFRecon combat probe never logged. FIX: static `WFAbilitySystemBlueprintLibrary:AbilitySystemHasTagExactly(ASC, tag)` - works for any tag check. Also `UEHelpers.GetPlayerController` calls global `Print` (nil in mod env) when no PC exists (menu) - avoid UEHelpers in loops, cache pawn via ClientRestart hook + FindAllOf fallback.
- AutoParry v2 timing solution: DATA-DRIVEN. `tools/extract-attack-timings.py` walks dump enemy GA jsons -> Montage -> AnimSequence -> first `AnimNotifyState_WeaponTrace` (or MeleeTrace) notify LinkValue = damage window start (montage segment mapping: StartPos + (t - AnimStartTime)/rate). 295 attack timings extracted (median 1.0s, e.g. Skirmisher Sweeping_Slash 0.476s, Brawler Punch_01 1.262s). Emits `AutoParry/Scripts/timings.lua`; mod schedules parry at hitTime - LEAD (0.25s) via ExecuteWithDelay, re-validates at fire (range/cooldown/enabled).
- Mount facts: player pawn STAYS the pawn while mounted (`WFCharacter.m_pMountComponent`, `IsMounted()`); sprint is the same GA_Player_Sprint (mount speed via GE_Mount_SprintSpeed passed to PlayerToggleMount). Rider velocity ~0 while mounted -> read `GetAttachParentActor():GetVelocity()`.
- Sprint status tag: `Character.State.Generic.Sprinting` (granted by GE_Status_Sprint; blocked by Character.State.Disable.Sprint / Exhausted / InAir).
- Re-run timing extractor after game updates (dump refresh) to regenerate timings.lua.

### 2026-07-04 - Gotcha - boot crash: no FindAllOf/ExecuteInGameThread before player spawn
- v2 AutoSprint crashed game AT LAUNCH (AV right after "Event loop start"): its LoopAsync ran FindAllOf("WFPlayerCharacter_Base_C") + ExecuteInGameThread at the MAIN MENU while the BP class was not loaded yet.
- RULE for this game: mods with periodic loops gate ALL engine access behind a `ready` flag set by the `PlayerController:ClientRestart` hook (plus boot-time `StaticFindObject(class):IsValid()` check to survive "Restart All Mods" mid-map - that pattern is safe, ShowNameplates uses it). Commit 9ab5d15.

### 2026-07-04 - Milestone - WFQoL merged mod (chain+parry+sprint+reload+overlay)
- Merged AutoChain/AutoParry/AutoSprint into ONE mod `WFQoL` (single Lua state = shared toggles for the overlay, no cross-mod IPC). Old 3 mods removed from game+repo (history: bb19a8a..9ab5d15).
- Overlay: `KismetSystemLibrary:PrintString(pawn, line, true, false, {R,G,B,A}, 0.65)` every 500ms = quasi-persistent top-left status line. NOTE: Engine module is MISSING from UHTHeaderDump (dumper skipped it) - verify Engine natives via UE4SS_ObjectDump.txt instead.
- AutoReload (new): active-reload minigame = `GA_Player_RangedWeapon_ActiveReload_C` with BP fns `CheckInWindow`/`CheckInForgivenessWindow` (props: Tolerance 3.0, EarlyPressForgivenessPercent 0.2, ReloadSuccessType=GA_..._ActiveReloadSucceeded_C). Mod: RegisterHook both checks -> `return true` when enabled (returning nothing = no override, clean toggle), + on reload GA activation auto-press `InpActEvt_Reload_K2Node_InputActionEvent_12` after 300ms. Reload input node edge assumption: lower index = press (consistent: Attack1 36/37, Attack2 40/41, Mount 0/1).
- AutoSprint v3: still-broken diagnosis pending; combat tag detection works but sprint never engages. v3 logs activation attempts; after 5 inert tries auto-switches to LOOSE TAG INJECTION (`asc:AddUniqueGameplayTag(Character.State.Generic.Sprinting)`, removed on stop/combat) - char native OnSprintingTagChanged listener should flip move speed.
- Wayfinder ships DLSS + FSR2 plugin modules (UHT dump dirs DLSS/DLSSBlueprint/FSR2TemporalUpscaling) - relevant for fps advice on AMD (FSR2).

### 2026-07-04 - Milestone - external WPF overlay + v4 tuning (264ef4d)
- In-game PrintString overlay DOES NOT RENDER in Wayfinder shipping build (user: "overlay doesnt work") - screen debug messages disabled. Replaced with EXTERNAL overlay: mod writes `Mods/WFQoL/overlay-state.json` (pure Lua io, 1s heartbeat + on toggle; relative path w/ absolute fallback), `tools/overlay/WFQoL-Overlay.ps1` = WPF glass card (drag, resize grip + Viewbox scaling, LOCK = WS_EX_TRANSPARENT click-through, Ctrl+Alt+O global hotkey unlock via RegisterHotKey+HwndSource hook, pos/size persisted %APPDATA%/wfqol-overlay.json, dims to "offline" when heartbeat stale >5s). Launch: tools/overlay/start-overlay.bat. Smoke-tested: parses + runs.
- Lua 5.4 GOTCHA: string.format("%d", float-with-fraction) THROWS ("number has no integer representation") - use %.0f for UE float props like MaxWalkSpeed.
- Parry v4: cooldown 0.6->0.3s, schedule-collapse 0.15->0.05s, +one 120ms retry when TryActivateAbilityByClass returns false (player mid-swing). User felt v3 "slow" = internal cooldown.
- Sprint mount fix: NEVER tag-inject/speed-write the rider while IsMounted() (mount jank reported); instead boost the MOUNT actor's CharacterMovement.MaxWalkSpeed x1.4 (mount = GetAttachParentActor()), restore on dismount/stop. Keybind debounce 300ms (INS key was firing 2-3x per press = key auto-repeat).

### 2026-07-04 - Milestone - v5: mount gate fix, parry forcing, perf pass
- ON-FOOT SPRINT CONFIRMED WORKING via tag injection (log: "tag injection works (walk 550 -> 870)"). Escalation chain validated in production.
- v4 mount bug root cause: sprint loop gated on PAWN velocity BEFORE reaching the mount branch; rider velocity ~0 while mounted -> mount branch unreachable. v5 resolves mount FIRST, movement = max(mount vel, pawn vel). Mount detection: IsMounted() native + fallback attach-parent class name contains "Mount". F5 = diagnostic dump keybind (pawn class/vels/walk speeds/tags/mode) for future mount debugging.
- Parry forcing ladder: TryActivate -> ServerCurrentMontageStop(0.15) cancels player's own swing + retry -> 2 more rounds at +80ms -> log "parry FAILED". User feedback: parry lost to his own attack chain (activation rejected mid-swing).
- Constant-lag perf pass: UE4SS GuiConsole DISABLED in ini (Enabled=0, Visible=0) - the DX11 debug window is a constant frame cost + user kept it open (re-enable manually for Live View/Restart Mods; deploys now need game relaunch). Lua-side: cached WF lib + sprint class objects (invalidated on ClientRestart), meleeCache memoizes per-CLASS attack-tag verdicts so tag containers aren't iterated per activation. If lag persists: A/B disable ShowNameplates (forces every enemy nameplate widget + rescans on HP changes), then SmartSort.
- Overlay confirmed functional end-to-end on this machine (smoke test: reads state file, window runs; user's game is Borderless so it CAN draw over it). Added self-log %APPDATA%/wfqol-overlay.log + start-overlay-debug.bat (visible console) + off-screen position rescue + Activate on load. If user still sees nothing: read that log.

### 2026-07-04 - Gotcha - "overlay doesnt work" = user never launched the app (82193eb)
- %APPDATA%/wfqol-overlay.log proved the overlay was NEVER started during his session (only my smoke-test entries). Lesson: separate helper apps MUST auto-start - users won't run a second bat.
- Fix: WFQoL main.lua runs `os.execute('start "" /min <repo>/tools/overlay/start-overlay.bat')` at mod load (absolute path, brief cmd flash at boot acceptable); overlay ps1 has a named mutex ("WFQoL-Overlay-SingleInstance") so repeat launches exit instantly. Verified: double-launch test -> second instance exits.
- Parry "FAILED after 3 attempts" log spam analysis: most failures happen right after a successful parry = block ability STILL ACTIVE, re-activation correctly rejected. Added blockAbility:IsActive() check -> returns "active" = treated as success, no retries, no noise. Remaining FAILED lines are now real rejections (stamina / hard-disable states).

### 2026-07-04 - Milestone - v6: smart parry gates, stable mount boost, resident overlay (1a9e41f)
- User's "overlay window I cant click through" SOLVED + explained: he had dragged/resized the UNLOCKED card to 1908x974 (near fullscreen) - it was sitting over the game eating clicks. Overlay now: locked (click-through) BY DEFAULT, resident tray app registered at Windows login (Startup shortcut -> wscript launch-overlay.vbs -> hidden powershell; installer tools/overlay/install-autostart.ps1), card AUTO-HIDES when game heartbeat stale >5s and auto-shows when fresh, tray menu (lock/unlock, reset position, exit), Ctrl+Alt+O unlock. os.execute auto-launch REMOVED from main.lua (cmd flash gone).
- PS gotcha: checking overlay by process CommandLine match matches the CHECKING powershell itself (its own cmdline contains the search string) - verify via the overlay's log truncation instead.
- Parry gates (fire-time, per attempt): (1) willConnect(enemy,pawn) = dist <= 450u AND enemy forward-dot toward player >= 0.2 - kills "parries attacks that were never going to hit" + saves stamina; (2) stamina reserve: read HUD widget HUD_PlayerMeters_C.HUD_PlayerStaminaMeters.PlayerStaminaMeter.Percent (ShowNameplates trick - avoids FGameplayAttribute marshaling), skip parry below 35% so dash always has stamina. Parry costs stamina (shared w/ dash) - user complained drain.
- Mount boost start/stop jitter fix: game periodically REWRITES mount MaxWalkSpeed -> boost must be RE-APPLIED each 300ms tick (if cur < target-1). Plus hysteresis (start >100u/s, stop <30u/s) and combat gate IGNORED while mounted. Mount-changed handling restores old mount first.

### 2026-07-04 - Milestone - v7: real reload timing, lock-chip overlay, debug lessons (ab93cb4)
- AutoReload root cause: `CheckInWindow(InputTime float) -> OUT bInWindow` (out-param, NOT return value!) - my RegisterHook `return true` never set the out param, so forced-perfect never worked and the 300ms blind press hit the early-fail zone. REAL fix: on reload activation, poll `ab:CheckInWindow(elapsed_since_activation)` every 40ms; press Reload input the moment it returns true = presses exactly in the white zone using the game's own check. Force-hooks removed.
- Overlay v7: card + separate always-clickable LOCK CHIP window (64x24 pill glued right of card; toggles card click-through). Hotkey removed. Drag = header row only; resize = grip. Chip follows card via the 300ms poll (NOT LocationChanged - see below).
- DEBUG LESSON 1: `$window.Add_LocationChanged({...})` attached before Show() = Show() HANGS FOREVER (WPF reentrancy during initial positioning). Never hook LocationChanged/SizeChanged on these overlay windows; follow via timer instead.
- DEBUG LESSON 2: PS variable collision - row-builder loop reused `$chip` for a Border; my lock-chip Window also `$chip` -> "[Border] does not contain method Hide" at runtime. Renamed to $lockChip. Watch for var reuse in single-scope PS scripts.
- DEBUG LESSON 3 (repeat offender): this PowerShell tool aborts compound statements when an inner command sets nonzero exit - do process kills via bash loop + `taskkill //F //PID` instead.
- Mount recon aids: ClientRestart now logs pawn class; if pawn class contains "Mount" (possession-swap mounts) the pawn itself is treated as the mount. F5 diag dump exists. Awaiting user mount test with these.
- Full overlay lifecycle verified headless: start (mutex single-instance) -> hidden while heartbeat stale -> fake fresh ts in overlay-state.json -> "game heartbeat fresh - showing" -> auto-hide. Resident via wscript launch-overlay.vbs; login autostart shortcut in shell:startup.

### 2026-07-04 - Milestone - v8: MOUNT SPRINT CONFIRMED WORKING + reload call fix (b348289)
- Session 9 log: "sprint: mount speed 635 -> 889" = mount branch fires (pawn stays WFPlayerCharacter_Base_C, attach-parent mount, no possession swap). Clunk cause: velocity hysteresis kept dropping the boost, and each re-engage captured the CURRENT (sometimes mid-gallop/boosted: 893/908/1100) speed as new baseline -> compounding surges to 1540. v8: baseline captured ONCE per mount instance, boost held PERSISTENTLY while mounted (MaxWalkSpeed is a cap, harmless while standing - no velocity gate at all), re-asserted only when game drops it below target, game's own faster states left alone. Restore on dismount/toggle only.
- Reload error decoded: "error reload: function: 0x..." = UE4SS threw on `ab:CheckInWindow(elapsed)` - BP functions with OUT params need different calling. v8: adaptive checkInWindow() tries out-table style (`ab:Fn(in, outTbl)` -> outTbl.bInWindow) then multi-return style (`local ok, a, b = pcall(...)`), remembers which works. If log shows "CheckInWindow call failed in both styles" -> next option is reading window vars off the ability instance or the ranged weapon attribute set.
- User verdicts this round: chain GOOD, parry GOOD (stamina+connect gates approved), mount sprint working-but-clunky (fixed), reload broken (fixed pending test), overlay WORKS with lock chip.

### 2026-07-04 - Milestone - v9: GC crash fix, address-keyed mount, self-learning reload (839f769)
- CRASH DECODED (crash reporter): "AssembleReferenceTokenStream for GE_Player_DW_ConsumeStamina_Parry_C called on a non-game thread while GC is not locked" = parry activation lazy-loaded the SOFT-referenced stamina-cost GE off-thread. UE4SS ExecuteInGameThread executes on the next ProcessEvent call, which is USUALLY the game thread but can be an engine worker thread -> never rely on it for class-LOADING work. FIX: preload all 8 parry cost/pushback assets via LoadAsset() inside the ClientRestart hook (hooks run inline in ProcessEvent on the game thread for gameplay calls = safe).
- Mount v8 clunk root cause CONFIRMED: UE4SS RemoteObject wrappers do NOT compare with ~= (each access = new wrapper). Use :GetAddress(). v9: baselines in `mountBaselines[addr]` captured once per mount actor per session; boost persists; game's faster states untouched.
- Reload v9 = SELF-LEARNING: hooks (1) ActiveReload activation (montage name = weapon key, t0), (2) real InpActEvt_Reload presses (manual press time captured), (3) ActiveReloadSucceeded activation -> learned[montage] = pressAt, persisted to Mods/WFQoL/reload-times.txt. Auto path: learned time exists -> replay press at t-30ms; else CheckInWindow probe (now with phase-tagged error logging: "reload@isactive:", "reload@checkwindow:" etc to finally pinpoint the mystery "function: 0x..." error). USER MANUAL PERFECT RELOADS TEACH THE MOD - tell user: do one manual perfect per weapon, mod owns it forever after.
- Session 10 log: stamina reserve gate seen working ("parry skipped: stamina 13% below 35%"); parry active on beast enemies too (GA_Lurker_*).

### 2026-07-04 - Milestone - v10: window-read reload, KB+M aim assist, input pause during reload
- Reload v9 probe verdict from session 11 log: "error reload@isactive: function: 0x..." = `ab:IsActive()` on the reload GA instance is what has been throwing ALL ALONG (UE4SS throws a function value as the error, hence the weird message). Parry's blockAbility:IsActive() works fine - the difference is unexplained; rule: never call IsActive on the reload GA. User's manual reloads that session were FAILS (his own words) so the learning hook (correctly) recorded nothing.
- Reload v10 = READ THE WINDOW DIRECTLY: FModel JSON (GA_Player_RangedWeapon_ActiveReload.json) shows instance vars `WindowMin/WindowMax/WindowCenter` (normalized 0-1 of slider unless >1 = seconds) + `MaxReloadTime` (seconds) + `bUseAttributesForWindow` (window can come from WFRangedWeaponAttributeSet.ActiveReloadCenter/ActiveReloadBounds attributes). CheckInWindow is BlueprintPure sugar over these (MapRangeClamped(InputTime,0,MaxReloadTime,0,1) then InRange(WindowMin,WindowMax)). v10 reads the vars ~100ms after activation (retries up to 10x60ms), presses dead-center at `(WindowMin+WindowMax)/2 * MaxReloadTime - 0.03`. No polling, no CheckInWindow, no IsActive; guards by `currentReload.t0 == t0` session token. Learning system stays as priority-1 override.
- USER INTEL: M1 (fire) is ALSO a valid minigame trigger, and the minigame runs in AND out of combat (no combat gate exists in our code - hook-driven). CRITICAL interaction found: AutoChain's injected M1 spam during the minigame = pressing outside the window = guaranteed FAIL - very likely why auto reloads failed even when timing was right. v10: chain loop pauses while reloadInProgress() (currentReload age < maxT+0.5s), parry doParry also skips (its ServerCurrentMontageStop would cancel the reload montage). Both Attack1_36 and Reload_12 manual presses during the minigame are captured as pressAt for learning.
- AIM ASSIST FOR KB+M (new, F10, default ON): game gates soft lock/magnetism/camera assist on believing a gamepad is active. Levers: (1) hook native `/Script/Wayfinder.WFPlayerCharacter:IsUsingGamepad` -> return true, (2) hook static `/Script/Wayfinder.WFGameplayBlueprintLibrary:GetPlayerInputSource` -> return 1 (EPlayerInputSource: NONE=0 GAMEPAD=1 KBM=2), (3) set `WFGameUserSettings.bCameraAssistOnGamepad = true` at spawn (engine default is FALSE even for real controllers; bEnableCameraAimTargetingOnMouse is already true by default). Return-value override convention: RETURN the value from the RegisterHook Lua callback (ShowNameplates' OverrideReturn pattern). aimCalls counters in F5 diag prove whether the hooks actually fire. UNVERIFIED until in-game test; possible side effect: UI button prompts may flip to controller glyphs while F10 on.
- Relevant header finds for future: WFTargetingComponent (m_MagnetizedAimTarget, FindSoftLockTarget), WFGameUserSettings (GamepadAimFriction=1.0, ADSAimFriction=0.0 defaults), WFPlayerCharacter.IsMagnetizingAim.
- Overlay: 5th pill AIM/F10 added; default card height 230->258, saved config height clamped up to fit.

### 2026-07-04 - Milestone - v11: reload percent-scale fix, own soft-lock magnetizer for aim assist
- Session 12 log: reload window read WORKED but values are PERCENT: "window 40.00-60.00 max 1.50s" - v10's unit heuristic (wmax>1.001 = seconds) computed press at 50 SECONDS = mod did visibly nothing. v11: center>1.001 -> /100. Correct press for that weapon = 0.5*1.5s = 0.75s.
- Gamepad-spoof hooks CONFIRMED DEAD END: registered fine at boot, but grep of the ENTIRE FModel JSON dump found ZERO blueprint callers of IsUsingGamepad/GetPlayerInputSource - the aim gating is pure C++, C++ calls never enter the BP VM, UFunction hooks never fire. LESSON: before hooking a native for behavior change, grep the JSON dump for blueprint callers; no callers = hook is decoration.
- Aim assist v11 = OWN MAGNETIZER: pawn.TargetingComponent (UWFTargetingComponent, prop on WFPlayerCharacter) has the full soft-lock machinery as callable natives. Loop 150ms: pawn:IsAiming()/IsFreeFireAiming() -> tc:FindSoftLockTarget({Radius=3000,Yaw=40,bCanTargetFriendly=false,bIgnoreHardLockedTarget=false}, bIsResultTransient=false, bEnsureVisibility=true) -> stored results read from tc.m_CurrentSoftLockTargetResults.AssociatedTargets[1] (property read avoids UE4SS struct-return marshaling) -> write tc.m_MagnetizedAimTarget = target (same slot the native gamepad path fills). Cleared when not aiming or F10 off. F5 diag: magnet=<class> magnetizing=<bool> aiming=<bool>.
- If magnetism consumption ALSO turns out gamepad-gated in C++ (magnet set but no pull felt): plan C = manual camera nudge via AddControllerYawInput/PitchInput toward GetMagnetizedAimTargetSocketLocation each tick while aiming - full custom assist, cannot be gated.

### 2026-07-04 - Milestone - v12: unfailable reload (window operand forcing), camera-pull aim assist
- Reload "works sometimes" DECODED: session 13 log showed presses landing 0.72s = dead-center of the DISPLAYED window every time, yet no successes. CheckInWindow's locals (GA JSON) reveal it reads an ASC ATTRIBUTE (WFRangedWeaponAttributeSet.ActiveReloadCenter/ActiveReloadBounds) + instance `Tolerance` (3.0) via Add/Subtract+InRange - NOT the displayed WindowMin/WindowMax. True window = attr center +/- ~3 percent; display center != true center on some weapons/rolls.
- v12 reload = FORCE THE CHECK'S OPERANDS on every activation (game-thread hook, before any input): ab.Tolerance=1000, ab.EarlyPressForgivenessPercent=0, all WFRangedWeaponAttributeSet instances' ActiveReloadBounds.CurrentValue/BaseValue=200. ANY press by ANYONE at ANY time = perfect; user manual fail now impossible (better than blocking input). Learning system removed (dead weight). Timed center press kept as hands-off auto path. "reload: PERFECT" logged on every success; "reload: window forced open" once per session.
- Aim assist v12 = CAMERA PULL, cannot be gated: magnetizer (v11, works - log proof) feeds target; new 33ms loop rotates camera toward GetMagnetizedAimTargetSocketLocation via pawn:AddControllerYawInput/AddControllerPitchInput. Look-at math in plain Lua (atan2 from camera loc, no struct passing). Tuning consts: PULL_RATE=0.15/tick, PULL_CAP=1.5deg, PULL_CONE=20deg, PULL_DEADZONE=0.3deg, INPUT_SCALE=1/2.5 (engine deg-per-input-unit), pitch input negated (engine convention). Also WFGameUserSettings.ADSAimFriction 0->1 (native friction lever). Gamepad-spoof hooks deleted (proven decoration).
- Terminology for user comms: "aim friction" (slowdown over target, exposed in settings for mouse) is only 1 of 3 aim assist parts; magnetism + camera assist are the felt pull and were gamepad-locked - now home-built.
- F5 diag: "magnet=<cls> magnetizing=<bool> aiming=<bool> pull=<yaw>/<pitch>".

### 2026-07-04 - Milestone - v13: reload press via GA's own callback, configurable aimbot w/ overlay sliders
- v12 double-reload DECODED from log: every activation was followed by a second one ~1s later = our injected InpActEvt_Reload press. The minigame listens via WFAbilityTask_HandlePlayerInputPress (OnTrigger delegate, sig (float ElapsedTime)); pawn InpActEvt injection NEVER reaches it - it just retriggers the reload ability after the current one ends. Zero ActiveReloadSucceeded activations across ALL sessions = no buff ever, even on user's visually-perfect presses.
- v13 press = call the GA's OWN bound press handler directly: `ab:OnTriggered_99CA32B34E7E0E8640D86BAE3837FFCB(elapsed)` (+ fallback `OnTriggered_0C5309BB4DBB1136C0E5DEBE6E42DF1A` 250ms later if ability still alive) - executes the exact graph path real input takes: CheckInWindow(forced open) -> success branch. LESSON: BP ability input = delegate-bound OnTriggered_<GUID> UFunctions on the GA instance; call those to simulate input, never the pawn's InpActEvt stubs.
- New instrumentation/hooks: GA K2_OnEndAbility -> clears currentReload (stale press can't fire after end - kills double-reload from any path) + "reload: ended at Xs"; GCNA_2HR_ActiveReload_Success/Fail K2_HandleGameplayCue (classes added to PRELOAD list to be hookable at spawn) -> ground truth "reload: success cue"/"FAIL cue". BUFF GUARANTEE: onReloadEnded waits 250ms; if no success signal seen and our press fired -> asc:TryActivateAbilityByClass(ab.ReloadSuccessType) -> "reload: buff FORCED".
- Aimbot v13 (user request): overlay gained AIM CONFIG section (FOV slider 2-90, SMOOTH slider 0.02-1.0, "ADS only" checkbox) -> writes Mods/WFQoL/aim-config.json (300ms debounce) -> Lua polls 1s, hot-applies + logs "aim: config ...". smooth=1.0 -> hard snap (cap 8deg/33ms tick); adsOnly=false engages while M1 held too. Magnetizer acquisition cone = fov*1.5. Card 258->352 tall.
- Overlay/PS lesson: sliders need unlocked card (lock chip) to interact; aim-config load/save via Split-Path $StateFile; PS 5.1 Stop-Process on CIM-matched PIDs inside try/catch (bare pipeline exits 255 through this tool).

### 2026-07-04 - Milestone - v14: aimbot via SetControlRotation (input-additive route dead)
- RELOAD CONFIRMED WORKING by user + log (session 15): "reload: success cue" x3 + "ended" per reload, no doubles, no fails, buff granted via real success path (no FORCED lines needed). The OnTriggered_<GUID> direct-call approach is the keeper.
- Aimbot v13 failure decoded: AddControllerYawInput/AddControllerPitchInput calls executed errorless (no aimpull errors, magnetizer acquiring, config hot-reload working) but ZERO camera movement - the game's input pipeline (ADS dampen scalars on WFPlayerCharacter, GamepadAimDampenScalar/ADSAimDampenScalar) scales additive controller input to nothing. LESSON: for camera control in Wayfinder, additive input API is a dead end; WRITE the rotation.
- v14: controller:SetControlRotation({Pitch,Yaw,Roll}) per 16ms tick, exponential approach (step = remaining-delta * smooth, cap 12deg*smooth/tick, pitch clamped +/-75, deadzone 0.25deg). Mouse still wins because every tick starts from the CURRENT (user-moved) rotation. "aim: pulling (dyaw=X dpitch=Y)" logged once per engagement = proof the write path runs. Magnetizer loop 150->100ms for snappier acquisition.
- Overlay sliders confirmed live in log ("aim: config fov=90 smooth=0.02 adsOnly=false" as user dragged) - note user had left smooth at 0.02 (weakest possible) during the "not working" test; even so, v13's additive route showed nothing at ANY smooth, so the pipeline was the real blocker.

### 2026-07-04 - Milestone - v15: MAGIC BULLETS (projectile homing), camera pull demoted to opt-in
- User verdict on camera aimbot: works ("aim: pulling" fired) but useless feel. Pivot to magic bullets per user.
- PROJECTILE API GOLD (UHT dump): ALL projectiles derive from `AMayhemBaseProjectile` (AMayhemProjectile, AMayhemBeamProjectile children). Native BlueprintCallable `SetHomingTarget(const AActor*)` on the base; `MovementComponent` = UAirshipProjectileMovementComponent : UProjectileMovementComponent (stock homing: bIsHomingProjectile / HomingTargetComponent / HomingAccelerationMagnitude). Movement comp also has `FakeNewHit(Actor,Component,Location,Normal)` / `FakeHit(HitResult)` = guaranteed-hit nuclear option (protected UFUNCTION, still callable via UE4SS) - PLAN B if homing curves too weak.
- v15: NotifyOnNewObject("/Script/Wayfinder.MayhemBaseProjectile") -> +30ms game thread -> filter proj:GetInstigator() addr == pawn addr -> target = magnetized (or on-demand FindSoftLockTarget for instant hipfire) -> SetHomingTarget + bIsHomingProjectile=true + HomingAccelerationMagnitude = 2000 + strength*38000. Throttled "bullets: homing -> <class>" log.
- Gating split: ACQUISITION (magnetizer) = aiming OR m1Held, always; CAMERA PULL = own `pull` config (default OFF) + adsOnly. aim-config.json now {fov,strength,smooth,bullets,pull,adsOnly}; overlay: STRENGTH slider + MAGIC BULLETS / CAM PULL / ADS-only checkboxes (Add-CfgCheck helper), card 400 tall.
- Weakpoint stretch if user wants heads: check which component native SetHomingTarget picks; override MovementComponent.HomingTargetComponent to mesh/weakpoint child actor.

### 2026-07-04 - Milestone - v16: FakeHit magic bullets (plan B), FOV-only config
- v15 homing NEVER ENGAGED: log had config + magnetize lines but ZERO "bullets:"/error lines - NotifyOnNewObject("/Script/Wayfinder.MayhemBaseProjectile") never reached the handler body (either no derived-class notify or silent instigator bail; couldn't distinguish, so v16 nukes both possibilities).
- Player shot class chain mapped via FModel: WFProjectile_2HR_Slug_C -> /Game/Blueprints/Projectiles/WFProjectile_Base_BP.WFProjectile_Base_BP_C -> /Script/Wayfinder.MayhemProjectile -> MayhemBaseProjectile. Enemy spits use the SEPARATE AWFBallisticProjectile hierarchy (good: not catching those on the Mayhem notifies).
- v16: notify registered on ALL THREE levels (both natives at boot + BP base in ClientRestart after preloading it via LoadAsset; PRELOAD list gained WFProjectile_Base_BP). addr-keyed seenProj dedup (same object can notify on several levels). ALWAYS-log diag (2s throttle): "bullets: saw <cls> inst=<cls> owner=<cls>" - next session log definitively answers who spawns what with which instigator. Ownership relaxed: instigator addr OR owner addr == pawn, OR instigator class == WFPlayerCharacter_Base_C.
- Magic = FakeNewHit(target, target:K2_GetRootComponent(), targetSocketLoc, normal(projLoc-tgtLoc)) on proj.MovementComponent = game's own guaranteed-impact API -> "bullets: FORCED HIT -> <cls>". Errors fall back to SetHomingTarget + 40k accel. ONE gate: angle(camera fwd, to-target) <= fov (plain Lua vector math from GetControlRotation).
- Config/UI simplified per user: aim-config.json {fov, bullets, pull:false}; overlay = FOV slider + MAGIC BULLETS checkbox only (STRENGTH/SMOOTH/CAM PULL/ADS rows removed, card 310). Lua still parses old keys w/ defaults (pull stays available by hand-editing json).

### 2026-07-04 - v17: per-shot projectile hooks (pooling killed spawn notifies)
- Session 17 log: all 3 NotifyOnNewObject registrations clean ("bullets: BP projectile notify registered") yet ZERO "bullets: saw" during active shooting = projectiles are POOLED (AWFBallisticProjectile ships IPoolableProjectileInterface; Mayhem tree evidently pooled too). Constructed once at load, reused per shot -> object-creation notifies structurally can't see shots. LESSON: for per-shot logic in Wayfinder, hook per-launch UFUNCTIONS, never object construction.
- v17: tryHook on WFProjectile_Base_BP_C:ReceiveBeginPlay + ComputeInitialSpeed + FindTargetActor (per-launch setup fns on the BP base; slug child overrides ReceiveBeginPlay but Compute*/FindTargetActor live on base). Handler unchanged (diag "bullets: saw", FOV gate, FakeNewHit, homing fallback). Dedup switched addr-forever -> addr+0.5s window (pooled actors reuse addresses across shots!). Notifies kept for fresh spawns.

### 2026-07-04 - v18: HITSCAN magic bullets (the actual answer)
- Session 18 log: per-launch BP hooks registered clean, user shot 4 mags, ZERO handler hits. FModel dissection of GA_Player_RangedWeapon_Fire_Batched CDO = smoking gun: **IsHitscanWeapon = true**. Basic rifle fire NEVER spawns projectiles - it line-traces (TraceTargetActor = WFTargetActor_LineTrace, NumberOfTraces=12, BaseSpread=12, HitGameplayEffect GE_Damage_Player_Rifle_Shoot_Standard). ProjectileToSpawn (WFProjectile_2HR_Slug) only used on the non-hitscan path. THREE detection rewrites chased projectiles that never exist for basic attacks.
- THE LEVER: AWFTargetActor_Trace.EndpointAimType (EWFTargetActorEndLocationAimingType: 0=AimAtStartLocation 1=AimAtSoftTargetActor 2=AimAtCameraForward 3=AimAtEndLocation). Set =1 -> trace endpoint = the soft-lock target (our magnetizer feeds it) regardless of camera. Plus spread zeroing on BOTH the GA instance (BaseSpread/AimSpreadModifier/SpreadIncrement/SpreadMax) and the trace actor (BaseSpread/AimingSpreadMod/TargetingSpreadIncrement/TargetingSpreadMax).
- v18: hook K2_ActivateAbility on all 4 fire GAs (Fire_Batched/_Burst/_Rail/_Shotgun) -> zero spreads + EndpointAimType = target-in-FOV and 1 or 2. currentBulletTarget(pawn) = magnetized-or-fresh soft lock + FOV angle gate. Logs: "bullets: hitscan locked -> <cls>" / "hitscan zero-spread (no target in fov)". Projectile path (v17 hooks + notifies) KEPT for the weapons that do spawn projectiles (weapon abilities, other rifle types).
- LESSON: before building shot-manipulation, check IsHitscanWeapon on the fire GA. Wayfinder basic ranged = hitscan; projectiles = weapon ABILITIES only.

### 2026-07-04 - v19: per-shot trace TA stamping (fresh TA per shot killed v18)
- Session 19 log: fire GA hooks LIVE per shot ("bullets: hitscan locked -> AI_Blaster" + FOV gate working "no target in fov") but traces still don't bend. Root cause: GAS WaitTargetData spawns a FRESH WFTargetActor_LineTrace PER SHOT - v18 edited the previous shot's dead TA via ab.TraceTargetActor. Same pooling-family lesson inverted: this object is per-use disposable.
- v19: hook the per-shot config calls on the NEW instance: /Script/Wayfinder.WFTargetActor_Trace:SetStartLocation + SetShouldProduceTargetDataOnServer (native BlueprintCallable, hookable, run between spawn and trace) + NotifyOnNewObject on WFTargetActor_LineTrace (fresh spawn = notify works here). Handler: gate ta.MasterPC valid (= player-owned; enemy TAs have no PlayerController - do NOT stamp theirs, would aim enemy shots), zero TA spreads, EndpointAimType=1 when currentBulletTarget(pawn) in FOV. Log "bullets: trace TA locked -> X".
- Damage-direct fallback researched + parked: ApplyHitscanEffect/HandleTargetData all take FGameplayAbilityTargetDataHandle (opaque TSharedPtr innards, not constructible from Lua). If v19 fails: UAbilitySystemBlueprintLibrary:AbilityTargetDataFromActor(target) -> pass handle to ab:ApplyHitscanEffect(handle) - struct round-trip through UE4SS untested.

### 2026-07-04 - v20: DIRECT damage injection (replicate manual hit), trace-redirect gave up
- Session 19 log: trace TA path fired ("trace TA zero-spread/locked") + fire GA hooks fired per shot, but EndpointAimType=1 / spread-zero still did NOT bend visible shots or land guaranteed hits. Redirecting GAS trace geometry is a dead end via UE4SS (fresh TA per shot, aim-mode enum semantics unclear, target data produced server-side).
- v20 = replicate a manual hit DIRECTLY: on fire GA activation w/ target in FOV, ExecuteWithDelay(60) [let ability set Source Weapon] -> handle = /Script/GameplayAbilities.Default__AbilitySystemBlueprintLibrary:AbilityTargetDataFromActor(target) -> ab:HandleTargetData(handle) [full pipeline: damage GE + hit cues], fallback ab:ApplyHitscanEffect(handle) if HandleTargetData errors on actor-only data (no FHitResult). This IS the call a real connecting trace makes - correct source/level/GE, camera-independent. Log "bullets: MAGIC HIT (HandleTargetData|ApplyHitscanEffect) -> X".
- Kept trace-redirect + spread-zero as cosmetic layer (visual beam) but damage no longer depends on it. Projectile FakeHit path kept for ability projectiles.
- WEAKPOINT: AbilityTargetDataFromActor targets actor root = body hit (guaranteed dmg, likely no weakpoint mult). Weakpoint needs AbilityTargetDataFromHitResult with FHitResult naming the weak bone/component - FHitResult construction from Lua is the hard follow-up (big struct). Deliver body-guaranteed first.
- KEY UNTESTED: struct round-trip - passing the FGameplayAbilityTargetDataHandle returned by one UE4SS call straight into another. If log shows "bullets-magichit" errors -> handle didn't survive marshaling; next = check if UE4SS exposes the handle as usable param or need OutTargetData pattern.

### 2026-07-04 - v21: real AIMBOT (camera snap), magic bullets shelved
- Magic bullets FINAL verdict: v20 HandleTargetData ran clean every shot (struct round-trip WORKED, "MAGIC HIT" spam, hit enemies+breakables+turtlesaur) but NO actual damage - HandleTargetData with actor-only target data (no FHitResult) no-ops the damage application. Every trace/projectile/target-data avenue exhausted across v15-v20. User: "not working... lets just do aimbotting, inject into aimbot like a cheat". OFFLINE single-player game = legit cheat mod.
- v21: the v14 SetControlRotation camera-snap (which worked mechanically, "aim: pulling") promoted to primary AIMBOT, un-gated (was hidden behind pull:false default all along - that's why it seemed useless). aimbotEngaged = state.aim(F10) + aimCfg.aimbot + (IsAiming OR m1Held). Snap cap raised 12->40 deg/tick*smooth so high SNAP truly locks. Aim point = GetMagnetizedAimTargetSocketLocation (chest). Pitch clamp +/-80. Log "aimbot: locking <cls>".
- Config simplified: aim-config.json {fov, smooth, aimbot, bullets}. Overlay AIMBOT CONFIG: FOV slider (2-90) + SNAP slider (0.02-1, 1=instant lock) + AIMBOT checkbox + MAGIC BULLETS checkbox (default off). F10/pill/logs renamed Aimbot. Card 368 tall. Defaults fov=30 smooth=0.5 aimbot=on bullets=off.
- Magnetizer loop (acquires m_MagnetizedAimTarget) still feeds the aimbot; unchanged.

### 2026-07-04 - v22: HARD LOCK aimbot (exact-rotation write, sticky target)
- User: "hard lock aimbot, very snappy". v22: smooth>=0.99 = write EXACT look-at rotation every 16ms tick (no interpolation) = camera glued to target. Sticky lock: lockedTarget held until dead or leaves fov*2 leash (no jitter between targets). Deadzone removed (min dist 30). Defaults raised: fov 40, smooth 1.0 (instant). Below 0.99 smooth still eases (cap 60*k deg/tick).
- DIAGNOSTIC added: one-time "aimbot: write check wanted yaw=X, now=Y" 50ms after first lock - if now!=wanted, the game OVERRIDES SetControlRotation (custom camera) and we need a different write target (PlayerCameraManager rotation / camera component / pawn actor rotation). This log line is the decider for whether SetControlRotation is even the right lever - never actually confirmed across v14-v21 (user never said camera moved).
- If write-check shows revert: next levers to try - controller has a camera manager; Wayfinder may use a spring-arm/custom third-person cam. Options: PlayerCameraManager:SetActorRotation, find CameraComponent + set world rotation, or pawn:K2_SetActorRotation (if camera follows pawn facing). Check WFPlayerCharacter camera setup in headers first.

### 2026-07-04 - v23: aimbot CONFIRMED working + overlay never-hide + off-screen fix
- "WHERE IS THE OVERLAY": (1) window parked at X=1943 (off right edge of 1920 screen) - config left=1943; off-screen guard only checked VIRTUAL desktop bounds (2nd monitor made 1943 "valid") -> now ALSO clamps vs PrimaryScreenWidth. Reset to 60,220. (2) overlay auto-HID whenever game heartbeat stale >5s (alt-tab/loading/menu) = vanished constantly -> REMOVED auto-hide; now ALWAYS visible while running, shows "offline" text + dims combat dot when stale. Kill overlay via bash taskkill loop (PS CIM Stop-Process exits 255 through the tool).
- AIMBOT CONFIRMED WORKING (session log): "aimbot: write check wanted yaw=127.7, now=127.4" = SetControlRotation STICKS (matched within 0.3deg). This finally proves SetControlRotation is the correct camera lever - control rotation writes hold, camera follows. First confirmation across v14-v22.
- v22 only locked ONCE in 48s because aimbotEngaged required (IsAiming or m1Held) AND magnetizer had gated target. v23: aimbotEngaged = state.aim and aimCfg.aimbot (always on when F10), aimbot loop acquires its OWN target via FindSoftLockTarget (independent of magnetizer gate). Now hard-locks any enemy in FOV cone continuously. Sticky lockedTarget + exact-rotation write (smooth 1.0) still in place.

### 2026-07-04 - v24: PURGED aimbot + magic bullets (user call)
- User done with aim features ("fuck all this aimbot and bullet magic, just fully purge it"). Removed ENTIRELY from main.lua: AimAssist/magnetizer, camera-pull aimbot, magic bullets (projectile FakeHit, hitscan trace-redirect, target-data damage inject), all aim hooks (projectile BP, WFTargetActor_Trace, fire GAs), applyAimSettings, aim-config parser, F10 bind, aim state key, aim in overlay-state.json, F5 diag magnet lines, PRELOAD WFProjectile. From overlay: AIMBOT pill, entire AIMBOT CONFIG section (FOV/SNAP sliders, checkboxes), Save-AimCfg, aim pill set, card 368->240. Deleted aim-config.json. Aim reference docs (soft-lock/magnetizer/SetControlRotation/hitscan/projectile APIs) retained in reference-wayfinder-ue4ss-api.md for future.
- WFQoL now = AutoChain(F7) + AutoParry(F8) + AutoSprint(F6) + AutoReload(F9) + overlay(4 pills). All confirmed working pre-purge. Clean base for NEW mods going forward.

### 2026-07-04 - v25: AutoLoot, overlay redesign, lifecycle fix, engine.ini perf, hitch log
- OVERLAY LIFECYCLE: removed Windows login-autostart (Startup shortcut - spawned overlay on PC boot). Mod now launches overlay on ClientRestart via os.execute wscript launch-overlay.vbs (once, guarded). Overlay self-EXITS 20s after heartbeat stale (game close/crash/alt-f4). install-autostart.ps1 repurposed to REMOVE the old shortcut.
- OVERLAY REDESIGN (user "not AI generated style"): full rewrite. Flat #0F1116 sharp-corner window (WindowStyle=None, NO AllowsTransparency/rounded/glass), Cascadia Mono font. Header=drag anchor + inline lock SQUARE (color: green unlocked/red locked, toggles move+resize freeze) + close x. REMOVED floating lock-chip window + WS_EX_TRANSPARENT click-through entirely. Rows are clickable buttons (hover highlight) that TOGGLE the mod. Visible resize grip = Polygon triangle + transparent Thumb (DragDelta resize) bottom-right. Always-autosaves pos/size (geometry-diff check in 250ms poll timer + on drag/resize end).
- CLICK-TO-TOGGLE channel: overlay row click writes Mods/WFQoL/overlay-cmd.json {seq=unix-ms, feature}; mod polls 200ms, toggles state[feature] on new seq (records pre-existing seq at load w/o applying = stale-click guard). Generic - works for any feature key.
- AUTOLOOT (F4, new): loot vacuum. LoopAsync(400) -> FindAllOf("WFPickup") (AWFPickup C++ base, catches BP subclasses) -> within LOOT_RANGE 3000u: set bRequireInteractionToCollect=false + bCanBePickedUp=true, K2_SetActorLocation onto player (overlap collects). Logs distinct pickup classes once + "vacuumed N" throttled 3s. UNTESTED in-game.
- USEFUL SELF-LOGS: (1) parry "no timing for <class> (default)" once per unlisted attack -> extend timings.lua offline; (2) perf "hitch %.1fs (combat=X)" when writeState 1s loop gaps >2s = real stutter for engine.ini correlation, throttled 10s.
- ENGINE.INI PERF (clarity/visibility/fps, kept looks): EmitterSpawnRateScale 2->1, ParticleLODBias -2->0, ParticleDistanceRelevanceScale 2->1, SSR.Quality 4->2, VolumetricFog.GridSizeZ 128->64, +DepthOfFieldQuality=0. File is RO-locked (game can't overwrite); backup Engine.ini.wfqol-bak; repo copy tools/optimization/Engine.ini + README. Active path %LOCALAPPDATA%\Wayfinder\Saved\Config\WindowsNoEditor\Engine.ini (NOT the game folder Saved which is empty).
- CLEANUP: dropped dead KISMET local. Audited growth tables (seenErrors/meleeCache/mountBaselines/lootSeen/lastToggle/registered) all bounded per-key - NO leaks. aim/bullet purge from v24 confirmed clean.
- WFQoL now: F4 loot / F6 sprint / F7 chain / F8 parry / F9 reload / F5 diag + redesigned overlay (5 rows, click-toggle).

## v26 (2026-07-04) - game crash after mount + AutoLoot PURGED, UE4SS dev-tooling purged
- CRASH: Fatal Error 18:52:53 while mounted/galloping (crash_2026_07_04_18_52_53.dmp). UE4SS.log clean-cut at 18:52:49 ("sprint: mount baseline 724, boosting to 1014"), no error line = NATIVE crash (Lua errors are pcall'd + logged, never crash the process). Prime suspect = AutoLoot: only new untested engine-mutating loop running (400ms). Two traps hit - FindAllOf("WFPickup") returns POOLED + CDO (Default__) instances, and K2_GetActorLocation/K2_SetActorLocation on those native-segfaults THROUGH pcall (pcall can't catch native AVs); plus re-teleporting a pickup that destructs on collection-overlap = use-after-free. No "loot:" log lines is consistent (segfault mid-loop before buffer flush).
- FIX (user "no need for autoloot mod, can purge"): AutoLoot fully removed from main.lua - state.loot, LOOT_RANGE/lootSeen/lootVacuum + LoopAsync(400), F4 bind, "loot" key from writeState json + header/loaded comments. Overlay: removed LOOT row (features array) + Set-Row loot. WFQoL now F6 sprint / F7 chain / F8 parry / F9 reload / F5 diag. Overlay = 4 rows.
- PURGE UE4SS dev-tooling (user "purge all non mods thing, less crashes/perf"): mods.txt CheatManagerEnablerMod/ConsoleCommandsMod/ConsoleEnablerMod/BPModLoaderMod/BPML_GenericFunctions -> 0. Kept gameplay mods ShowNameplates:1, SmartSort:1 (both Lua, not dumpers), WFQoL:1, built-in Keybinds:1. ActorDumper/SplitScreen/LineTrace/jsbLuaProfiler already 0. Re-enable any if needed later. mods.txt is game-side (D:/.../Win64/Mods/mods.txt), NOT in repo.
- LESSON: never enumerate pooled UObject classes (WFPickup/projectiles/trace-target) via FindAllOf and call native actor methods on the results - pcall is NO protection against native access violations. Skip Default__ CDOs + dedup by address at minimum, or avoid the whole approach.

## v27 (2026-07-04) - transition crash hardening (settle-gate + CharacterMovement validation)
- CRASH #2 (post-AutoLoot-purge): EXCEPTION_ACCESS_VIOLATION reading 0x1c, callstack all UE4SS frames = our Lua->native call crashed the game thread. Happened on a BREACH/level transition (last UE4SS.log lines = ShowNameplates re-registering HUD hooks 19-22 on the new level, then instant crash). Crash #1 was open-world galloping (streaming). Common thread = WORLD STREAMING / level transition: actors tear down while our always-on game-thread loops keep touching them. 0x1c = null-pointer + small offset deref (e.g. null CharacterMovement sub-object). pcall does NOT catch native access violations.
- FIX (WFQoL side): (1) SETTLE gate - transitionAt=os.clock() set in ClientRestart hook; settling()=(clock-transitionAt)<1.5s; AutoSprint loop + onEnemyAbility + doParry all bail while settling() so no native actor access during the (un)construction window. (2) CharacterMovement validation - new maxWalk fetches actor.CharacterMovement, IsValid-checks it, THEN reads MaxWalkSpeed; new setMaxWalk(actor,v) validates actor + CM before writing. All 5 raw `X.CharacterMovement.MaxWalkSpeed=Y` write sites routed through setMaxWalk. (3) metersCache cleared on ClientRestart (HUD widgets rebuilt per level; stale ref = another null-deref vector).
- NOTE: AutoSprint is WFQoL's ONLY always-on game-thread native loop (runs every 300ms gated just by `ready`, which stays true across transitions). If transition crash persists after v27, suspect ShowNameplates (its HUD/nameplate hooks were the last thing logged before the crash) - bisect by disabling ShowNameplates+SmartSort for a WFQoL-only run.
- RULE: any always-on game-thread loop must validate sub-objects (CharacterMovement/ASC/HUD widgets) before native access AND pause during level transitions - actors are torn down mid-stream and pcall can't save you.

## v28 (2026-07-04) - overlay no-focus-steal (fix bleep + mouse-unfocus)
- SYMPTOM: random "bleep" + mouse unfocuses from game mid-play. Cause = a window grabbing FOREGROUND focus off the borderless game (Windows plays the window-switch/default beep on focus change).
- OVERLAY was one source: Topmost window + $window.Activate() on load + poll timer re-Show()ing when IsVisible false = re-grabbed focus whenever the game hid it. FIX (WFQoL-Overlay.ps1): ShowActivated="False" on the Window + WS_EX_NOACTIVATE via Win32 interop (GetWindowLong/SetWindowLong GWL_EXSTYLE -20, 0x08000000) applied at SourceInitialized; removed the Activate() call. Clicks on rows STILL register (NOACTIVATE != TRANSPARENT), window just never becomes foreground. Hot-applied: killed old overlay PID + relaunched via VBS.
- OTHER source = the AGENT's own shell commands: every Bash/PowerShell/deploy I run while user is in-game spawns a cmd/conhost/powershell console that steals foreground + dings. Mitigation: batch/minimize shell commands during active play; the wscript-hidden overlay launch itself does NOT flash.
- GOTCHA (self-inflicted): Get-CimInstance ... Where CommandLine -like '*WFQoL-Overlay*' MATCHES ITS OWN QUERY PROCESS (the pattern text is in the -Command line). Looked like duplicate overlays; was false. Exclude with -notmatch '-Command'. Mutex "WFQoL-Overlay-SingleInstance" works - single instance confirmed.
- SESSION WIN: v27 transition hardening held - no crash 19:28->19:45+, reload/parry/overlay-click all working in log.

## v29 (2026-07-04) - log-driven tuning: parry stamina reserve + bulletproof ClientRestart
- LOG REVIEW (19:28->19:51, clean/no-crash, reload PERFECT throughout, overlay no-activate working): two improvements.
- (1) STAMINA_RESERVE 0.35 -> 0.20. Log showed constant "parry skipped: stamina X% below 35%" at 25/26/28/31/33/35% = mod refusing parries right when needed. AutoParry exists to land parries; a blocked hit beats a saved dash. 0.20 keeps a small dash buffer, lands far more parries.
- (2) ClientRestart callback FULLY pcall-wrapped. Log had 2x "Error executing hook pre-callback ClientRestart: ... Was unable to register a hook" on transitions. WFQoL's registerAll is a no-op on re-entry (registered[] guard) so likely NOT us (prob ShowNameplates re-hooking HUD), but wrapping the whole body means WFQoL can PROVABLY never emit that line now. If it persists post-v29 = ShowNameplates confirmed.
- "no timing for <class>" entries (GA_AI_Atk_Melee_L/R/_ALC_C, GA_AI_Atk_180_L/R_ALC_C, GA_Elemental_Flowing_Bubble_C, GA_Turtlesaur_Atk_Bite_Swamp_C) left at default 0.6s - can't extract real frame data from logs, and fire-time willConnect recheck makes exact timing non-critical. Extend timings.lua offline if a dump becomes available.
- Applies on NEXT game launch (hot-reload disabled). Needs test: fewer stamina skips, more parries landing.
