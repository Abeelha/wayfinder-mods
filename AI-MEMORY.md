# AI Memory - wayfinder-mods

Per-repo memory. Append-only, concise. Format: `### YYYY-MM-DD - Category - entry`.

---

### 2026-07-04 - TechStack - Wayfinder UE4SS setup facts
- Game dir: `D:/SteamLibrary/steamapps/common/Wayfinder`, exe dir `Atlas/Binaries/Win64` (project name = Atlas)
- EAC is dead post-Echoes: `EasyAntiCheat/Settings.json` points at `Atlas/Binaries/Win64/WayfinderClient.exe` which does not exist. Nexus UE4SS mods run fine.
- UE4SS v3.0.1 (flat layout: dwmapi.dll + UE4SS.dll + Mods/ at exe dir). Wayfinder REQUIRES custom `UE4SS_Signatures/GUObjectArray.lua` AOB sig (shipped by ShowNameplates + SmartSort, identical) AND `bUseUObjectArrayCache = false` in UE4SS-settings.ini.
- Native class prefix `/Script/Wayfinder.` (e.g. `PlayerInventoryComponent:CLIENT_NotifyItemAdded`). Player pawn BP: `/Game/Blueprints/Main/WFPlayerCharacter_Base.WFPlayerCharacter_Base_C`.
- Game uses GAS. Player abilities: `Atlas/Content/Blueprints/Player/GAS/GameplayAbilities/` - weapon families 2H / 2HR / DW / SnS / RangedWeapon. Combo GAs `GA_Player_2H_LightAttack1..4`, parry `GA_Player_2H_Parry{,_Light,_Heavy}`, block `GA_Player_Block{,_2H}`. `GA_Player_Melee_Base` holds `Attack1PressCombo`, `HasComboFollowUps`, `InputTask`, CallFuncs `HandlePlayerInputPress/Hold/ReleaseWithAbility`.
- Char BP input UFunctions: `InpActEvt_Attack1_K2Node_InputActionEvent_36/_37` (M1), `InpActEvt_Attack2_K2Node_InputActionEvent_40/_41` (M2). Pressed vs released edge = TBD at runtime (recon phase).
- Full FModel JSON dump in repo `Atlas/` (gitignored), from Nexus mod 2 ("serialised file").

### 2026-07-04 - Milestone - Runtime recon done (WFRecon session 1), verified APIs
- Input edges CONFIRMED: `InpActEvt_Attack1_..._36` = PRESS, `_37` = RELEASE. Attack2: `_40` = press, `_41` = release. M1 hold produces NO extra events (game has no native auto-chain).
- ALL enemy generic attacks flow through `GA_AI_Base_C:K2_ActivateAbility` (base hook catches Skirmisher/Brawler/Rifleman/etc subclasses). Enemy attack GAs tagged `Ability.Characteristic.Attack` + `.Melee` (verified GA_AI_Atk_Melee defaults).
- Weapon defense model differs per family: SnS = `GA_Player_Block_SNS` (hold), 2H = `GA_Player_Block_2H` + `GA_Player_2H_Parry*`, DW = `GA_Player_DW_Parry` (active parry w/ InvulnEffect + `DW_Parry_Pushback` counter). DW M2 = HeavyAttack_Flourish, NOT block - Block is its own input (`EWFGameplayAbilityInput::Block`, tag `Input.Combat.Block`).
- Player ASC = `WFPlayerAbilitySystemComponent` living on PLAYERSTATE as `UnrealAbilityComponent`, NOT on pawn. Clean access: static lib `/Script/Wayfinder.Default__WFAbilitySystemBlueprintLibrary` -> `GetWFPlayerAbilitySystemComponent(actor)`.
- Key callable natives (UHT dump `Win64/UHTHeaderDump/Wayfinder/Public/`): `WFPlayerAbilitySystemComponent:GetAbilityFromInputTag(FGameplayTag)` (weapon-agnostic block/parry lookup), `AbilitySystemComponent:TryActivateAbilityByClass(class, bool)`, `WFAbilitySystemComponent:AddGameplayTag/RemoveGameplayTag`, `WFAbilitySystemBlueprintLibrary:AUTH_TryGiveAndActivateAbilityOnce(...)`.
- Combo system: melee GA waits on `WFPlayerInputAsyncQuery*`/`WFAbilityTask_HandlePlayerInput*` tasks listening for INPUT TAGS; next ability class per attack in `Attack1PressCombo`/`Attack2PressCombo` properties.
- Sprint: `GA_Player_Sprint_C:K2_ActivateAbility` hook works. `WFPlayerCharacter:IsPlayerSprinting()` pure native. Combat tag `Character.State.Generic.InCombat`.
- GAME CRASHED (EXCEPTION_ACCESS_VIOLATION, UE4SS frames) ~200s into session. Prime suspect: WFRecon's LoopAsync combat probe touching UObjects OFF game thread (probe also never logged = failing silently in pcall). RULE: every LoopAsync body that touches UE objects goes inside ExecuteInGameThread. WFRecon removed from game + repo (git history baf5ab0).
- Session log archived: scratchpad/UE4SS-session1.log. Object dump: Win64/UE4SS_ObjectDump.txt (from GUI Console tab "Dump Objects & Properties" button; Dumpers tab "Generate UHT Compatible Headers" for the SDK).

### 2026-07-04 - Milestone - AutoChain + AutoParry + AutoSprint v1 deployed
- AutoChain (F7): hooks Attack1 press/release edges for held state; LoopAsync 70ms alternates synthetic press/release calls of the char's own `InpActEvt_Attack1_*` UFunctions; `injecting` guard flag prevents self-hook feedback loop.
- AutoParry (F8): hooks `GA_AI_Base_C:K2_ActivateAbility`; filters melee attacks by AbilityTags; range gate 700u; activates `GetAbilityFromInputTag(Input.Combat.Block):GetClass()` via `TryActivateAbilityByClass` = weapon-agnostic (DW parry / SnS block / 2H parry). 0.6s cooldown.
- AutoSprint (F6): LoopAsync 300ms in game thread; gates: not InCombat tag, not IsPlayerSprinting, speed > 100u/s; activates GA_Player_Sprint_C.
- All need in-game test (user). Open Qs: does synthetic InpActEvt call drive combo? does DW parry fire counter (Pushback) on parried hit? does InCombat tag actually flip?

### 2026-07-04 - Milestone - v2: timed AutoParry + fixed AutoSprint (test 1 feedback)
- Test 1 results: AutoChain WORKS (hold M1 chains). AutoParry triggered but at windup START (parry window expired before hit). AutoSprint dead.
- AutoSprint root cause: `pawn:HasMatchingGameplayTag()` is PURE_VIRTUAL C++ interface stub - calling it via UE4SS UFunction dispatch silently fails. Same reason WFRecon combat probe never logged. FIX: static `WFAbilitySystemBlueprintLibrary:AbilitySystemHasTagExactly(ASC, tag)` - works for any tag check. Also `UEHelpers.GetPlayerController` calls global `Print` (nil in mod env) when no PC exists (menu) - avoid UEHelpers in loops, cache pawn via ClientRestart hook + FindAllOf fallback.
- AutoParry v2 timing solution: DATA-DRIVEN. `tools/extract-attack-timings.py` walks dump enemy GA jsons -> Montage -> AnimSequence -> first `AnimNotifyState_WeaponTrace` (or MeleeTrace) notify LinkValue = damage window start (montage segment mapping: StartPos + (t - AnimStartTime)/rate). 295 attack timings extracted (median 1.0s, e.g. Skirmisher Sweeping_Slash 0.476s, Brawler Punch_01 1.262s). Emits `AutoParry/Scripts/timings.lua`; mod schedules parry at hitTime - LEAD (0.25s) via ExecuteWithDelay, re-validates at fire (range/cooldown/enabled).
- Mount facts: player pawn STAYS the pawn while mounted (`WFCharacter.m_pMountComponent`, `IsMounted()`); sprint is the same GA_Player_Sprint (mount speed via GE_Mount_SprintSpeed passed to PlayerToggleMount). Rider velocity ~0 while mounted -> read `GetAttachParentActor():GetVelocity()`.
- Sprint status tag: `Character.State.Generic.Sprinting` (granted by GE_Status_Sprint; blocked by Character.State.Disable.Sprint / Exhausted / InAir).
- Re-run timing extractor after game updates (dump refresh) to regenerate timings.lua.

### 2026-07-04 - Gotcha - boot crash: no FindAllOf/ExecuteInGameThread before player spawn
- v2 AutoSprint crashed game AT LAUNCH (AV right after "Event loop start"): its LoopAsync ran FindAllOf("WFPlayerCharacter_Base_C") + ExecuteInGameThread at the MAIN MENU while the BP class was not loaded yet.
- RULE for this game: mods with periodic loops gate ALL engine access behind a `ready` flag set by the `PlayerController:ClientRestart` hook (plus boot-time `StaticFindObject(class):IsValid()` check to survive "Restart All Mods" mid-map - that pattern is safe, ShowNameplates uses it). Commit 9ab5d15.

### 2026-07-04 - Milestone - WFQoL merged mod (chain+parry+sprint+reload+overlay)
- Merged AutoChain/AutoParry/AutoSprint into ONE mod `WFQoL` (single Lua state = shared toggles for the overlay, no cross-mod IPC). Old 3 mods removed from game+repo (history: bb19a8a..9ab5d15).
- Overlay: `KismetSystemLibrary:PrintString(pawn, line, true, false, {R,G,B,A}, 0.65)` every 500ms = quasi-persistent top-left status line. NOTE: Engine module is MISSING from UHTHeaderDump (dumper skipped it) - verify Engine natives via UE4SS_ObjectDump.txt instead.
- AutoReload (new): active-reload minigame = `GA_Player_RangedWeapon_ActiveReload_C` with BP fns `CheckInWindow`/`CheckInForgivenessWindow` (props: Tolerance 3.0, EarlyPressForgivenessPercent 0.2, ReloadSuccessType=GA_..._ActiveReloadSucceeded_C). Mod: RegisterHook both checks -> `return true` when enabled (returning nothing = no override, clean toggle), + on reload GA activation auto-press `InpActEvt_Reload_K2Node_InputActionEvent_12` after 300ms. Reload input node edge assumption: lower index = press (consistent: Attack1 36/37, Attack2 40/41, Mount 0/1).
- AutoSprint v3: still-broken diagnosis pending; combat tag detection works but sprint never engages. v3 logs activation attempts; after 5 inert tries auto-switches to LOOSE TAG INJECTION (`asc:AddUniqueGameplayTag(Character.State.Generic.Sprinting)`, removed on stop/combat) - char native OnSprintingTagChanged listener should flip move speed.
- Wayfinder ships DLSS + FSR2 plugin modules (UHT dump dirs DLSS/DLSSBlueprint/FSR2TemporalUpscaling) - relevant for fps advice on AMD (FSR2).

### 2026-07-04 - Milestone - external WPF overlay + v4 tuning (264ef4d)
- In-game PrintString overlay DOES NOT RENDER in Wayfinder shipping build (user: "overlay doesnt work") - screen debug messages disabled. Replaced with EXTERNAL overlay: mod writes `Mods/WFQoL/overlay-state.json` (pure Lua io, 1s heartbeat + on toggle; relative path w/ absolute fallback), `tools/overlay/WFQoL-Overlay.ps1` = WPF glass card (drag, resize grip + Viewbox scaling, LOCK = WS_EX_TRANSPARENT click-through, Ctrl+Alt+O global hotkey unlock via RegisterHotKey+HwndSource hook, pos/size persisted %APPDATA%/wfqol-overlay.json, dims to "offline" when heartbeat stale >5s). Launch: tools/overlay/start-overlay.bat. Smoke-tested: parses + runs.
- Lua 5.4 GOTCHA: string.format("%d", float-with-fraction) THROWS ("number has no integer representation") - use %.0f for UE float props like MaxWalkSpeed.
- Parry v4: cooldown 0.6->0.3s, schedule-collapse 0.15->0.05s, +one 120ms retry when TryActivateAbilityByClass returns false (player mid-swing). User felt v3 "slow" = internal cooldown.
- Sprint mount fix: NEVER tag-inject/speed-write the rider while IsMounted() (mount jank reported); instead boost the MOUNT actor's CharacterMovement.MaxWalkSpeed x1.4 (mount = GetAttachParentActor()), restore on dismount/stop. Keybind debounce 300ms (INS key was firing 2-3x per press = key auto-repeat).

### 2026-07-04 - Milestone - v5: mount gate fix, parry forcing, perf pass
- ON-FOOT SPRINT CONFIRMED WORKING via tag injection (log: "tag injection works (walk 550 -> 870)"). Escalation chain validated in production.
- v4 mount bug root cause: sprint loop gated on PAWN velocity BEFORE reaching the mount branch; rider velocity ~0 while mounted -> mount branch unreachable. v5 resolves mount FIRST, movement = max(mount vel, pawn vel). Mount detection: IsMounted() native + fallback attach-parent class name contains "Mount". F5 = diagnostic dump keybind (pawn class/vels/walk speeds/tags/mode) for future mount debugging.
- Parry forcing ladder: TryActivate -> ServerCurrentMontageStop(0.15) cancels player's own swing + retry -> 2 more rounds at +80ms -> log "parry FAILED". User feedback: parry lost to his own attack chain (activation rejected mid-swing).
- Constant-lag perf pass: UE4SS GuiConsole DISABLED in ini (Enabled=0, Visible=0) - the DX11 debug window is a constant frame cost + user kept it open (re-enable manually for Live View/Restart Mods; deploys now need game relaunch). Lua-side: cached WF lib + sprint class objects (invalidated on ClientRestart), meleeCache memoizes per-CLASS attack-tag verdicts so tag containers aren't iterated per activation. If lag persists: A/B disable ShowNameplates (forces every enemy nameplate widget + rescans on HP changes), then SmartSort.
- Overlay confirmed functional end-to-end on this machine (smoke test: reads state file, window runs; user's game is Borderless so it CAN draw over it). Added self-log %APPDATA%/wfqol-overlay.log + start-overlay-debug.bat (visible console) + off-screen position rescue + Activate on load. If user still sees nothing: read that log.

### 2026-07-04 - Gotcha - "overlay doesnt work" = user never launched the app (82193eb)
- %APPDATA%/wfqol-overlay.log proved the overlay was NEVER started during his session (only my smoke-test entries). Lesson: separate helper apps MUST auto-start - users won't run a second bat.
- Fix: WFQoL main.lua runs `os.execute('start "" /min <repo>/tools/overlay/start-overlay.bat')` at mod load (absolute path, brief cmd flash at boot acceptable); overlay ps1 has a named mutex ("WFQoL-Overlay-SingleInstance") so repeat launches exit instantly. Verified: double-launch test -> second instance exits.
- Parry "FAILED after 3 attempts" log spam analysis: most failures happen right after a successful parry = block ability STILL ACTIVE, re-activation correctly rejected. Added blockAbility:IsActive() check -> returns "active" = treated as success, no retries, no noise. Remaining FAILED lines are now real rejections (stamina / hard-disable states).

### 2026-07-04 - Milestone - v6: smart parry gates, stable mount boost, resident overlay (1a9e41f)
- User's "overlay window I cant click through" SOLVED + explained: he had dragged/resized the UNLOCKED card to 1908x974 (near fullscreen) - it was sitting over the game eating clicks. Overlay now: locked (click-through) BY DEFAULT, resident tray app registered at Windows login (Startup shortcut -> wscript launch-overlay.vbs -> hidden powershell; installer tools/overlay/install-autostart.ps1), card AUTO-HIDES when game heartbeat stale >5s and auto-shows when fresh, tray menu (lock/unlock, reset position, exit), Ctrl+Alt+O unlock. os.execute auto-launch REMOVED from main.lua (cmd flash gone).
- PS gotcha: checking overlay by process CommandLine match matches the CHECKING powershell itself (its own cmdline contains the search string) - verify via the overlay's log truncation instead.
- Parry gates (fire-time, per attempt): (1) willConnect(enemy,pawn) = dist <= 450u AND enemy forward-dot toward player >= 0.2 - kills "parries attacks that were never going to hit" + saves stamina; (2) stamina reserve: read HUD widget HUD_PlayerMeters_C.HUD_PlayerStaminaMeters.PlayerStaminaMeter.Percent (ShowNameplates trick - avoids FGameplayAttribute marshaling), skip parry below 35% so dash always has stamina. Parry costs stamina (shared w/ dash) - user complained drain.
- Mount boost start/stop jitter fix: game periodically REWRITES mount MaxWalkSpeed -> boost must be RE-APPLIED each 300ms tick (if cur < target-1). Plus hysteresis (start >100u/s, stop <30u/s) and combat gate IGNORED while mounted. Mount-changed handling restores old mount first.

### 2026-07-04 - Milestone - v7: real reload timing, lock-chip overlay, debug lessons (ab93cb4)
- AutoReload root cause: `CheckInWindow(InputTime float) -> OUT bInWindow` (out-param, NOT return value!) - my RegisterHook `return true` never set the out param, so forced-perfect never worked and the 300ms blind press hit the early-fail zone. REAL fix: on reload activation, poll `ab:CheckInWindow(elapsed_since_activation)` every 40ms; press Reload input the moment it returns true = presses exactly in the white zone using the game's own check. Force-hooks removed.
- Overlay v7: card + separate always-clickable LOCK CHIP window (64x24 pill glued right of card; toggles card click-through). Hotkey removed. Drag = header row only; resize = grip. Chip follows card via the 300ms poll (NOT LocationChanged - see below).
- DEBUG LESSON 1: `$window.Add_LocationChanged({...})` attached before Show() = Show() HANGS FOREVER (WPF reentrancy during initial positioning). Never hook LocationChanged/SizeChanged on these overlay windows; follow via timer instead.
- DEBUG LESSON 2: PS variable collision - row-builder loop reused `$chip` for a Border; my lock-chip Window also `$chip` -> "[Border] does not contain method Hide" at runtime. Renamed to $lockChip. Watch for var reuse in single-scope PS scripts.
- DEBUG LESSON 3 (repeat offender): this PowerShell tool aborts compound statements when an inner command sets nonzero exit - do process kills via bash loop + `taskkill //F //PID` instead.
- Mount recon aids: ClientRestart now logs pawn class; if pawn class contains "Mount" (possession-swap mounts) the pawn itself is treated as the mount. F5 diag dump exists. Awaiting user mount test with these.
- Full overlay lifecycle verified headless: start (mutex single-instance) -> hidden while heartbeat stale -> fake fresh ts in overlay-state.json -> "game heartbeat fresh - showing" -> auto-hide. Resident via wscript launch-overlay.vbs; login autostart shortcut in shell:startup.

### 2026-07-04 - Milestone - v8: MOUNT SPRINT CONFIRMED WORKING + reload call fix (b348289)
- Session 9 log: "sprint: mount speed 635 -> 889" = mount branch fires (pawn stays WFPlayerCharacter_Base_C, attach-parent mount, no possession swap). Clunk cause: velocity hysteresis kept dropping the boost, and each re-engage captured the CURRENT (sometimes mid-gallop/boosted: 893/908/1100) speed as new baseline -> compounding surges to 1540. v8: baseline captured ONCE per mount instance, boost held PERSISTENTLY while mounted (MaxWalkSpeed is a cap, harmless while standing - no velocity gate at all), re-asserted only when game drops it below target, game's own faster states left alone. Restore on dismount/toggle only.
- Reload error decoded: "error reload: function: 0x..." = UE4SS threw on `ab:CheckInWindow(elapsed)` - BP functions with OUT params need different calling. v8: adaptive checkInWindow() tries out-table style (`ab:Fn(in, outTbl)` -> outTbl.bInWindow) then multi-return style (`local ok, a, b = pcall(...)`), remembers which works. If log shows "CheckInWindow call failed in both styles" -> next option is reading window vars off the ability instance or the ranged weapon attribute set.
- User verdicts this round: chain GOOD, parry GOOD (stamina+connect gates approved), mount sprint working-but-clunky (fixed), reload broken (fixed pending test), overlay WORKS with lock chip.

### 2026-07-04 - Milestone - v9: GC crash fix, address-keyed mount, self-learning reload (839f769)
- CRASH DECODED (crash reporter): "AssembleReferenceTokenStream for GE_Player_DW_ConsumeStamina_Parry_C called on a non-game thread while GC is not locked" = parry activation lazy-loaded the SOFT-referenced stamina-cost GE off-thread. UE4SS ExecuteInGameThread executes on the next ProcessEvent call, which is USUALLY the game thread but can be an engine worker thread -> never rely on it for class-LOADING work. FIX: preload all 8 parry cost/pushback assets via LoadAsset() inside the ClientRestart hook (hooks run inline in ProcessEvent on the game thread for gameplay calls = safe).
- Mount v8 clunk root cause CONFIRMED: UE4SS RemoteObject wrappers do NOT compare with ~= (each access = new wrapper). Use :GetAddress(). v9: baselines in `mountBaselines[addr]` captured once per mount actor per session; boost persists; game's faster states untouched.
- Reload v9 = SELF-LEARNING: hooks (1) ActiveReload activation (montage name = weapon key, t0), (2) real InpActEvt_Reload presses (manual press time captured), (3) ActiveReloadSucceeded activation -> learned[montage] = pressAt, persisted to Mods/WFQoL/reload-times.txt. Auto path: learned time exists -> replay press at t-30ms; else CheckInWindow probe (now with phase-tagged error logging: "reload@isactive:", "reload@checkwindow:" etc to finally pinpoint the mystery "function: 0x..." error). USER MANUAL PERFECT RELOADS TEACH THE MOD - tell user: do one manual perfect per weapon, mod owns it forever after.
- Session 10 log: stamina reserve gate seen working ("parry skipped: stamina 13% below 35%"); parry active on beast enemies too (GA_Lurker_*).

### 2026-07-04 - Milestone - v10: window-read reload, KB+M aim assist, input pause during reload
- Reload v9 probe verdict from session 11 log: "error reload@isactive: function: 0x..." = `ab:IsActive()` on the reload GA instance is what has been throwing ALL ALONG (UE4SS throws a function value as the error, hence the weird message). Parry's blockAbility:IsActive() works fine - the difference is unexplained; rule: never call IsActive on the reload GA. User's manual reloads that session were FAILS (his own words) so the learning hook (correctly) recorded nothing.
- Reload v10 = READ THE WINDOW DIRECTLY: FModel JSON (GA_Player_RangedWeapon_ActiveReload.json) shows instance vars `WindowMin/WindowMax/WindowCenter` (normalized 0-1 of slider unless >1 = seconds) + `MaxReloadTime` (seconds) + `bUseAttributesForWindow` (window can come from WFRangedWeaponAttributeSet.ActiveReloadCenter/ActiveReloadBounds attributes). CheckInWindow is BlueprintPure sugar over these (MapRangeClamped(InputTime,0,MaxReloadTime,0,1) then InRange(WindowMin,WindowMax)). v10 reads the vars ~100ms after activation (retries up to 10x60ms), presses dead-center at `(WindowMin+WindowMax)/2 * MaxReloadTime - 0.03`. No polling, no CheckInWindow, no IsActive; guards by `currentReload.t0 == t0` session token. Learning system stays as priority-1 override.
- USER INTEL: M1 (fire) is ALSO a valid minigame trigger, and the minigame runs in AND out of combat (no combat gate exists in our code - hook-driven). CRITICAL interaction found: AutoChain's injected M1 spam during the minigame = pressing outside the window = guaranteed FAIL - very likely why auto reloads failed even when timing was right. v10: chain loop pauses while reloadInProgress() (currentReload age < maxT+0.5s), parry doParry also skips (its ServerCurrentMontageStop would cancel the reload montage). Both Attack1_36 and Reload_12 manual presses during the minigame are captured as pressAt for learning.
- AIM ASSIST FOR KB+M (new, F10, default ON): game gates soft lock/magnetism/camera assist on believing a gamepad is active. Levers: (1) hook native `/Script/Wayfinder.WFPlayerCharacter:IsUsingGamepad` -> return true, (2) hook static `/Script/Wayfinder.WFGameplayBlueprintLibrary:GetPlayerInputSource` -> return 1 (EPlayerInputSource: NONE=0 GAMEPAD=1 KBM=2), (3) set `WFGameUserSettings.bCameraAssistOnGamepad = true` at spawn (engine default is FALSE even for real controllers; bEnableCameraAimTargetingOnMouse is already true by default). Return-value override convention: RETURN the value from the RegisterHook Lua callback (ShowNameplates' OverrideReturn pattern). aimCalls counters in F5 diag prove whether the hooks actually fire. UNVERIFIED until in-game test; possible side effect: UI button prompts may flip to controller glyphs while F10 on.
- Relevant header finds for future: WFTargetingComponent (m_MagnetizedAimTarget, FindSoftLockTarget), WFGameUserSettings (GamepadAimFriction=1.0, ADSAimFriction=0.0 defaults), WFPlayerCharacter.IsMagnetizingAim.
- Overlay: 5th pill AIM/F10 added; default card height 230->258, saved config height clamped up to fit.

### 2026-07-04 - Milestone - v11: reload percent-scale fix, own soft-lock magnetizer for aim assist
- Session 12 log: reload window read WORKED but values are PERCENT: "window 40.00-60.00 max 1.50s" - v10's unit heuristic (wmax>1.001 = seconds) computed press at 50 SECONDS = mod did visibly nothing. v11: center>1.001 -> /100. Correct press for that weapon = 0.5*1.5s = 0.75s.
- Gamepad-spoof hooks CONFIRMED DEAD END: registered fine at boot, but grep of the ENTIRE FModel JSON dump found ZERO blueprint callers of IsUsingGamepad/GetPlayerInputSource - the aim gating is pure C++, C++ calls never enter the BP VM, UFunction hooks never fire. LESSON: before hooking a native for behavior change, grep the JSON dump for blueprint callers; no callers = hook is decoration.
- Aim assist v11 = OWN MAGNETIZER: pawn.TargetingComponent (UWFTargetingComponent, prop on WFPlayerCharacter) has the full soft-lock machinery as callable natives. Loop 150ms: pawn:IsAiming()/IsFreeFireAiming() -> tc:FindSoftLockTarget({Radius=3000,Yaw=40,bCanTargetFriendly=false,bIgnoreHardLockedTarget=false}, bIsResultTransient=false, bEnsureVisibility=true) -> stored results read from tc.m_CurrentSoftLockTargetResults.AssociatedTargets[1] (property read avoids UE4SS struct-return marshaling) -> write tc.m_MagnetizedAimTarget = target (same slot the native gamepad path fills). Cleared when not aiming or F10 off. F5 diag: magnet=<class> magnetizing=<bool> aiming=<bool>.
- If magnetism consumption ALSO turns out gamepad-gated in C++ (magnet set but no pull felt): plan C = manual camera nudge via AddControllerYawInput/PitchInput toward GetMagnetizedAimTargetSocketLocation each tick while aiming - full custom assist, cannot be gated.

### 2026-07-04 - Milestone - v12: unfailable reload (window operand forcing), camera-pull aim assist
- Reload "works sometimes" DECODED: session 13 log showed presses landing 0.72s = dead-center of the DISPLAYED window every time, yet no successes. CheckInWindow's locals (GA JSON) reveal it reads an ASC ATTRIBUTE (WFRangedWeaponAttributeSet.ActiveReloadCenter/ActiveReloadBounds) + instance `Tolerance` (3.0) via Add/Subtract+InRange - NOT the displayed WindowMin/WindowMax. True window = attr center +/- ~3 percent; display center != true center on some weapons/rolls.
- v12 reload = FORCE THE CHECK'S OPERANDS on every activation (game-thread hook, before any input): ab.Tolerance=1000, ab.EarlyPressForgivenessPercent=0, all WFRangedWeaponAttributeSet instances' ActiveReloadBounds.CurrentValue/BaseValue=200. ANY press by ANYONE at ANY time = perfect; user manual fail now impossible (better than blocking input). Learning system removed (dead weight). Timed center press kept as hands-off auto path. "reload: PERFECT" logged on every success; "reload: window forced open" once per session.
- Aim assist v12 = CAMERA PULL, cannot be gated: magnetizer (v11, works - log proof) feeds target; new 33ms loop rotates camera toward GetMagnetizedAimTargetSocketLocation via pawn:AddControllerYawInput/AddControllerPitchInput. Look-at math in plain Lua (atan2 from camera loc, no struct passing). Tuning consts: PULL_RATE=0.15/tick, PULL_CAP=1.5deg, PULL_CONE=20deg, PULL_DEADZONE=0.3deg, INPUT_SCALE=1/2.5 (engine deg-per-input-unit), pitch input negated (engine convention). Also WFGameUserSettings.ADSAimFriction 0->1 (native friction lever). Gamepad-spoof hooks deleted (proven decoration).
- Terminology for user comms: "aim friction" (slowdown over target, exposed in settings for mouse) is only 1 of 3 aim assist parts; magnetism + camera assist are the felt pull and were gamepad-locked - now home-built.
- F5 diag: "magnet=<cls> magnetizing=<bool> aiming=<bool> pull=<yaw>/<pitch>".

### 2026-07-04 - Milestone - v13: reload press via GA's own callback, configurable aimbot w/ overlay sliders
- v12 double-reload DECODED from log: every activation was followed by a second one ~1s later = our injected InpActEvt_Reload press. The minigame listens via WFAbilityTask_HandlePlayerInputPress (OnTrigger delegate, sig (float ElapsedTime)); pawn InpActEvt injection NEVER reaches it - it just retriggers the reload ability after the current one ends. Zero ActiveReloadSucceeded activations across ALL sessions = no buff ever, even on user's visually-perfect presses.
- v13 press = call the GA's OWN bound press handler directly: `ab:OnTriggered_99CA32B34E7E0E8640D86BAE3837FFCB(elapsed)` (+ fallback `OnTriggered_0C5309BB4DBB1136C0E5DEBE6E42DF1A` 250ms later if ability still alive) - executes the exact graph path real input takes: CheckInWindow(forced open) -> success branch. LESSON: BP ability input = delegate-bound OnTriggered_<GUID> UFunctions on the GA instance; call those to simulate input, never the pawn's InpActEvt stubs.
- New instrumentation/hooks: GA K2_OnEndAbility -> clears currentReload (stale press can't fire after end - kills double-reload from any path) + "reload: ended at Xs"; GCNA_2HR_ActiveReload_Success/Fail K2_HandleGameplayCue (classes added to PRELOAD list to be hookable at spawn) -> ground truth "reload: success cue"/"FAIL cue". BUFF GUARANTEE: onReloadEnded waits 250ms; if no success signal seen and our press fired -> asc:TryActivateAbilityByClass(ab.ReloadSuccessType) -> "reload: buff FORCED".
- Aimbot v13 (user request): overlay gained AIM CONFIG section (FOV slider 2-90, SMOOTH slider 0.02-1.0, "ADS only" checkbox) -> writes Mods/WFQoL/aim-config.json (300ms debounce) -> Lua polls 1s, hot-applies + logs "aim: config ...". smooth=1.0 -> hard snap (cap 8deg/33ms tick); adsOnly=false engages while M1 held too. Magnetizer acquisition cone = fov*1.5. Card 258->352 tall.
- Overlay/PS lesson: sliders need unlocked card (lock chip) to interact; aim-config load/save via Split-Path $StateFile; PS 5.1 Stop-Process on CIM-matched PIDs inside try/catch (bare pipeline exits 255 through this tool).

### 2026-07-04 - Milestone - v14: aimbot via SetControlRotation (input-additive route dead)
- RELOAD CONFIRMED WORKING by user + log (session 15): "reload: success cue" x3 + "ended" per reload, no doubles, no fails, buff granted via real success path (no FORCED lines needed). The OnTriggered_<GUID> direct-call approach is the keeper.
- Aimbot v13 failure decoded: AddControllerYawInput/AddControllerPitchInput calls executed errorless (no aimpull errors, magnetizer acquiring, config hot-reload working) but ZERO camera movement - the game's input pipeline (ADS dampen scalars on WFPlayerCharacter, GamepadAimDampenScalar/ADSAimDampenScalar) scales additive controller input to nothing. LESSON: for camera control in Wayfinder, additive input API is a dead end; WRITE the rotation.
- v14: controller:SetControlRotation({Pitch,Yaw,Roll}) per 16ms tick, exponential approach (step = remaining-delta * smooth, cap 12deg*smooth/tick, pitch clamped +/-75, deadzone 0.25deg). Mouse still wins because every tick starts from the CURRENT (user-moved) rotation. "aim: pulling (dyaw=X dpitch=Y)" logged once per engagement = proof the write path runs. Magnetizer loop 150->100ms for snappier acquisition.
- Overlay sliders confirmed live in log ("aim: config fov=90 smooth=0.02 adsOnly=false" as user dragged) - note user had left smooth at 0.02 (weakest possible) during the "not working" test; even so, v13's additive route showed nothing at ANY smooth, so the pipeline was the real blocker.

### 2026-07-04 - Milestone - v15: MAGIC BULLETS (projectile homing), camera pull demoted to opt-in
- User verdict on camera aimbot: works ("aim: pulling" fired) but useless feel. Pivot to magic bullets per user.
- PROJECTILE API GOLD (UHT dump): ALL projectiles derive from `AMayhemBaseProjectile` (AMayhemProjectile, AMayhemBeamProjectile children). Native BlueprintCallable `SetHomingTarget(const AActor*)` on the base; `MovementComponent` = UAirshipProjectileMovementComponent : UProjectileMovementComponent (stock homing: bIsHomingProjectile / HomingTargetComponent / HomingAccelerationMagnitude). Movement comp also has `FakeNewHit(Actor,Component,Location,Normal)` / `FakeHit(HitResult)` = guaranteed-hit nuclear option (protected UFUNCTION, still callable via UE4SS) - PLAN B if homing curves too weak.
- v15: NotifyOnNewObject("/Script/Wayfinder.MayhemBaseProjectile") -> +30ms game thread -> filter proj:GetInstigator() addr == pawn addr -> target = magnetized (or on-demand FindSoftLockTarget for instant hipfire) -> SetHomingTarget + bIsHomingProjectile=true + HomingAccelerationMagnitude = 2000 + strength*38000. Throttled "bullets: homing -> <class>" log.
- Gating split: ACQUISITION (magnetizer) = aiming OR m1Held, always; CAMERA PULL = own `pull` config (default OFF) + adsOnly. aim-config.json now {fov,strength,smooth,bullets,pull,adsOnly}; overlay: STRENGTH slider + MAGIC BULLETS / CAM PULL / ADS-only checkboxes (Add-CfgCheck helper), card 400 tall.
- Weakpoint stretch if user wants heads: check which component native SetHomingTarget picks; override MovementComponent.HomingTargetComponent to mesh/weakpoint child actor.

### 2026-07-04 - Milestone - v16: FakeHit magic bullets (plan B), FOV-only config
- v15 homing NEVER ENGAGED: log had config + magnetize lines but ZERO "bullets:"/error lines - NotifyOnNewObject("/Script/Wayfinder.MayhemBaseProjectile") never reached the handler body (either no derived-class notify or silent instigator bail; couldn't distinguish, so v16 nukes both possibilities).
- Player shot class chain mapped via FModel: WFProjectile_2HR_Slug_C -> /Game/Blueprints/Projectiles/WFProjectile_Base_BP.WFProjectile_Base_BP_C -> /Script/Wayfinder.MayhemProjectile -> MayhemBaseProjectile. Enemy spits use the SEPARATE AWFBallisticProjectile hierarchy (good: not catching those on the Mayhem notifies).
- v16: notify registered on ALL THREE levels (both natives at boot + BP base in ClientRestart after preloading it via LoadAsset; PRELOAD list gained WFProjectile_Base_BP). addr-keyed seenProj dedup (same object can notify on several levels). ALWAYS-log diag (2s throttle): "bullets: saw <cls> inst=<cls> owner=<cls>" - next session log definitively answers who spawns what with which instigator. Ownership relaxed: instigator addr OR owner addr == pawn, OR instigator class == WFPlayerCharacter_Base_C.
- Magic = FakeNewHit(target, target:K2_GetRootComponent(), targetSocketLoc, normal(projLoc-tgtLoc)) on proj.MovementComponent = game's own guaranteed-impact API -> "bullets: FORCED HIT -> <cls>". Errors fall back to SetHomingTarget + 40k accel. ONE gate: angle(camera fwd, to-target) <= fov (plain Lua vector math from GetControlRotation).
- Config/UI simplified per user: aim-config.json {fov, bullets, pull:false}; overlay = FOV slider + MAGIC BULLETS checkbox only (STRENGTH/SMOOTH/CAM PULL/ADS rows removed, card 310). Lua still parses old keys w/ defaults (pull stays available by hand-editing json).

### 2026-07-04 - v17: per-shot projectile hooks (pooling killed spawn notifies)
- Session 17 log: all 3 NotifyOnNewObject registrations clean ("bullets: BP projectile notify registered") yet ZERO "bullets: saw" during active shooting = projectiles are POOLED (AWFBallisticProjectile ships IPoolableProjectileInterface; Mayhem tree evidently pooled too). Constructed once at load, reused per shot -> object-creation notifies structurally can't see shots. LESSON: for per-shot logic in Wayfinder, hook per-launch UFUNCTIONS, never object construction.
- v17: tryHook on WFProjectile_Base_BP_C:ReceiveBeginPlay + ComputeInitialSpeed + FindTargetActor (per-launch setup fns on the BP base; slug child overrides ReceiveBeginPlay but Compute*/FindTargetActor live on base). Handler unchanged (diag "bullets: saw", FOV gate, FakeNewHit, homing fallback). Dedup switched addr-forever -> addr+0.5s window (pooled actors reuse addresses across shots!). Notifies kept for fresh spawns.

### 2026-07-04 - v18: HITSCAN magic bullets (the actual answer)
- Session 18 log: per-launch BP hooks registered clean, user shot 4 mags, ZERO handler hits. FModel dissection of GA_Player_RangedWeapon_Fire_Batched CDO = smoking gun: **IsHitscanWeapon = true**. Basic rifle fire NEVER spawns projectiles - it line-traces (TraceTargetActor = WFTargetActor_LineTrace, NumberOfTraces=12, BaseSpread=12, HitGameplayEffect GE_Damage_Player_Rifle_Shoot_Standard). ProjectileToSpawn (WFProjectile_2HR_Slug) only used on the non-hitscan path. THREE detection rewrites chased projectiles that never exist for basic attacks.
- THE LEVER: AWFTargetActor_Trace.EndpointAimType (EWFTargetActorEndLocationAimingType: 0=AimAtStartLocation 1=AimAtSoftTargetActor 2=AimAtCameraForward 3=AimAtEndLocation). Set =1 -> trace endpoint = the soft-lock target (our magnetizer feeds it) regardless of camera. Plus spread zeroing on BOTH the GA instance (BaseSpread/AimSpreadModifier/SpreadIncrement/SpreadMax) and the trace actor (BaseSpread/AimingSpreadMod/TargetingSpreadIncrement/TargetingSpreadMax).
- v18: hook K2_ActivateAbility on all 4 fire GAs (Fire_Batched/_Burst/_Rail/_Shotgun) -> zero spreads + EndpointAimType = target-in-FOV and 1 or 2. currentBulletTarget(pawn) = magnetized-or-fresh soft lock + FOV angle gate. Logs: "bullets: hitscan locked -> <cls>" / "hitscan zero-spread (no target in fov)". Projectile path (v17 hooks + notifies) KEPT for the weapons that do spawn projectiles (weapon abilities, other rifle types).
- LESSON: before building shot-manipulation, check IsHitscanWeapon on the fire GA. Wayfinder basic ranged = hitscan; projectiles = weapon ABILITIES only.

### 2026-07-04 - v19: per-shot trace TA stamping (fresh TA per shot killed v18)
- Session 19 log: fire GA hooks LIVE per shot ("bullets: hitscan locked -> AI_Blaster" + FOV gate working "no target in fov") but traces still don't bend. Root cause: GAS WaitTargetData spawns a FRESH WFTargetActor_LineTrace PER SHOT - v18 edited the previous shot's dead TA via ab.TraceTargetActor. Same pooling-family lesson inverted: this object is per-use disposable.
- v19: hook the per-shot config calls on the NEW instance: /Script/Wayfinder.WFTargetActor_Trace:SetStartLocation + SetShouldProduceTargetDataOnServer (native BlueprintCallable, hookable, run between spawn and trace) + NotifyOnNewObject on WFTargetActor_LineTrace (fresh spawn = notify works here). Handler: gate ta.MasterPC valid (= player-owned; enemy TAs have no PlayerController - do NOT stamp theirs, would aim enemy shots), zero TA spreads, EndpointAimType=1 when currentBulletTarget(pawn) in FOV. Log "bullets: trace TA locked -> X".
- Damage-direct fallback researched + parked: ApplyHitscanEffect/HandleTargetData all take FGameplayAbilityTargetDataHandle (opaque TSharedPtr innards, not constructible from Lua). If v19 fails: UAbilitySystemBlueprintLibrary:AbilityTargetDataFromActor(target) -> pass handle to ab:ApplyHitscanEffect(handle) - struct round-trip through UE4SS untested.

### 2026-07-04 - v20: DIRECT damage injection (replicate manual hit), trace-redirect gave up
- Session 19 log: trace TA path fired ("trace TA zero-spread/locked") + fire GA hooks fired per shot, but EndpointAimType=1 / spread-zero still did NOT bend visible shots or land guaranteed hits. Redirecting GAS trace geometry is a dead end via UE4SS (fresh TA per shot, aim-mode enum semantics unclear, target data produced server-side).
- v20 = replicate a manual hit DIRECTLY: on fire GA activation w/ target in FOV, ExecuteWithDelay(60) [let ability set Source Weapon] -> handle = /Script/GameplayAbilities.Default__AbilitySystemBlueprintLibrary:AbilityTargetDataFromActor(target) -> ab:HandleTargetData(handle) [full pipeline: damage GE + hit cues], fallback ab:ApplyHitscanEffect(handle) if HandleTargetData errors on actor-only data (no FHitResult). This IS the call a real connecting trace makes - correct source/level/GE, camera-independent. Log "bullets: MAGIC HIT (HandleTargetData|ApplyHitscanEffect) -> X".
- Kept trace-redirect + spread-zero as cosmetic layer (visual beam) but damage no longer depends on it. Projectile FakeHit path kept for ability projectiles.
- WEAKPOINT: AbilityTargetDataFromActor targets actor root = body hit (guaranteed dmg, likely no weakpoint mult). Weakpoint needs AbilityTargetDataFromHitResult with FHitResult naming the weak bone/component - FHitResult construction from Lua is the hard follow-up (big struct). Deliver body-guaranteed first.
- KEY UNTESTED: struct round-trip - passing the FGameplayAbilityTargetDataHandle returned by one UE4SS call straight into another. If log shows "bullets-magichit" errors -> handle didn't survive marshaling; next = check if UE4SS exposes the handle as usable param or need OutTargetData pattern.

### 2026-07-04 - v21: real AIMBOT (camera snap), magic bullets shelved
- Magic bullets FINAL verdict: v20 HandleTargetData ran clean every shot (struct round-trip WORKED, "MAGIC HIT" spam, hit enemies+breakables+turtlesaur) but NO actual damage - HandleTargetData with actor-only target data (no FHitResult) no-ops the damage application. Every trace/projectile/target-data avenue exhausted across v15-v20. User: "not working... lets just do aimbotting, inject into aimbot like a cheat". OFFLINE single-player game = legit cheat mod.
- v21: the v14 SetControlRotation camera-snap (which worked mechanically, "aim: pulling") promoted to primary AIMBOT, un-gated (was hidden behind pull:false default all along - that's why it seemed useless). aimbotEngaged = state.aim(F10) + aimCfg.aimbot + (IsAiming OR m1Held). Snap cap raised 12->40 deg/tick*smooth so high SNAP truly locks. Aim point = GetMagnetizedAimTargetSocketLocation (chest). Pitch clamp +/-80. Log "aimbot: locking <cls>".
- Config simplified: aim-config.json {fov, smooth, aimbot, bullets}. Overlay AIMBOT CONFIG: FOV slider (2-90) + SNAP slider (0.02-1, 1=instant lock) + AIMBOT checkbox + MAGIC BULLETS checkbox (default off). F10/pill/logs renamed Aimbot. Card 368 tall. Defaults fov=30 smooth=0.5 aimbot=on bullets=off.
- Magnetizer loop (acquires m_MagnetizedAimTarget) still feeds the aimbot; unchanged.

### 2026-07-04 - v22: HARD LOCK aimbot (exact-rotation write, sticky target)
- User: "hard lock aimbot, very snappy". v22: smooth>=0.99 = write EXACT look-at rotation every 16ms tick (no interpolation) = camera glued to target. Sticky lock: lockedTarget held until dead or leaves fov*2 leash (no jitter between targets). Deadzone removed (min dist 30). Defaults raised: fov 40, smooth 1.0 (instant). Below 0.99 smooth still eases (cap 60*k deg/tick).
- DIAGNOSTIC added: one-time "aimbot: write check wanted yaw=X, now=Y" 50ms after first lock - if now!=wanted, the game OVERRIDES SetControlRotation (custom camera) and we need a different write target (PlayerCameraManager rotation / camera component / pawn actor rotation). This log line is the decider for whether SetControlRotation is even the right lever - never actually confirmed across v14-v21 (user never said camera moved).
- If write-check shows revert: next levers to try - controller has a camera manager; Wayfinder may use a spring-arm/custom third-person cam. Options: PlayerCameraManager:SetActorRotation, find CameraComponent + set world rotation, or pawn:K2_SetActorRotation (if camera follows pawn facing). Check WFPlayerCharacter camera setup in headers first.

### 2026-07-04 - v23: aimbot CONFIRMED working + overlay never-hide + off-screen fix
- "WHERE IS THE OVERLAY": (1) window parked at X=1943 (off right edge of 1920 screen) - config left=1943; off-screen guard only checked VIRTUAL desktop bounds (2nd monitor made 1943 "valid") -> now ALSO clamps vs PrimaryScreenWidth. Reset to 60,220. (2) overlay auto-HID whenever game heartbeat stale >5s (alt-tab/loading/menu) = vanished constantly -> REMOVED auto-hide; now ALWAYS visible while running, shows "offline" text + dims combat dot when stale. Kill overlay via bash taskkill loop (PS CIM Stop-Process exits 255 through the tool).
- AIMBOT CONFIRMED WORKING (session log): "aimbot: write check wanted yaw=127.7, now=127.4" = SetControlRotation STICKS (matched within 0.3deg). This finally proves SetControlRotation is the correct camera lever - control rotation writes hold, camera follows. First confirmation across v14-v22.
- v22 only locked ONCE in 48s because aimbotEngaged required (IsAiming or m1Held) AND magnetizer had gated target. v23: aimbotEngaged = state.aim and aimCfg.aimbot (always on when F10), aimbot loop acquires its OWN target via FindSoftLockTarget (independent of magnetizer gate). Now hard-locks any enemy in FOV cone continuously. Sticky lockedTarget + exact-rotation write (smooth 1.0) still in place.

### 2026-07-04 - v24: PURGED aimbot + magic bullets (user call)
- User done with aim features ("fuck all this aimbot and bullet magic, just fully purge it"). Removed ENTIRELY from main.lua: AimAssist/magnetizer, camera-pull aimbot, magic bullets (projectile FakeHit, hitscan trace-redirect, target-data damage inject), all aim hooks (projectile BP, WFTargetActor_Trace, fire GAs), applyAimSettings, aim-config parser, F10 bind, aim state key, aim in overlay-state.json, F5 diag magnet lines, PRELOAD WFProjectile. From overlay: AIMBOT pill, entire AIMBOT CONFIG section (FOV/SNAP sliders, checkboxes), Save-AimCfg, aim pill set, card 368->240. Deleted aim-config.json. Aim reference docs (soft-lock/magnetizer/SetControlRotation/hitscan/projectile APIs) retained in reference-wayfinder-ue4ss-api.md for future.
- WFQoL now = AutoChain(F7) + AutoParry(F8) + AutoSprint(F6) + AutoReload(F9) + overlay(4 pills). All confirmed working pre-purge. Clean base for NEW mods going forward.

### 2026-07-04 - v25: AutoLoot, overlay redesign, lifecycle fix, engine.ini perf, hitch log
- OVERLAY LIFECYCLE: removed Windows login-autostart (Startup shortcut - spawned overlay on PC boot). Mod now launches overlay on ClientRestart via os.execute wscript launch-overlay.vbs (once, guarded). Overlay self-EXITS 20s after heartbeat stale (game close/crash/alt-f4). install-autostart.ps1 repurposed to REMOVE the old shortcut.
- OVERLAY REDESIGN (user "not AI generated style"): full rewrite. Flat #0F1116 sharp-corner window (WindowStyle=None, NO AllowsTransparency/rounded/glass), Cascadia Mono font. Header=drag anchor + inline lock SQUARE (color: green unlocked/red locked, toggles move+resize freeze) + close x. REMOVED floating lock-chip window + WS_EX_TRANSPARENT click-through entirely. Rows are clickable buttons (hover highlight) that TOGGLE the mod. Visible resize grip = Polygon triangle + transparent Thumb (DragDelta resize) bottom-right. Always-autosaves pos/size (geometry-diff check in 250ms poll timer + on drag/resize end).
- CLICK-TO-TOGGLE channel: overlay row click writes Mods/WFQoL/overlay-cmd.json {seq=unix-ms, feature}; mod polls 200ms, toggles state[feature] on new seq (records pre-existing seq at load w/o applying = stale-click guard). Generic - works for any feature key.
- AUTOLOOT (F4, new): loot vacuum. LoopAsync(400) -> FindAllOf("WFPickup") (AWFPickup C++ base, catches BP subclasses) -> within LOOT_RANGE 3000u: set bRequireInteractionToCollect=false + bCanBePickedUp=true, K2_SetActorLocation onto player (overlap collects). Logs distinct pickup classes once + "vacuumed N" throttled 3s. UNTESTED in-game.
- USEFUL SELF-LOGS: (1) parry "no timing for <class> (default)" once per unlisted attack -> extend timings.lua offline; (2) perf "hitch %.1fs (combat=X)" when writeState 1s loop gaps >2s = real stutter for engine.ini correlation, throttled 10s.
- ENGINE.INI PERF (clarity/visibility/fps, kept looks): EmitterSpawnRateScale 2->1, ParticleLODBias -2->0, ParticleDistanceRelevanceScale 2->1, SSR.Quality 4->2, VolumetricFog.GridSizeZ 128->64, +DepthOfFieldQuality=0. File is RO-locked (game can't overwrite); backup Engine.ini.wfqol-bak; repo copy tools/optimization/Engine.ini + README. Active path %LOCALAPPDATA%\Wayfinder\Saved\Config\WindowsNoEditor\Engine.ini (NOT the game folder Saved which is empty).
- CLEANUP: dropped dead KISMET local. Audited growth tables (seenErrors/meleeCache/mountBaselines/lootSeen/lastToggle/registered) all bounded per-key - NO leaks. aim/bullet purge from v24 confirmed clean.
- WFQoL now: F4 loot / F6 sprint / F7 chain / F8 parry / F9 reload / F5 diag + redesigned overlay (5 rows, click-toggle).

## v26 (2026-07-04) - game crash after mount + AutoLoot PURGED, UE4SS dev-tooling purged
- CRASH: Fatal Error 18:52:53 while mounted/galloping (crash_2026_07_04_18_52_53.dmp). UE4SS.log clean-cut at 18:52:49 ("sprint: mount baseline 724, boosting to 1014"), no error line = NATIVE crash (Lua errors are pcall'd + logged, never crash the process). Prime suspect = AutoLoot: only new untested engine-mutating loop running (400ms). Two traps hit - FindAllOf("WFPickup") returns POOLED + CDO (Default__) instances, and K2_GetActorLocation/K2_SetActorLocation on those native-segfaults THROUGH pcall (pcall can't catch native AVs); plus re-teleporting a pickup that destructs on collection-overlap = use-after-free. No "loot:" log lines is consistent (segfault mid-loop before buffer flush).
- FIX (user "no need for autoloot mod, can purge"): AutoLoot fully removed from main.lua - state.loot, LOOT_RANGE/lootSeen/lootVacuum + LoopAsync(400), F4 bind, "loot" key from writeState json + header/loaded comments. Overlay: removed LOOT row (features array) + Set-Row loot. WFQoL now F6 sprint / F7 chain / F8 parry / F9 reload / F5 diag. Overlay = 4 rows.
- PURGE UE4SS dev-tooling (user "purge all non mods thing, less crashes/perf"): mods.txt CheatManagerEnablerMod/ConsoleCommandsMod/ConsoleEnablerMod/BPModLoaderMod/BPML_GenericFunctions -> 0. Kept gameplay mods ShowNameplates:1, SmartSort:1 (both Lua, not dumpers), WFQoL:1, built-in Keybinds:1. ActorDumper/SplitScreen/LineTrace/jsbLuaProfiler already 0. Re-enable any if needed later. mods.txt is game-side (D:/.../Win64/Mods/mods.txt), NOT in repo.
- LESSON: never enumerate pooled UObject classes (WFPickup/projectiles/trace-target) via FindAllOf and call native actor methods on the results - pcall is NO protection against native access violations. Skip Default__ CDOs + dedup by address at minimum, or avoid the whole approach.

## v27 (2026-07-04) - transition crash hardening (settle-gate + CharacterMovement validation)
- CRASH #2 (post-AutoLoot-purge): EXCEPTION_ACCESS_VIOLATION reading 0x1c, callstack all UE4SS frames = our Lua->native call crashed the game thread. Happened on a BREACH/level transition (last UE4SS.log lines = ShowNameplates re-registering HUD hooks 19-22 on the new level, then instant crash). Crash #1 was open-world galloping (streaming). Common thread = WORLD STREAMING / level transition: actors tear down while our always-on game-thread loops keep touching them. 0x1c = null-pointer + small offset deref (e.g. null CharacterMovement sub-object). pcall does NOT catch native access violations.
- FIX (WFQoL side): (1) SETTLE gate - transitionAt=os.clock() set in ClientRestart hook; settling()=(clock-transitionAt)<1.5s; AutoSprint loop + onEnemyAbility + doParry all bail while settling() so no native actor access during the (un)construction window. (2) CharacterMovement validation - new maxWalk fetches actor.CharacterMovement, IsValid-checks it, THEN reads MaxWalkSpeed; new setMaxWalk(actor,v) validates actor + CM before writing. All 5 raw `X.CharacterMovement.MaxWalkSpeed=Y` write sites routed through setMaxWalk. (3) metersCache cleared on ClientRestart (HUD widgets rebuilt per level; stale ref = another null-deref vector).
- NOTE: AutoSprint is WFQoL's ONLY always-on game-thread native loop (runs every 300ms gated just by `ready`, which stays true across transitions). If transition crash persists after v27, suspect ShowNameplates (its HUD/nameplate hooks were the last thing logged before the crash) - bisect by disabling ShowNameplates+SmartSort for a WFQoL-only run.
- RULE: any always-on game-thread loop must validate sub-objects (CharacterMovement/ASC/HUD widgets) before native access AND pause during level transitions - actors are torn down mid-stream and pcall can't save you.

## v28 (2026-07-04) - overlay no-focus-steal (fix bleep + mouse-unfocus)
- SYMPTOM: random "bleep" + mouse unfocuses from game mid-play. Cause = a window grabbing FOREGROUND focus off the borderless game (Windows plays the window-switch/default beep on focus change).
- OVERLAY was one source: Topmost window + $window.Activate() on load + poll timer re-Show()ing when IsVisible false = re-grabbed focus whenever the game hid it. FIX (WFQoL-Overlay.ps1): ShowActivated="False" on the Window + WS_EX_NOACTIVATE via Win32 interop (GetWindowLong/SetWindowLong GWL_EXSTYLE -20, 0x08000000) applied at SourceInitialized; removed the Activate() call. Clicks on rows STILL register (NOACTIVATE != TRANSPARENT), window just never becomes foreground. Hot-applied: killed old overlay PID + relaunched via VBS.
- OTHER source = the AGENT's own shell commands: every Bash/PowerShell/deploy I run while user is in-game spawns a cmd/conhost/powershell console that steals foreground + dings. Mitigation: batch/minimize shell commands during active play; the wscript-hidden overlay launch itself does NOT flash.
- GOTCHA (self-inflicted): Get-CimInstance ... Where CommandLine -like '*WFQoL-Overlay*' MATCHES ITS OWN QUERY PROCESS (the pattern text is in the -Command line). Looked like duplicate overlays; was false. Exclude with -notmatch '-Command'. Mutex "WFQoL-Overlay-SingleInstance" works - single instance confirmed.
- SESSION WIN: v27 transition hardening held - no crash 19:28->19:45+, reload/parry/overlay-click all working in log.

## v29 (2026-07-04) - log-driven tuning: parry stamina reserve + bulletproof ClientRestart
- LOG REVIEW (19:28->19:51, clean/no-crash, reload PERFECT throughout, overlay no-activate working): two improvements.
- (1) STAMINA_RESERVE 0.35 -> 0.20. Log showed constant "parry skipped: stamina X% below 35%" at 25/26/28/31/33/35% = mod refusing parries right when needed. AutoParry exists to land parries; a blocked hit beats a saved dash. 0.20 keeps a small dash buffer, lands far more parries.
- (2) ClientRestart callback FULLY pcall-wrapped. Log had 2x "Error executing hook pre-callback ClientRestart: ... Was unable to register a hook" on transitions. WFQoL's registerAll is a no-op on re-entry (registered[] guard) so likely NOT us (prob ShowNameplates re-hooking HUD), but wrapping the whole body means WFQoL can PROVABLY never emit that line now. If it persists post-v29 = ShowNameplates confirmed.
- "no timing for <class>" entries (GA_AI_Atk_Melee_L/R/_ALC_C, GA_AI_Atk_180_L/R_ALC_C, GA_Elemental_Flowing_Bubble_C, GA_Turtlesaur_Atk_Bite_Swamp_C) left at default 0.6s - can't extract real frame data from logs, and fire-time willConnect recheck makes exact timing non-critical. Extend timings.lua offline if a dump becomes available.
- Applies on NEXT game launch (hot-reload disabled). Needs test: fewer stamina skips, more parries landing.

## v30 (2026-07-04) - patched ShowNameplates (self health/stamina broken)
- User: self nameplate showed wrong life+stamina. ShowNameplates is a THIRD-PARTY mod (D:/.../Mods/ShowNameplates/scripts/main.lua, not in my-mods). Two bugs: (1) main() re-registered ALL hooks on every ClientRestart -> stacked hooks + stale-closure updateNameplate wrote an OLD pawn's HUD values, AND this was the "Was unable to register a hook" log spam (confirmed ShowNameplates, NOT WFQoL). (2) health only written to PlayerLastHealthBar (damage-trail); primary fill never driven -> life read wrong.
- FIX (rewrite): hooks install ONCE (hooksInstalled guard); ClientRestart -> onRestart() just refreshes currentPlayer + nulls playerNameplate/hudMeters so ShouldBeVisible rebinds for the new pawn. Drives primary health fill (candidates PlayerHealthBar + characterHealthFill) + trail + additive(shield) + characterStaminaFill; setPct() pcall-guards each so wrong bar names no-op. One-shot diagBars() logs which bars exist -> trim mapping next pass. All native access pcall-wrapped.
- Repo-tracked at tools/third-party-patches/ShowNameplates/ (main.lua + README). NOT synced by deploy-mods.ps1 (my-mods only) - manual copy. Game-folder original backed up main.lua.wfqol-bak-*. Applies next launch (hot-reload off).
- NEXT: read log for "[ShowNameplates] bar 'X' present=true/false" -> confirm real bar names, drop the dead candidates.

## v31 (2026-07-04) - re-enabled ConsoleEnablerMod for SmartSort F10 console
- SmartSort registers its sort command via RegisterConsoleCommandHandler("RunSmartSort",...) (main.lua:260) = needs the in-game console. F10 console = ConsoleEnablerMod (was purged v26). Re-enabled ConsoleEnablerMod:1 in mods.txt (game-side). ConsoleCommandsMod NOT needed (that's a separate command pack; RunSmartSort is self-registered). Other purged tooling stays off.
- Usage: F10 -> type RunSmartSort -> sorts existing inventory (may freeze briefly). New items auto-sort. Config rules at Mods/SmartSort/scripts/config.lua, restart game to apply rule edits.
- mods.txt now enabled: ConsoleEnablerMod, ShowNameplates, SmartSort, WFQoL, Keybinds. Applies next launch.

## v32 (2026-07-04) - full-mod log verify + ShowNameplates bar cleanup
- Session 20:14->20:40 all GREEN: 5 mods loaded (ConsoleEnabler/ShowNameplates/SmartSort/WFQoL/Keybinds), F10 mapped (ConsoleKey: F10) so SmartSort RunSmartSort ready, NO crash across multiple WFPlayerCharacter_Base_C <-> HousingEditPawn_BP_C swaps (housing/edit mode = new transition type, v27 settle-gate handled it), and ClientRestart hook errors = 0 (ShowNameplates run-once fix + WFQoL pcall-wrap killed the "Was unable to register a hook" spam - CONFIRMED it was ShowNameplates).
- ShowNameplates diag confirmed real bars: PlayerHealthBar=true (main - old code NEVER set this = the self-life bug), PlayerHealthBar_Additive=true (shield), PlayerLastHealthBar=true (trail), characterStaminaFill=true. characterHealthFill + PlayerStaminaBar = FALSE. v32 cleanup: dropped the 2 dead candidates + removed the one-shot probe. Final mapping: health->PlayerHealthBar+PlayerLastHealthBar, shield->_Additive, stamina->characterStaminaFill.
- STILL UNVERIFIED (no combat this session - was in housing): parry stamina-reserve 0.20 tuning, reload, and whether nameplate life now reads correct in a fight. Need a combat session.

## v33 (2026-07-04) - SmartSort "not sorting" = near-empty config + silent; instrumented
- User: auto-sort not working. Log = only "Starting SmartSort" + its CLIENT_NotifyItemAdded hook registered, ZERO activity. Root cause candidates: (a) mod is silent by default (debug=false, auto path never logs), (b) CONFIG NEARLY EMPTY - only weaponFavoriteFilters={{aboveLevel=38}} set; weaponJunkFilters={}, ALL accessory + ALL echo filters {}. So auto-sort only favorites L38+ weapons, touches nothing else. Housing-mode session likely never fired the hook (no combat loot).
- INSTRUMENTED (SmartSort main.lua): debug=true; CLIENT_NotifyItemAdded hook wrapped in pcall + "fired"/"ERROR" logs; FlagItem logs "type=.. weapon/acc/echo="; handleApplyItemFlag logs "applying flag N". Next loot session will show if hook fires + item classification + apply. Backups main.lua/config.lua .wfqol-bak-*. Repo copy tools/third-party-patches/SmartSort/.
- NOTE struct: auto hook passes ItemEntry["Handle"] to FlagItem which reads itemEntry["Handle"]["Data"]["DataTable"] (double-Handle) + itemEntry.Spec + itemEntry.Handle - if post-Echoes patch changed InventoryItemEntry, FlagItem may throw (now caught+logged). RunSmartSort console cmd (F10) path same.
- PENDING: user to define real favorite/junk rules (thresholds their call) -> write config.lua. SmartSort only MARKS flags (fav/junk), user manually sells/dusts - marking is not destructive.

## v33b (2026-07-04) - SmartSort real rules written
- User picked: junk weapons+accessories belowLevel<=30 (flag 1), junk echoes rarity {1,2} Common+Uncommon (flag 4), keep favorite weapons aboveLevel>=38 (flag 2). Fixed swapped accessory var/comment (accessoryFavoriteFilters vs accessoryJunkFilters). config.lua rules now: weaponFavoriteFilters={{aboveLevel=38}}, weaponJunkFilters={{belowLevel=30}}, accessoryJunkFilters={{belowLevel=30}}, echoJunkFilters={{rarity={1,2}}}, rest empty. Favorites run first + block junk (L38+ weapon stays fav). L31-37 weapons untouched (neither rule).
- Config edits need game RESTART to load (or F10 -> RunSmartSort re-runs on current inventory). Repo tools/third-party-patches/SmartSort/{main,config}.lua. Backups .wfqol-bak-*.
- TEST next launch: F10 -> RunSmartSort (sorts existing inv, watch "[SmartSort] ITEM COUNT", "FlagItem type=..", "applying flag N"); loot a <=L30 weapon -> "CLIENT_NotifyItemAdded fired" + auto-junk. If "auto-sort ERROR" appears = InventoryItemEntry struct changed post-Echoes, needs FlagItem struct fix.

## v34 (2026-07-04) - CRASH: parry cost-GE off-thread load + nameplate self-HP
- CRASH (crash reporter, NEW clear error): "Fatal error [Line:2724] AssembleReferenceTokenStream for BlueprintGeneratedClass /Game/Blueprint..." during combat (log ends "combat: true" 22:17:13, fighting Bandits). = WFQoL's documented parry failure mode: activating a block ability whose cost/cooldown GE is a not-yet-loaded soft class -> game async-loads it -> class token stream finalized OFF game thread = fatal. PRELOAD list only covered 2H/DW/SnS; user's weapon's parry cost GE was missing. SmartSort CLEARED (near crash it only READ item types weapon/acc/echo=false, last "applying flag" was 30min earlier - applied nothing).
- FIX (WFQoL): new preloadAbilityGraph(ability) - reads the LIVE ability (no hardcoded paths, covers every weapon), force-loads its class + CostGameplayEffectClass/CooldownGameplayEffectClass on the GAME THREAD, one-shot per ability class, logs "parry: preloading cost graph for <class>". Called 3 places (all game-thread): tryActivateParry before activation, AutoSprint on combat-start (before first parry), and loadObjectPath(successClass) in onReloadActivated (reload-success force path). loadObjectPath strips "<ClassType> " prefix from GetFullName -> LoadAsset path. If crash recurs, the new log names the exact ability -> add its cost GE path to PRELOAD.
- FIX (ShowNameplates self-HP): user "shows my stamina + friends' HP but not MY hp". Self nameplate hides its health bars by default (only other players' show). Added showWidget(np,name) SetVisibility(0) on PlayerHealthBar/_Additive/LastHealthBar in updateNameplate (re-assert every tick, game re-hides) + capture block. Also probes SetHealthMeterVisibility/SetHealthBarVisibility/SetHealthVisibility (logs which exists, symmetric to SetStaminaMeterVisibility) -> trim to the real one next pass.
- Both apply next launch (hot-reload off). Watch log: "parry: preloading cost graph for X" (crash gone if X's cost preloaded), "[ShowNameplates] method Y ok=true" (real HP-visibility method).

## v35 (2026-07-04) - REVERT v34 dynamic LoadAsset (it caused a NEW crash)
- v34 introduced a WORSE crash: EXCEPTION_ACCESS_VIOLATION reading 0xffffffffffffffff (freed-pointer deref), all-Wayfinder frames + kernel32/ntdll, NO UE4SS frames. Log showed my loadObjectPath(successClass) in onReloadActivated firing LoadAsset EVERY reload ("Asset loaded" spam every ~10s) - dynamic mid-combat/every-reload LoadAsset + GetFullName on live class refs destabilized the game's async loader -> delayed native crash.
- ALSO learned: user is on a RIFLE (constant reloads, "parry FAILED... no block ability"). preloadAbilityGraph NEVER logged "preloading cost graph" = GetAbilityFromInputTag(BLOCK_TAG) returns nil for ranged. So v34's parry preload was dormant AND the per-reload preload was pure harm. The ORIGINAL parry crash (v34 target, AssembleReferenceTokenStream) only happens with a MELEE weapon equipped.
- FIX v35: ripped out loadObjectPath entirely + the per-reload call. preloadAbilityGraph is now LOG-ONLY (diagnostic): logs "parry: block ability = <class>" once per class (safe GetFName read, no LoadAsset). Static PRELOAD list (LoadAsset on ClientRestart, game thread, proven safe) unchanged. When user next fights with MELEE, the log names the block ability -> add its exact cost GE path to PRELOAD to kill the original parry crash safely.
- LESSON: NEVER LoadAsset dynamically in a hot path (per-reload/per-combat). Only preload via the static list on ClientRestart. Dynamic LoadAsset + GetFullName on live refs = freed-pointer/async-loader crashes.
- Nameplate: probe confirmed NO SetHealth*Visibility method exists (all ok=false). Removed the probe; kept showWidget() SetVisibility(0) on the 3 health bars. If self HP still hidden next session, a parent container is collapsing them (traverse parents next).
- Both apply next launch. PRIORITY: confirm no crash on rifle reload spam.

## v36 (2026-07-04) - proactive crash audit + CDO hardening
- CORE RULE (reconfirmed): pcall does NOT catch native access violations. ANY native call (K2_*, Get*, TryActivate*, property deref, self:get) on a stale/freed/pooled/CDO/mid-teardown object crashes THROUGH pcall. So guarding = validity+identity checks BEFORE the native call, not pcall around it.
- SYSTEMIC FIX: FindAllOf returns the class CDO ("Default__X") which passes :IsValid() but has no world/components. Added isReal(obj)=IsValid AND not Default__ prefix; applied to WFQoL getPawn (critical - CDO-as-pawn poisons everything downstream), staminaPct, forceWindowOpen(WFRangedWeaponAttributeSet), + ShowNameplates resolveHUD. Also settle-gated AutoChain (was injecting InpActEvt every 70ms with no transition pause).
- REMAINING RANKED RISKS (not yet fixed): (1) HIGH - parry MELEE AssembleReferenceTokenStream: TryActivateAbilityByClass(blockAbility) lazy-loads cost GE for weapons NOT in PRELOAD (only 2H/DW/SnS). Fix pending: melee session -> "parry: block ability = X" diag log -> add exact cost GE path to static PRELOAD. DON'T guess paths (v34 lesson) and DON'T dynamic-LoadAsset. (2) MED - SmartSort applyTagToItem does RegisterHook+UnregisterHook dynamically per matched item + SERVER_TryApplyItemFlags in the CLIENT_NotifyItemAdded hot path; hook-table mutation during item pickup is risky now that rules are live. (3) MED - open-world streaming (galloping) tears down actors WITHOUT ClientRestart so settle-gate doesn't arm; only CharacterMovement is validated, other enemy-actor native calls during streaming rely on per-call IsValid. (4) LOW - onReloadEnded TryActivateAbilityByClass(successClass soft class) can lazy-load like parry (rare, success cue usually fires first). (5) LOW - reload OnTriggered_<GUID> hardcoded to current weapon's reload graph; different weapon = missing func = pcall'd Lua error (not native), safe-ish.
- All safe pure-guard changes (no new native calls) so cannot repeat v34. Committed 8a211ce.

## v37 (2026-07-04) - UNIVERSAL parry preload (weapon-folder derived, no hardcoding)
- Diag confirmed block abilities: 2H=/Game/Blueprints/Player/GAS/GameplayAbilities/2H/GA_Player_Block_2H_C, DW=.../DW/GA_Player_DW_Parry_C, SnS=.../SnS/GA_Player_Block_SNS_C. costGE property = none (not readable; cost applied via ability graph). User: "shit ton of weapons, don't hardcode each, derive weapon-type hierarchy universal".
- SOLUTION: preloadAbilityGraph now DERIVES the weapon folder from the live block ability path (.../GameplayAbilities/<WPN>/) and sync-LoadAssets the parry cost/pushback siblings by Wayfinder's naming patterns, on the GAME THREAD, once per weapon (deduped, runs every AutoSprint tick so equip triggers it - no combat needed). Covers EVERY weapon incl future ones, zero per-weapon hardcoding.
- Patterns (token = folder name + UPPER variant; SnS folder -> SNS token): <WPN>/GameplayEffects/ AND <WPN>/ (SnS GEs sit directly in <WPN>/, 2H+DW under GameplayEffects/ - try both): GE_Player_<T>_ConsumeStamina_Parry(+_Survivalist), GE_<T>_Parry_Cooldown, GE_<T>_Parry_DamageReduction; plus <WPN>/GA_Player_<T>_Parry_Pushback + TA_Player_<T>_Parry_Pushback. Reconstructs all old hardcoded PRELOAD entries + extends. Non-existent speculative paths = harmless no-ops (LoadAsset SYNCHRONOUS on game thread = the safe kind; NOT the v34 per-reload/stale-ref pattern).
- Static PRELOAD trimmed to ONLY the 2 reload cue notifies (2HR success/fail); all weapon-parry GEs removed (now universal).
- Log line "parry: block ability=<class> folder=<folder>" per weapon confirms derivation. If a weapon crashes still, its folder/pattern differs -> log names it.

## v38 (2026-07-04) - MULTIPLAYER join crash (mods assumed single-player)
- CRASH on MP join/combat: EXCEPTION_ACCESS_VIOLATION reading 0x10 (null+small offset), all-UE4SS-frames = a mod native call. MP breaks the SP assumption: multiple player characters + multiple player nameplates now exist. (Universal parry preload CONFIRMED WORKING same log: "block ability=GA_Player_DW_Parry_C folder=.../DW" + "Asset loaded" x2, no parry crash.)
- FIX 1 (ShowNameplates, likely culprit): UI_NameplateWidget_Player:ShouldBeVisible fires for EVERY player nameplate incl remote players'. Callback did np.AttachedOwnerActor:GetAddress() - a remote/just-spawned nameplate has NULL AttachedOwnerActor -> native null-deref through pcall. Now validates np:IsValid + owner:IsValid + currentPlayer:IsValid before GetAddress. Also makes it correctly bind ONLY the local player's nameplate.
- FIX 2 (WFQoL getPawn): FindAllOf(WFPlayerCharacter_Base_C) returns REMOTE players' chars in MP (null components client-side -> crash if driven). Now prefers p:IsLocallyControlled() pawn, falls back to first-real (SP unchanged). pawnRef from ClientRestart is already local so this is mainly defensive for the FindAllOf fallback path.
- Both pure-guard (validity before native call), luaparse-clean. MP is otherwise UNTESTED - may surface more SP-assumptions (onEnemyAbility on replicated enemies, etc). Watch next MP session.

## v39 (2026-07-04) - MP hardening round 2 (user: keep hardening for MP, not disable)
- v38 (nameplate + getPawn) did NOT stop MP-join crash (same 0x10, all-UE4SS frames, right as ShowNameplates registered nameplate hooks). v38 ShowNameplates guard confirmed DEPLOYED, so crash was elsewhere = WFQoL ability hooks firing on MP-join for replicated/remote entities.
- ROOT: onEnemyAbility + onReloadActivated did self:get() then used the ability with NO IsValid check; a client-side replicated/stub ability is non-nil-but-invalid -> ab:GetClass() native null-deref (0x10) through pcall. onEnemyAbility fires IMMEDIATELY on join for already-active enemies.
- FIXES v39 (all pure validity guards, luaparse-clean): (1) onEnemyAbility ab:IsValid guard [fa943b1]. (2) onReloadActivated ab:IsValid guard [fa943b1] + AVATAR GATE: skip if ab:GetAvatarActorFromActorInfo() addr != local pawn addr (don't apply window-forcing/press-injection to remote players' reloads - would touch their ability + everyone's WFRangedWeaponAttributeSet). (3) Attack1 input hook: cache pawnRef only if self:get() valid. (4) ClientRestart: set ready/m1Held always, cache pawnRef only if p:IsValid (MP-join frame p may be mid-construct; getPawn re-finds local via IsLocallyControlled).
- User chose "keep hardening for MP" over MP-safe-auto-disable. MP still needs live test - more SP-assumptions may surface (onReloadEnded/Succeeded fire for any player's reload = minor correctness not crash; ShowNameplates updateNameplate reads local HUD = low risk).

## v40 (2026-07-05) - SmartSort FROZE game (bulk sync) -> DISABLED; + INS overlay toggle
- FREEZE (not crash): full-inventory sync event fired CLIENT_NotifyItemAdded for HUNDREDS of items at once (00:43:34 burst - wallpapers/dyes/personas/cosmetics/weapons). SmartSort processed all synchronously; each matching weapon did RegisterHook + SERVER_TryApplyItemFlags (server RPC, round-trips to host in MP!) + UnregisterHook -> game-thread freeze. This was the v36-flagged SmartSort risk realized. Added bulk-burst guard (BURST_CAP 12/1s window) + debug=false, THEN user said "just disable smartsort, i'll do manually, re-enable future" -> SmartSort:0 in mods.txt. Kept ConsoleEnabler:1 (F10 console for t.MaxFPS + future). SmartSort patch (burst guard) preserved in repo for when re-enabled.
- FPS (GPU-bound: 98% GPU / 36% CPU / 62C, streaming Discord 720p30): advised lower Resolution Quality 100->90 (streaming hides it, biggest lever), AA High->Med, View Dist High->Med, Texture Ultra->High (kills dips), Shadow Mid->Low, AO off, FX Mid->Low; hard-cap 162 (NVCP Max Frame Rate or F10 `t.MaxFPS 162`); Discord NVENC hardware encode. Target steady 162 = keep GPU ~85-90%.
- INS OVERLAY TOGGLE (user req): WFQoL writeState now includes "overlay":bool; bindToggle(Key.INS or Key.INSERT,"overlay") flips it (logs "overlay show/hide bound to INS" - if "no INS key found" appears, fix the Key name). Overlay PS poll: state.overlay=false -> $window.Hide() (app stays alive polling), true/null -> Show(). Replaced the unconditional auto-show at line ~243. Show() won't steal focus (WS_EX_NOACTIVATE). Applies next launch.
- MP hardening (v38-39) HELD: no crash on MP join this session.

## v41 (2026-07-05) - autoparry keybind-independent + nameplate boss/down fixes + diag
- LOG (01:xx session, MP boss fight, 12x BP_Spectator_Pawn_C = co-op downs): AutoParry TOTALLY SILENT all session (no "parry vs"/"FAILED"/"no timing") despite constant combat. block ability found ONCE early (GA_Player_DW_Parry_C, deduped after). INS overlay toggle CONFIRMED working ("overlay show/hide bound to INS", "Overlay OFF"). No crashes mid-session; crash was on EXIT (teardown, log just ends).
- AUTOPARRY FIXES: (1) keybind-independent activation - cache block ability CLASS (lastBlockClass); activate via TryActivateAbilityByClass even if GetAbilityFromInputTag(Input.Combat.Block) goes nil after an in-game rebind. logs "no ability at Input.Combat.Block tag" once. (2) DIAGNOSTICS to find why parry silent: onEnemyAbility logs "enemy GA seen <class>" once/class (if a boss fight logs NOTHING = boss doesn't derive from GA_AI_Base -> must hook its base class); doParry logs "not connecting (range/facing)" throttled 5s. LIKELY CAUSE: boss/these enemies don't fire GA_AI_Base:K2_ActivateAbility. Next session's "enemy GA seen" lines will confirm.
- NAMEPLATE FIXES (ShowNameplates): (1) BOSS - was OverrideReturn(NP_ENEMY ShouldBeVisible, true) forcing ALL enemy nameplates incl bosses -> conflicts with boss's dedicated health bar. Now returns true for normal enemies, FALSE for boss (owner class name contains "boss"); logs "enemy owner <class>" once each to make the boss match precise. (2) "breaks sometimes" - co-op down swaps pawn to BP_Spectator_Pawn_C; onRestart was binding self-nameplate to the spectator pawn = wrong health. Now onRestart only sets currentPlayer if pawn class == WFPlayerCharacter_Base_C (keeps last real char through down/respawn).
- EXIT CRASH: not fixed (teardown, no clean UE4SS shutdown signal; loops touch dying actors on close). Low priority (game closing, no data loss).
- Applies next launch. Watch: "enemy GA seen X" (boss ability base class), "enemy owner X" (boss class name for precise match).

## 2026-07-05 — perma-load hardening (v42)
- PERMA-LOAD (infinite loading screen at zone transition ~03:08 in log): NOT a crash, NOT a lua error - log just STOPS after "ConsoleKey F10" (ConsoleEnabler re-registers = a level load starting), no "pawn:"/"combat:" after = ClientRestart never fired = OPEN-WORLD STREAMING load. settle gate `settling()` only armed on ClientRestart, so AutoSprint LoopAsync(300) kept driving getASC/ascHasTag/getMount/preloadAbilityGraph on the GAME THREAD against half-constructed actors through the load. Can't prove mod caused it (base-game streaming is buggy + a pure game-thread hang leaves no fingerprint).
- FIX A (main.lua): transition-aware settle gate. Track pawn ADDRESS (lastPawnAddr) in the sprint loop; address change with no ClientRestart = streaming transition -> set transitionAt=os.clock() + skip tick. Covers the loading-screen case ClientRestart misses. Cost: ~1.5s sprint delay after each load (fine).
- FIX B (main.lua): GAME-THREAD FREEZE FINGERPRINT in perf watchdog LoopAsync(1000). async watchdog pings game thread each tick (gtBeat++ / ExecuteInGameThread sets gtAck); behind>=6 (~6s unanswered while watchdog still ticking) -> log "GAME THREAD FROZEN ~Ns ... perma-load / native hang, no lua error". re-arms when behind<3 (normal finite zone load recovers). NEXT perma-load will leave a log line -> disambiguates base-game hang vs mod hang.
- FIX C (main.lua): preloadAbilityGraph now bails `if settling() then return` before any sync LoadAsset (belt-and-suspenders; our rule = never sync-LoadAsset in a transition).
- Log was otherwise clean: parry working (many "parry vs X"), nameplates fine. "no timing for X" lines (GA_AI_Atk_180_R_ALC, Flowing_Bubble/Water_Ring, Flea_Latch, Bandit_Brawler_Catalyst_Punch) use 0.60s default - most don't connect anyway, not worth guessing timings.
- Deployed + committed 78393e5. NEEDS IN-GAME TEST: if it perma-loads again, check UE4SS.log for "GAME THREAD FROZEN" (= confirms hang, tells us thread state) and whether the streaming-settle stopped mod activity during the load.

## 2026-07-05 — clean session + parry-default tune (v43)
- RELAUNCH after v42: CLEAN. Ran 03:29->04:31 (>1h), multiple zone transitions all COMPLETED (pawn: lines 03:32/03:35/03:40/03:50/04:31), mount boost worked, parries landing. NO "GAME THREAD FROZEN", NO errors, NO perf hitch, NO perma-load. v42 streaming-settle held (or base-game bug didn't recur).
- RESOLVED open question: MINIBOSSES fire GA_AI_Base -> autoparry SEES boss/miniboss attacks (log: GA_Gloom_Farseer_SoI_Miniboss_Atk_Meteor_Cast_C seen+scheduled). The "boss doesn't derive from GA_AI_Base" worry is DISPROVEN for minibosses. (Full raid bosses still untested.)
- UNTIMED ATTACKS ARE NOT STATICALLY FIXABLE (investigated via Atlas/Content dump, 135k jsons):
  * GA_AI_Atk_Melee_*_ALC / GA_AI_Atk_180_*_ALC (the MOST COMMON enemy melee): CDO has Montage=null + "Get Montage From ALC"=true + "ALC Montage Key"={RowName Atk_Basic}. Montage resolved at RUNTIME per-enemy from the enemy's AnimLayerController datatable -> different montage/timing per enemy -> a GA-class-keyed table (timings.lua) physically can't hold one value. Only the runtime default applies.
  * GA_Gloom_Farseer_Atk_Channel/Blast/Meteor: HAVE a montage but NO weapon-trace notify (ranged Beam/projectile casts). No melee hit-time exists; they fail willConnect (out of range) anyway. Don't try to time ranged casts.
  * => extract-attack-timings.py correctly emits nothing for these; DON'T hand-add guessed numbers.
- TUNE: DEFAULT_HIT 0.6 -> 0.8s. Empirical median of the 295 real timings = 1.0s, melee-band(0.15-1.6) median 0.94; old 0.60 fired untimed parries ~0.35s TOO EARLY. 0.8 = near light-melee cluster (0.65-0.85), late enough for slow swings, still early. Only affects UNTIMED attacks (measured ones use exact timings.lua).
- CLEANUP: combat-state log debounce. InCombat tag micro-flaps near enemies (199 flips/session, e.g. 8 in 7s). Now lastCombat (raw) still drives sprint gate + overlay instantly; only LOG fires after state settles >=0.75s (loggedCombat/combatPendingSince). Cuts log noise, zero behavior change.
- Committed. All in main.lua. No in-game test blocker; parry-default change is felt only on untimed attacks.

## 2026-07-05 — 5 new QoL features (v44): AutoDodge / Homing / AutoHeal / Soft-face / Safe-loot + telegraph & stats overlay
- All new features gated behind state flags, toggle via existing overlay click-channel (no new keybinds). DEFAULTS: dodge=ON, homing=ON, telegraph+stats=ON; heal=OFF, face=OFF, loot=OFF (spend resources / intrusive / crash-history - can't hurt a session unless enabled).
- VERIFIED APIs (from Atlas/Content dump, 5 parallel cavecrew-investigators):
  * DODGE: /Game/Blueprints/Player/GAS/GameplayAbilities/Dodge/GA_Player_PrepDodge.GA_Player_PrepDodge_C, input tag "Input.Combat.Dodge". PrepDodge reads movement input for direction + fires weapon-specific dodge (inherits GA_Player_Dodge). Activate by CLASS via ASC:TryActivateAbilityByClass (like parry). GA_Player_System_Invulnerability_C = i-frame source, fired as GameplayEvent "Ability.Events.Unique.Player.StartSystemInvulnerability" (not directly activatable).
  * TARGETING: WFTargetingComponent on WFPlayerCharacter_Base_C. getter GetWFTargetingComponent() OR property pawn.WFTargetingComponent. Current soft-lock target = component.m_MagnetizedAimTarget (also FindSoftLockTarget() -> WFSoftLockTargetResults). ROTATION: PlayerController:SetControlRotation({Pitch,Yaw,Roll}) (degrees) - moves camera+facing.
  * PROJECTILE HOMING: all projectiles derive /Script/Wayfinder.MayhemBaseProjectile (WFProjectile_Base_BP extends MayhemProjectile). Register once: NotifyOnNewObject("/Script/Wayfinder.MayhemBaseProjectile", cb) catches all incl children. Movement comp property is ProjectileMovementComponent (fallback MovementComponent), type AirshipProjectileMovementComponent. Homing fields (verified on enemy Phoenix fireball): bIsHomingProjectile(bool)=true, HomingAccelerationMagnitude(float, phoenix uses 3000). SetHomingTarget(AActor) native/header-only (pcall it). Filter our shots: proj:GetInstigator() addr == pawn addr. Set mc.HomingTargetComponent = target.RootComponent.
  * HEAL: /Game/Blueprints/GAS/GameplayAbilities/GA_ConsumeItem_Base.GA_ConsumeItem_Base_C = potion/flask consumable (cost via GE_Cooldown_UseItem1). Activate by class. Health % read via HUD_PlayerMeters_C.PlayerHealthBar.Percent (same widget as stamina).
  * PICKUP: native WFPickup, BP /Game/Blueprints/Pickups/BP_Pickup.BP_Pickup_C. Collect via game's own path pk:OnAwarded(WFCharacter) - NOT K2_SetActorLocation (that's what crashed old AutoLoot on pooled/CDO). Player has InteractInstigatorComponent (native interact logic).
  * DAMAGE: NO clean BP damage-dealt UFunction to hook (all via native WFPlayerEnemyDamageExecution / BP_WFBaseDamageExecution; Health attr in WFCharacterAttributeSet, native). => did NOT build DPS meter; built session STATS from reliable signals instead (parry/parryFail/dodge/heal/seen counters).
  * MOUNT SUMMON: NOT_FOUND in dump (C++/item-system) -> deferred, did not guess.
- IMPL (main.lua): AutoDodge routes in onEnemyAbility (melee->parry as before; attack-but-not-melee=ranged/AOE -> doDodge) + doParry falls back to doDodge on low-stamina/parry-failed. isAttackAbility() new (Ability.Characteristic.Attack tag). Homing/AutoHeal(500ms)/soft-face(80ms LoopAsync)/safe-loot(600ms, addr-deduped OnAwarded) added before overlay-state section. writeState json extended: dodge/homing/heal/face/loot flags + statParry/ParryFail/Dodge/Heal/Seen + incoming/incomingKind/incomingTs telegraph.
- OVERLAY (tools/overlay/WFQoL-Overlay.ps1, runs from REPO not game folder): 5 new rows (auto via $features array), Rows wrapped in ScrollViewer (any height fits), default Height 236->360, TelegraphBar under header (amber=PARRY, blue=DODGE, shows ~2s off incomingTs), footer now "P# D# H# | mode" session stats.
- Applies NEXT LAUNCH (no hot-reload; running overlay self-exits ~20s after game close, mod relaunches it). NEEDS IN-GAME TEST: (1) dodge fires on ranged/AOE + low-stamina (log "dodge vs X"); (2) homing - log "homing: projectile notify registered", shots curve into soft-lock target (tune HOMING_ACCEL 12000 if weak); (3) telegraph + stats show on overlay; (4) if enabling HEAL/LOOT watch for potion waste / dupe. SetHomingTarget + m_MagnetizedAimTarget are the least-certain reads (pcall'd - worst case feature idles, no crash).

## 2026-07-05 — v44 crash (autoloot) + purge + teardown-gating (v45)
- CRASH: EXCEPTION_ACCESS_VIOLATION reading heap addr, at a transition (ConsoleKey F10 05:23:28). Log timeline: v44 loaded clean, homing hooks registered (ReceiveBeginPlay/ComputeInitialSpeed/FindTargetActor ALL registered on WFProjectile_Base_BP_C), dodge+homing ran ~73s clean THEN user enabled heal+face+LOOT via overlay (05:22:50-52) -> 36s later crash at transition.
- ROOT CAUSE: loot's pk:OnAwarded(pawn) = gameplay-MUTATING native call on pooled/CDO pickups during teardown (pcall can't catch native AV). Same crash CLASS as the original AutoLoot (that used K2_SetActorLocation). CONFIRMED unsafe direction: calling the game's award fn directly on pooled pickups. No safe external-collect path verified (future: maybe drive InteractInstigatorComponent, not OnAwarded).
- FIX v45: (1) PURGED autoloot entirely (user: "no need for autoloot, purge it") - removed state.loot, loop, overlay LOOT row, writeState field. (2) TEARDOWN-GATED the other new native paths that lacked it: getSoftTarget (+ so homing & face), applyHoming, onProjectileLaunch now bail on settling(). homing was the only new path with NO settle gate (hook-driven, fires on projectile spawn incl during teardown) - now gated.
- CONFIRMED GOOD from the v44 log: homing hooks DO register (all 3 fns exist on WFProjectile_Base_BP_C); ReceiveBeginPlay is the reliable per-launch one. equipped weapon was 2HScy (block=GA_Player_Block_2HScy_C). dodge/homing ran through combat without crashing.
- STILL ON: AutoDodge + Homing (default ON, now teardown-safe). AutoHeal + soft-Face still available (default OFF, both settle-gated + getSoftTarget-gated). NEEDS in-game test: relaunch, verify no crash on transitions; test homing curves shots (needs projectile weapon ability + soft-lock target); face/heal if re-enabled.
- LESSON: never call a gameplay-MUTATING native fn (OnAwarded, award/collect/apply) on pooled/FindAllOf'd actors - only READ (guarded) or activate player-owned abilities. Every new native-touching feature MUST be settling()-gated, including hook-driven ones (not just LoopAsync bodies).

## 2026-07-05 — purge AutoDodge + AutoHeal + soft-face (v46)
- User: purge heal + face (no need); AutoDodge "doesn't seem to work" -> check, improve or purge.
- AutoDodge VERDICT = purge. Root cause it was inert: doDodge gated by willConnect (melee CONNECT_RANGE=450 + facing), but the whole point was dodging RANGED/AOE attacks which come from >450 -> willConnect false -> dodge never fires for its main case. Only the low-stamina melee fallback could trigger (rare). Reliably dodging ranged needs projectile-travel timing (montage hitTime != projectile arrival) = out of scope. Not worth improving.
- PURGED (main.lua): state.dodge/heal/face flags; doDodge/tryDodge/DODGE_TAG/dodge vars; healthPct + AutoHeal loop + getConsumeClass; soft-face loop + FACE_STEP; isAttackAbility; onEnemyAbility reverted to bool verdict (melee->parry only, no dodge routing); doParry dodge-fallbacks removed; writeState json dropped dodge/heal/face + statDodge/statHeal; stat table = {parry,parryFail,seen}.
- KEPT: Homing (uses getSoftTarget - stays), telegraph (incoming, now always kind="parry"), session stats (parry/parryFail/seen). Overlay: removed DODGE/HEAL/FACE rows, footer now "parries:N seen:N | mode".
- APIs still valid if ever re-added (in reference memory): dodge=GA_Player_PrepDodge_C/Input.Combat.Dodge, heal=GA_ConsumeItem_Base_C, face=SetControlRotation. Deployed 4fd8f20.
- SIDEQUEST (not mod): set up OptiScaler for FSR2->FSR4.1.1 on the RX 9070 XT (proxy=dxgi.dll NOT dwmapi/UE4SS; ShortcutKey=0x23 End off INS; Dx12Upscaler=fsr31 auto-loads FSR4 on RDNA4; DLSS-spoof via fakenvapi). Working. Then perf: game GPU-bound on Ultra (93% GPU/41% CPU, 1% low 34) -> retuned game Engine.ini (%LOCALAPPDATA%/Wayfinder/Saved/Config/WindowsNoEditor/Engine.ini, [SystemSettings] overrides menu): shadows 2/1024/2cascades, effects 2, fog cheaper, SSR 1, view 3. Advise FSR4 Quality->Balanced + FreeSync + cap 120 for stable fps.

## 2026-07-05 — AutoChain heavy attacks (v47)
- Extended AutoChain (F7) to also chain HEAVY melee attacks. Hold M1 = light combo (Attack1), hold M2 = heavy combo (Attack2). Same toggle, one loop.
- VERIFIED (dump WFPlayerCharacter_Base.json + Input.ini): Attack2 = InpActEvt_Attack2_K2Node_InputActionEvent_40 (press) / _41 (release), bound to RightMouseButton. Mirrors Attack1 _36/_37 (LMB).
- IMPL: RMB const; m2Held + sendRelease2 vars; AutoChain loop injects Attack1 when m1Held AND/OR Attack2 when m2Held (game gates next swing by animation, 70ms tick just keeps combo flowing); Attack2 _40/_41 hooks in registerAll mirror Attack1 (injecting-guard so injected presses don't re-set m2Held; cache pawnRef on press). Deployed + committed.
- NOTE: user's manual heavy attacks did NOT appear in UE4SS.log (mod wasn't hooking Attack2 yet) - the BP dump is the source of truth for input UFunction names, not the log.

## 2026-07-05 — parry no-block fix + always-sprint (v48)
- BUG (user, confirmed in log "parry FAILED vs X after 3 attempts" spam): on a weapon with NO block/parry, AutoParry was janky - cancelling the player's own actions. ROOT CAUSE: lastBlockClass cached a PREVIOUS weapon's block ability class (v41 keybind-rebind fallback); on a no-block weapon GetAbilityFromInputTag(Input.Combat.Block)=nil but the stale class made doParry force a parry -> ServerCurrentMontageStop(0.15) cancelled whatever the player was mid-action, 3x per enemy attack (the force ladder).
- FIX: tryActivateParry now REQUIRES a live block ability from the tag; returns "noblock" when nil. doParry bails on "noblock" BEFORE the montage-stop force ladder = no action-cancel on no-parry weapons. Dropped lastBlockClass/blockNilLogged entirely (the rebind fallback caused this bug; rare rebind case regresses - revisit only if reported; can't distinguish rebind-nil from no-block-nil via the tag alone).
- FEATURE: AutoSprint now ALWAYS auto-runs when moving, IN and OUT of combat (removed the `not inCombat` gate on shouldSprint). User accepts the in-combat stamina drain. inCombat still used for the combat: log + mount branch.
- Deployed 523ed3c. Both apply next launch.

## 2026-07-05 — weapon-aware parry (rebind-safe) + validated stutter cvars (v49)
- USER: rebinds parry key AND swaps weapons -> parry must be BLOCK/PARRY WEAPON-AWARE, not just tag-based. v48's "require live tag" broke rebind (tag goes nil after rebind, indistinguishable from no-block).
- FIX: findGrantedBlockAbility(asc) enumerates asc.ActivatableAbilities.Items[].Ability (verified: FGameplayAbilitySpecContainer public on UAbilitySystemComponent; .Items array; each spec .Ability), matches name `^GA_Player_Block_` (not Broken) OR `_Parry`. TAG-INDEPENDENT = survives rebind + correctly nil on no-block weapon. currentBlockAbility(asc) = tag fast-path then enum fallback. tryActivateParry uses it; "noblock" when none -> doParry bails before montage-stop. Diag logs "block found via granted-ability scan (<class>)" once when the enum path kicks in. Fully guarded (native struct walk in pcall; failure -> nil -> parry stands down, no crash). NEEDS in-game test: rebind parry key + no-block weapon + block weapon, confirm enum finds block on rebind + skips no-block.
- Block ability names per weapon (dump): GA_Player_Block_2H/2HS/2HScy/SNS_C, GA_Player_2H_Parry_C, GA_Player_DW_Parry_C. Block_Broken_* = the block-break, NOT player block (excluded).
- STUTTER (validated deep-research, applied game-side Engine.ini [SystemSettings], file is READ-ONLY - clear IsReadOnly, edit, re-set): added r.OneFrameThreadLag=1, r.GTSyncType=1, gc.TimeBetweenPurgingPendingKillObjects=120 (kills rhythmic 30-60s GC spikes), r.Streaming.LimitPoolSizeToVRAM=1 + FullyLoadUsedTextures=0 + HLODStrategy=2, r.ShaderPipelineCache.Enabled=1 + r.PSOPrecaching=1. Root cause = shader-PSO comp + traversal/streaming + frame-pacing (dev-confirmed shader caching + community-confirmed repeatable streaming stutter), NOT hardware. PLACEBO cvars (ignored in shipped UE): r.CreateShadersOnLoad/UseShaderPredraw/Shaders.Optimize/UseShaderCaching (mine had none). Best lever: RTSS cap 3-5 below refresh + FreeSync + VSync on. RDNA4: test ReBAR OFF. FSR4 can't use Adrenalin override (game=FSR2, needs signed FSR3.1 DLL) -> OptiScaler only.
- DELIVERABLE: tools/optimization/wayfinder-stutter-prompt.md = universal parameterized ({VRAM_GB}/{RESOLUTION}/{GPU_VENDOR}/{REFRESH_HZ}) Claude Code prompt for any Wayfinder rig.
- Claude Code CAN do deep research (WebSearch/WebFetch + deep-research skill) - user did it in claude.ai browser but can do it here.
- CRASH (v50, 2026-07-05): EXCEPTION_ACCESS_VIOLATION reading 0x...709c, stack all UE4SS frames, ~30s post-combat at hub. Fault addr = obj_base+0x9c; game offset dump says UObjectBase::CreationTime=0x9C -> native read of an AActor lifetime field on a FREED actor = actor use-after-free. ROOT CAUSE: gc.TimeBetweenPurgingPendingKillObjects=120 (added in v49 stutter batch; default 60) DOUBLED how long dead actors linger as garbage in GUObjectArray -> doubled the window where a dead actor still passes UE4SS :IsValid() but its memory is torn down. Every native-touching loop/callback in the mod gates on :IsValid() then calls a native (K2_GetActorLocation/GetVelocity/MaxWalkSpeed/mount reads) - the widened window turned a latent near-miss into a hard AV. FIX: removed the cvar (default 60 restored). No stutter benefit anyway (game just unoptimized). LESSON: object-lifetime cvars (gc.*) are NOT free for UE4SS mods that hold/deref UObject refs across ticks/delays - they resize the UAF window our IsValid gates depend on. Deeper Lua hardening (stale mountBoostRef re-validate, drop stale-enemy read in delayed doParry/willConnect) held back unless it recurs.

## 2026-07-05 — v51: purge homing mod (last experimental feature removed)
- User: "purge homing mod entirely" (after transition-crash reports). Removed homing fully from main.lua (state.homing, applyHoming, onProjectileLaunch, PROJ_BASE, HOMING_ACCEL, homedProj, homingHooked, registerAll projectile-hook block, writeState homing field, getSoftTarget) + overlay (HOMING row + Set-Row).
- WHY it was crash-risky: homing's getSoftTarget() called `TargetingComponent:FindSoftLockTarget(...)` - a native call that ITERATES LIVE WORLD ACTORS. During a level transition an actor is mid-teardown with a null class -> native read at obj+0x10 (ClassPrivate) = EXCEPTION_ACCESS_VIOLATION, and pcall CANNOT catch a native AV. Also the 3 projectile-BP hooks (ReceiveBeginPlay/ComputeInitialSpeed/FindTargetActor) fired during teardown. LESSON: never call world-iterating native queries (FindSoftLockTarget / FindAllOf-then-native) from hot paths or hooks that can fire during a transition; a single settling() gate isn't enough because it only arms on ClientRestart (open-world/console transitions don't).
- WFQoL feature set is now the STABLE core: AutoChain (M1 + M2/heavy) / AutoParry (weapon-aware, no-block-skip) / AutoSprint (always-on) / AutoReload + INS overlay + parry telegraph + session stats. ALL experimental extras removed across v46 (dodge/heal/face/loot) and v51 (homing). getSoftTarget/WFTargetingComponent aim-assist path is no longer used by any live feature.
- Deployed + committed 97257f6 (on 52c7ee4). Reference [[reference-wayfinder-ue4ss-api]] still documents the targeting/projectile APIs for future use.

## 2026-07-05 — v52: load-time GetClass-on-fresh-pawn crash (0x10)
- CRASH: EXCEPTION_ACCESS_VIOLATION reading 0x10 (=UObjectBase::ClassPrivate offset), during the FIRST world load (log: ShowNameplates installHooks registered hooks 5-9, then crash, NO "pawn:" log = WFQoL ClientRestart never ran). NOT homing (already purged, 0 refs in log). Reproduced across launches.
- ROOT CAUSE: both mods read `pawn:GetClass():GetFName()` on the RAW ClientRestart NewPawn. On the first load the possessed pawn can still be mid-construction with a NULL ClassPrivate ptr -> GetClass() derefs null+0x10 = native AV. pcall CANNOT catch it. ShowNameplates onRestart runs first (its ClientRestart hook registered before WFQoL's) so it crashed before WFQoL's handler.
- FIX: ShowNameplates onRestart -> wrap the class read in ExecuteWithDelay(300) + re-validate pawn:IsValid() before GetClass (let the pawn finish constructing). WFQoL ClientRestart -> removed the cosmetic "pawn: <class>" log entirely (was p:GetClass():GetFName(), same 0x10 surface); pawnRef=p still cached, getPawn() re-validates lazily on use.
- LESSON: NEVER call GetClass()/GetFName() synchronously on a pawn/actor handed to you by a ClientRestart/BeginPlay/spawn hook - the class ptr may be null mid-construction (0x10) and IsValid() does NOT guarantee a non-null class. Defer + re-validate, or skip the read.
- STILL A CANDIDATE if crashes continue: ShowNameplates enemy ShouldBeVisible line ~94 `owner:GetClass():GetFName()` (boss-skip patch) - same 0x10 pattern but fires when enemies are constructed (lower risk). Base-game transition UAF fragility also persists (see v50). Deployed WFQoL + ShowNameplates(game+repo), committed 09cdf94.

## 2026-07-05 — v53: ShowNameplates MP rewrite (per-owner health+stamina, crash-proof)
- PROBLEM: co-op nameplates broken for BOTH self + teammates (health blank, stamina blank, flicker) + repeated 0x10 load/transition crashes.
- KEY DISCOVERY (dump): the nameplate widget UI_NameplateWidget_Player_C is SELF-DRIVEN from its owner's ability system, and EVERY player pawn (incl remote teammates) already has its own one (via BP_WFPlayerNameplateWidgetComponent_C). So the old approach (scrape LOCAL HUD_PlayerMeters -> push onto a single address-matched self-nameplate) was architecturally wrong for MP: HUD only has local values, address-match fragile, raw SetVisibility fought the widget's own hide logic -> flicker.
- WIDGET API (UI_NameplateWidget_Player_C): GetHealthPercentage(Actor)->(Success,pct), GetStaminaPercentage(Actor)->(Success,pct) [read owner ASC Health/MaxHealth, Stamina/MaxStamina], native OnHealthUpdated keeps health current once shown, SetHealthMeterVisibility(bool)/SetStaminaMeterVisibility(bool) = intended toggles (use these not raw SetVisibility). Bars: PlayerHealthBar, PlayerHealthBar_Additive(shield), PlayerLastHealthBar(trail), characterStaminaFill. Owner = native AttachedOwnerActor. Enemy widget self-drives via WaitForAttributeChangeUI.
- NEW DESIGN: 200ms LoopAsync (settle-gated, ExecuteInGameThread) -> FindAllOf("UI_NameplateWidget_Player_C") -> for each real np: SetHealth/StaminaMeterVisibility(true) + push GetHealthPercentage/GetStaminaPercentage(np.AttachedOwnerActor). Drives self + every teammate from ITS OWN owner. OverrideReturn ShouldBeVisible=true (player+enemy) with SAFE no-op callback (no owner deref). Removed: currentPlayer/playerNameplate/updateNameplate/resolveHUD/HUD hooks/onRestart GetClass/enemy boss-skip GetClass.
- CRASH FIX: added a settle gate (SETTLE_SECS=1.5, transitionAt armed on ClientRestart) - ShowNameplates had NONE, which is why it AV'd during teardown/construction. ClientRestart handler no longer reads the fresh pawn's class.
- CAVEATS (need in-game/MP test): (1) Health/MaxHealth ARE replicated (game's HUD_PartyMemberWidget reads remote teammate health same way) -> friends' health guaranteed. STAMINA replication to non-owners UNCERTAIN -> GetStaminaPercentage(remotePawn) may return Success=false -> teammate stamina best-effort (bar left untouched, not blanked). (2) Confirm UE4SS multi-return convention of GetHealthPercentage/GetStaminaPercentage (ok,pct) at runtime; fallback = read owner ASC directly via GetFloatAttributeFromAbilitySystemComponent(Health/MaxHealth). (3) Forcing all nameplates always-visible = slight clutter (accepted). Deployed game+repo, committed 178b66f.

## 2026-07-05 — v54: nameplates MP-join crash (remote teammate ASC deref)
- v53 loaded fine, ran solo clean, but crashed a few seconds after JOINING co-op ("joined ok, moved a bit, then crashed"). CAUSE: the 200ms drive loop called np:GetHealthPercentage/GetStaminaPercentage on EVERY UI_NameplateWidget_Player_C incl remote teammates; those getters read the owner's ASC, and a REMOTE teammate's ASC is null/unresolved client-side right after join -> native AV (uncatchable by pcall). The settle gate only arms on LOCAL ClientRestart, so a remote player spawning mid-session isn't covered.
- FIX (v54): split visibility from values. SetHealthMeterVisibility/SetStaminaMeterVisibility forced for ALL nameplates (pure widget toggle, no ASC read = safe). GetHealth/GetStaminaPercentage pushed ONLY for the LOCAL player's nameplate, matched by addr: owner:GetAddress() == localPawn:GetAddress() (localPawn via UEHelpers:GetPlayerController().Pawn; GetAddress is a safe pointer read, no internal deref). Remote teammates: HEALTH self-drives natively once visible (replicated); STAMINA best-effort (meter shown, fill left to game - can't safely read remote stamina).
- LESSON: in MP, NEVER read a remote pawn's ASC/attributes from a mod loop - the client may not have resolved the ASC link -> null deref. Only read the LOCAL pawn's ASC. Compare identity via GetAddress (safe) not IsLocallyControlled/GetClass (native derefs on transient owners). Visibility/widget toggles are safe on remote nameplates; attribute reads are not.
- User also noted LOW FPS in MP - likely network/game-side, not the mod (200ms loop is cheap); FPS is a separate track (Engine.ini/optiscaler done earlier). Deployed game+repo, committed a061431.

## 2026-07-05 — v55: nameplates FPS fix (drop FindAllOf polling) + enemy removed + stamina responsive
- FPS REGRESSION (my fault): v53/54 replaced the OLD event-driven hooks with a 200ms LoopAsync that called FindAllOf("UI_NameplateWidget_Player_C") every tick. FindAllOf scans the ENTIRE GUObjectArray (~135k objects) -> 5x/sec on the game thread = big cost, tanked MP fps. LESSON: NEVER poll FindAllOf on a timer; it is a full object-array scan. Use hooks (event-driven) or cache the result. Cached/lazy FindAllOf (only when the ref goes invalid) is fine; per-tick is not.
- REWRITE (v55, event-driven, NO polling): OverrideReturn(NP_PLAYER:ShouldBeVisible, true, cb) forces every player nameplate's meters visible (pure widget toggle, safe on remotes) + captures the LOCAL nameplate once (owner GetAddress == localPawn GetAddress). LOCAL values driven by the HUD meter-change hooks (OnHealthChanged/UpdateShieldBar/OnStaminaChanged) -> driveSelf mirrors the LIVE HUD bars (PlayerHealthBar/PlayerShieldBar/HUD_PlayerStaminaMeters.PlayerStaminaMeter .Percent) onto the self nameplate. resolveHUD FindAllOf is cached (bounded, not per-frame).
- STAMINA FIX: v53 used np:GetStaminaPercentage(owner) (ASC compute, wrong/unresponsive). Now mirror the live HUD stamina meter .Percent = the real value the HUD shows = responsive. (Teammate stamina still not driven - can't read remote ASC safely; teammate health is native.)
- ENEMY NAMEPLATES REMOVED: user - "enemies enabled by default, don't double-enable; mod is for us + teammates, not NPCs." Dropped the NP_ENEMY OverrideReturn entirely (also removes a hook + the boss-skip 0x10 surface for good). Enemy nameplates left to the game.
- NET: players only, event-driven (no fps hit), self health+stamina responsive (HUD mirror), teammates visible + health native, remote ASC never touched (no MP-join crash), settle-gated, no GetClass. Deployed game+repo, committed 458c493.

## 2026-07-05 — v56: allies healthbar-only (no stamina)
- User: allies just need healthbar, no stamina. ShowNameplates now forces SetHealthMeterVisibility(true) for ALL player nameplates (self + allies) but SetStaminaMeterVisibility(true) ONLY on the local player's nameplate (gated addrOf(np)==addrOf(selfNameplate) in ShouldBeVisible; driveSelf is local-only). Allies = health only; self = health + stamina (HUD-mirrored, responsive). Committed 6bdfdd7.

## 2026-07-05 — v57: SmartSort re-enabled + duplicate dedup via overlay SORT button
- User (leveling, not max, lots of dupe gear): junk low-level + DUPLICATE weapons/trinkets/echoes; keep+favorite the highest-level copy of a dupe, junk the lower ones; favorites self-de-stack (re-sort de-favorites the stale best). Trigger = an OVERLAY BUTTON (not auto, not console). Marks only, never sells (user sells/dusts junk in-game).
- SmartSort capabilities (config.lua): per-item filters belowLevel/aboveLevel/slots (weapon+accessory) + rarity/upgraded (echo), junk+favorite. NO built-in dedup (each item judged in isolation). Flags: 2=favorite, 1=weapon/accessory junk, 4=echo junk (dust). applyTagToItem = RegisterHook + SERVER_TryApplyItemFlags (server RPC) + UnregisterHook per item -> hundreds synchronously = FREEZE (why it was disabled; BURST_CAP=12/1s guards the auto-add path).
- ADDED (SmartSort/scripts/main.lua, appended): sortOwnedItems(pic) - GetItemsByTag, group by RowName per category (ss_key = entry.Handle.Data.RowName:ToString), level via ss_level (echoExpToLevel / expToLevel + WEAPON_CURVE/ACCESSORY_CURVE). DUPES (#group>1): favorite highest-lvl (flag 2), junk rest (1/4). UNIQUE: ss_ruleFlag applies config belowLevel/aboveLevel/rarity. All flag ops go through a BATCHED queue (8 per 60ms via ExecuteWithDelay) so it never freezes. Triggered by a poll loop reading Mods/SmartSort/smartsort-cmd.json {seq} (LoopAsync 500ms, tiny file read - no object scan).
- OVERLAY (tools/overlay/WFQoL-Overlay.ps1): added "SORT OWNED ITEMS" action button (below the toggle rows) that writes smartsort-cmd.json {seq=unix ms} to the SmartSort folder; flashes "SORTING...". Path = Split-Path(Split-Path $StateFile)\SmartSort\smartsort-cmd.json.
- mods.txt: SmartSort 0 -> 1 (re-enabled). config.lua kept as-is (weaponJunk belowLevel30, weaponFav aboveLevel38, accessoryJunk belowLevel30, echoJunk rarity 1-2).
- NEEDS IN-GAME TEST (game was closed; can't verify item struct offline): (1) click SORT, log should show "SORT: N items scanned, X queued (fav/junk)" + "sort complete"; verify dupes get best-favorited + rest junked, uniques follow config. (2) confirm ss_key (RowName:ToString) actually groups same-item copies (if grouping wrong, dedup won't work - adjust the key). (3) confirm no freeze (batched). (4) re-sort after getting a higher dupe should de-favorite the old best. Deployed game+repo, committed ba75d8b.

## 2026-07-05 — v58: SmartSort never junks uniques (keep >=1 of every item)
- User feedback on v57: it junked UNIQUE items (single copy, e.g. Geartooth Toolbox, Tempest weapon) because sortOwnedItems ran the config belowLevel junk on #group==1 items. Rule now: NEVER junk a unique - always keep >=1 of every item, ALL categories (weapons/accessories/echoes), even when lower level than current gear.
- FIX: sortOwnedItems removed the unique (#group==1) branch entirely -> uniques untouched (no junk, no favorite). Only DUPLICATE groups (#group>1) get processed: favorite highest-level, junk the rest. Category-agnostic (groups by RowName across all types), so universal. Also DISABLED the auto-sort-on-pickup hook (CLIENT_NotifyItemAdded) - it judged items per-item and couldn't tell a unique low-level pickup from a dupe -> junked uniques. All sorting is now the overlay SORT button only (dupe-aware). ss_ruleFlag/config filters now unused by the button (kept defined, harmless; RunSmartSort console legacy still uses old FlagItems but user uses the button).
- NET: click SORT -> only duplicate EXTRAS get marked sell (best copy of each kept+favorited), every unique kept regardless of level. Marks only. Deployed game+repo, committed 40b3fed.

## 2026-07-05 — v59: SmartSort echo-rarity dedup + idempotent self-heal (all categories)
- User: echoes must respect RARITY (higher rarity never dusted, only lower) - example: an EPIC Shrike Enforcer got dusted because dedup ranked by LEVEL only. Also: SORT should self-heal (clear wrong marks) on ALL categories so no manual deselect on relaunch.
- sortOwnedItems REWRITTEN idempotent + self-correcting: per item compute DESIRED flag, apply only if it DIFFERS from current (entry.Spec.ItemFlags) -> re-clicking SORT clears stale/wrong marks (un-dusts). Rules: FAVORITED (flag 2) + equipped never touched; UNIQUE -> desired NONE; WEAPON/ACCESSORY dupes -> keep highest LEVEL (NONE), junk rest (1); ECHO dupes -> keeper = highest RARITY then level (NONE), of the rest DUST (4) only rarity<=ECHO_DUST_MAX_RARITY(=2, common/uncommon), keep rare+ (NONE). NO auto-favorite anymore (was causing favorite-stacking + conflicts with respecting manual favorites) - favorites are 100% player-managed + respected.
- Flags recap: 2=favorite, 1=weapon/accessory junk, 4=echo dust, 0=none/clear. echoRarity + ItemFlags read from entry.Spec (confirmed fields). equipped-skip is best-effort (entry.Spec.bEquipped/Equipped - may not exist -> inert; needs runtime check).
- NEEDS IN-GAME TEST: (1) SORT log "SORT: N items, X marked, Y cleared"; verify epic/rare echoes NOT dusted, common/uncommon dupes dusted, keeper of each kept; (2) re-click SORT un-dusts anything wrongly marked (self-heal) - CONFIRM flag 0 actually clears a mark in-game (if not, clears are no-ops -> need a different clear value); (3) equipped items not marked (if they are, find the real equipped field). Deployed game+repo, committed 76cd0dc.

## 2026-07-05 — v60: SmartSort dedup rare/epic extras too (keep 1 best per group)
- Log from v59 sort: "SORT: 754 items, 2 marked, 23 cleared" -> self-heal + flag-0 CLEAR CONFIRMED WORKING (23 un-marked). But only 2 dusted because the ECHO_DUST_MAX_RARITY=2 floor kept ALL rare+ dupes (e.g. ~13 rare Wurm Echo dupes never dusted). User: keep unique + keep the BEST, but don't hoard high-rarity dupes.
- FIX: removed the rarity floor. DUPLICATES -> keep exactly 1 (the keeper), mark ALL others regardless of rarity (echo->dust 4, weapon/acc->junk 1). Keeper = echoes: highest RARITY then highest CurrentExp (most upgraded); weapons/accessories: highest CurrentExp. So epic/most-upgraded is the keeper (never marked), rare/common dupes get dusted. Uniques still kept (>=1 of every item). Idempotent self-heal on all (unchanged).
- entry.Spec fields used: echoRarity, CurrentExp, ItemFlags (all confirmed). equipped-skip still best-effort (bEquipped/Equipped guess). Deployed game+repo, committed.

## 2026-07-05 — v61: AutoSprint simplify (=#1 FPS fix) + nameplates solo fix
- CPU-bound low FPS (CPU 90% / GPU 45%). Full-log analysis: WFQoL AutoSprint "sprint: direct speed X->Y" = 376 lines = ~37% of the whole log = the dominant continuous CPU offender. It was STUCK in the direct-speed fallback (ability + tag "inert" at startup once) and re-wrote CharacterMovement.MaxWalkSpeed every 300ms poll (up to 2x/sec) because the game resets it each frame -> endless ping-pong + spam + jittery/broken sprint. Everything else bounded (onEnemyAbility activity-gated, ShowNameplates silent event-driven, SmartSort click-transient, no errors).
- FIX (user: "only real sprint", "simple, don't overcomplicate", "insta run on W"): ripped out the whole 3-mode escalation (sprintMode/sprintTries/tag-inject/direct-speed/speedBoosted/origMaxWalk/stopSprintAssist). On-foot AutoSprint is now ONE line: `if state.sprint and velSq(pawn)>=MIN_SPEED_SQ then asc:TryActivateAbilityByClass(getSprintClass(), true) end` (GA_Player_Sprint, bound to Input.Sprint). Re-activate each tick while moving; no fallback, no SPRINTING_TAG check, no logging. If the real sprint is inert in some content it just does nothing (no fake speed hack). Mount branch unchanged. sprintMode kept as static "real" for overlay/diag. This deletes the 376-line spam + the continuous MaxWalkSpeed writes = the main mod CPU load.
  * CAVEAT: if class-activation of GA_Player_Sprint is genuinely inert (activates+ends w/o input held), sprint won't move faster - would then need Input.Sprint input-press injection (there's NO InpActEvt_Sprint on the pawn; sprint is input-tag-driven). Test in-game.
- PERF TRIM (moderate): sprint loop's per-tick preloadAbilityGraph(GetAbilityFromInputTag(BLOCK_TAG)) throttled to every 10th tick (~3s) via preloadTick. onEnemyAbility left as-is (bounded).
- NAMEPLATES SOLO FIX: v55/56 event-driven rewrite broke solo self hp/stamina - it revealed bars only via SetHealthMeterVisibility/SetStaminaMeterVisibility (unproven). Restored showWidget() = raw SetVisibility(0) on the 4 bars (PlayerHealthBar/PlayerLastHealthBar/PlayerHealthBar_Additive/characterStaminaFill) IN ADDITION, in driveSelf + ShouldBeVisible capture. Added one-shot diags: "player ShouldBeVisible firing", "self nameplate captured", "self drive OK: hp/shield/stam". If still blank next launch those lines pinpoint capture-vs-HUD-resolve.
- Deployed game+repo, committed+pushed 085abba. NEEDS in-game test: (1) W = insta real-sprint (or silently inert), (2) no "direct speed" spam, CPU down/FPS up, (3) solo self nameplate shows hp+stamina (check the 3 diag lines).

## 2026-07-05 - v62: on-foot AutoSprint FIXED (real ability was inert)
- ROOT CAUSE (log line 525 was the ONLY sprint line: "mount baseline 713 boosting to 998", ZERO on-foot): v61's on-foot `asc:TryActivateAbilityByClass(GA_Player_Sprint)` is INERT. GA_Player_Sprint is an INPUT-HELD ability (activate-on-press / end-on-release) - class-activation fires it then it ends instantly with no held input = no sprint. Confirmed: no on-foot sprint effect ever.
- FIX: drive on-foot the SAME proven way the mount branch does - direct MaxWalkSpeed cap. Added footBaselines{}/footBoostRef/footBoostAddr/stopFootBoost() mirroring the mount helpers. On-foot block now: baseline captured ONCE per pawn address, target = base*1.5, re-assert ONLY when maxWalk drops below target-1 (no per-tick write = no spam), ONE-shot "sprint: foot baseline N boosting to M" log. Out-of-combat gated (lastCombat). NO velocity gate -> cap held even at standstill so pressing W = INSTA sprint (user's exact ask).
- Why direct-speed is OK now (v61 killed it for spam): the spam was the per-write LOGGING + escalation churn, NOT the write itself - mount proves conditional re-assert at 300ms + one-shot log = no spam/fps hit. sprintMode diag string "real"->"speed".
- getSprintClass/SPRINT_CLASS/MIN_SPEED_SQ now unused but LEFT (harmless; don't strip helpers).
- SmartSort: image "sort not perfect" = actually CORRECT per rules. S&D 15-equipped kept + 13 sold; Tempest one 14 kept (keeper) + dup 14 sold + 13 kept because FAVORITED (heart). Favorites never sold by design. No code change. Log "725 items, 6 marked, 0 cleared" stable = converged (6 = delta re-marked, most already flagged from prior session).
- Deployed game+repo (identical), committed. NEEDS in-game test: press W out of combat -> should insta-run at ~1.5x; log shows "sprint: foot baseline .." once, no spam; combat/stop restores walk speed.

## 2026-07-05 - v63: REAL on-foot sprint via WFLocomotionComponent (found in ObjectDump)
- DISCOVERY (UE4SS_ObjectDump.txt): the game's real sprint driver is WFLocomotionComponent, reached via pawn.LocomotionComponent (WFPlayerCharacter:LocomotionComponent ObjectProperty offset 0x890). Functions: BeginSprint() / EndSprint() (both parameterless) / IsSprintingLocomotionStateActive()->bool. Also WFPlayerCharacter:IsPlayerSprinting().
- v62's MaxWalkSpeed cap DID fire (log "sprint: foot baseline 550 boosting to 825") but user still felt lag + wanted simpler. REPLACED with the real sprint: getLoco(pawn)=pawn.LocomotionComponent; on-foot block calls loco:BeginSprint() when (state.sprint AND not lastCombat AND velSq>=100^2) AND not already sprinting; loco:EndSprint() when active but not wanted. Begin/End fire ONLY on a state CHANGE (guarded by IsSprintingLocomotionStateActive) = near-zero per-tick work, no MaxWalkSpeed writes, real anim/stamina/speed. velocity gate = must be moving (press W) to sprint.
- Removed footBaselines/footBoostRef/footBoostAddr/stopFootBoost (v62 dead). maxWalk/setMaxWalk kept (mount still uses them). sprintMode diag "speed"->"real". Toggle-vs-hold in-game sprint setting is IRRELEVANT now (we call the component directly).
- SmartSort Tempest "14 + dup 13 didn't sort": added diag in sortOwnedItems - logs each dupe group "[SmartSort] group 'ROWNAME' n=X [i exp=.. flag=.. eq=.. KEEP]". flag=2 = favorite (blocks mark), eq=true = equipped (blocks mark); a dupe showing as a LONE group = different RowName (not grouped). First image showed a heart on the 13 Tempest = likely favorite. Next SORT click will confirm.
- Deployed WFQoL (repo+game identical) + SmartSort synced game->repo, committed. NEEDS in-game test: press W out of combat -> real sprint anim + speed, log clean (no spam); click SORT once -> read the Tempest 'group' line to see flag/eq.

## 2026-07-05 - v64: sort found 0 items (wrong inventory) + sprint diag
- SORT BUG (log 21:39-21:40: every click "SORT: 0 items, 0 marked, 0 cleared" though 725 last launch): FindFirstOf("PlayerInventoryComponent") returned an EMPTY instance this session. Dump shows 5+ instances: Default__PlayerInventoryComponent (CDO), WFPreviewCharacter:Inventory, WFPlayerState:InventoryComponent (the real one). FindFirstOf is order-dependent -> grabbed a 0-item one.
- FIX: added findInventory() in SmartSort - FindAllOf("PlayerInventoryComponent"), skip Default__ CDO + invalid, pick the instance whose GetItemsByTag returns the MOST items (the live player one). Overlay SORT trigger now uses it; message "no populated inventory found (0 items) - open inventory then retry" if none. 0 items = no groups = why the group-diag never printed.
- SPRINT: no on-foot log because v63 Begin/EndSprint fire silently. Added one-shot diag: "sprint: foot BeginSprint - active now=true/false" (true = IsSprintingLocomotionStateActive flipped = real sprint engaged) OR "sprint: foot NO LocomotionComponent on pawn" (getLoco failed). locoLogged one-shot flag. Only mount line seen so far (995->1393).
- Deployed WFQoL + SmartSort synced, committed. NEXT LAUNCH: (1) move on foot out of combat -> read "foot BeginSprint - active now=?" (confirms real sprint), (2) OPEN INVENTORY then click SORT -> should show 725 items + per-dupe "group '..' n=.." lines incl Tempest (flag=2 fav? eq? or lone group).

## 2026-07-05 - v65: equipped detection fixed (respect items equipped on other chars)
- USER: the Tempest dupe that "didn't sort" = item equipped on ANOTHER character; the game blocks marking those for sell server-side, can't force it, respect it. (Also 0-items last log = he'd already sold, red herring; findInventory hardens it anyway.)
- BUG FOUND: SmartSort equipped-check read entry.Spec.bEquipped/Equipped - NEITHER field exists on InventoryItemSpec (dump), so equipped was ALWAYS false. Equipped dupes only survived because they happened to be the keeper (max exp). Cross-char-equipped dupes got marked (flag=1) every sort but the server RPC rejected -> the repeating "N marked, 0 cleared" (marks that never stuck).
- FIX: read the REAL field InventoryItemSpec.EquippedToSlotName (NameProperty, offset 0x20; "None"/empty when unequipped, slot name when equipped anywhere incl other chars). equipped = (name ~= "None" and ~= ""). want() already skips equipped -> now correctly never marks equipped items, and stops re-enqueuing rejected marks. (EquippedToSlotId struct + bHasEverBeenEquipped also exist; used the Name = simplest.)
- Synced game->repo, committed. NEEDS in-game: click SORT, the group-diag "eq=true" now shows for equipped dupes (incl other-char) and they stay unmarked = respected.

## 2026-07-05 - v66: on-foot sprint = Sprinting TAG injection (BeginSprint alone inert, proven)
- DIAG (log 21:52:34): "sprint: foot BeginSprint - active now=false" -> loco resolved + BeginSprint() called but WFLocomotionComponent did NOT enter sprint state. BeginSprint alone is inert; the state machine keys off the Sprinting gameplay TAG, not the call. (mount sprint works separately, 857->1200.)
- FIX: on-foot now injects the tag: asc:AddUniqueGameplayTag(SPRINTING_TAG) [WFAbilitySystemComponent method, called ON asc] when moving out of combat, asc:RemoveGameplayTag(SPRINTING_TAG) otherwise. Native OnSprintingTagChanged listener applies LocomotionSettings_Sprint speed. Also call loco:BeginSprint()/EndSprint() AFTER the tag to force the transition. state-change gated via ascHasTag(SPRINTING_TAG). SPRINTING_TAG={TagName=FName("Character.State.Generic.Sprinting")} (already declared line 53).
- This RESTORES the tag-injection method from the original reference notes (v61 wrongly abandoned it for "only real sprint"; the real ability + BeginSprint are both inert from Lua). Updated reference-wayfinder-ue4ss-api memory.
- New diag: "sprint: foot tag+Begin - tag=true active=?" one-shot. NEEDS in-game: move on foot out of combat -> should sprint now; read tag=true (tag stuck) + active (locomotion state). If tag=true but no speed, LocomotionSettings_Sprint may need the ability-driven tag not a loose one -> next lever = activate GA_Player_Sprint via GetAbilityFromInputTag(Input.Sprint) then hold.

## 2026-07-05 - v67: CRASH fix (sprint tag reverted) + nameplate self-capture fix
- CRASH crash_2026_07_05_22_05_37.dmp (no Lua error in log = native AV). Last log: rapid combat flip-flop (false/true every ~2s) at 22:05:33-35, crash at :37. v66 Sprinting-tag injection WORKED (log 21:58:19 "foot tag+Begin - tag=true active=true" = real sprint engaged) BUT toggling the tag on combat flips drives the native OnSprintingTagChanged locomotion listener - thrashing it = uncatchable native AV. REVERTED on-foot to the crash-safe MaxWalkSpeed cap (v62 style: float write on validated movement comp, no native listener, out-of-combat, baseline/pawn-addr keyed). Removed getLoco/isSprinting/tag add-remove. sprintMode "real"->"speed".
- NAMEPLATES stuck-full HP + no stamina: log had "player ShouldBeVisible firing" but NEVER "self nameplate captured"/"self drive OK" -> self-capture failed -> driveSelf never ran. ROOT: localPawn() used require("UEHelpers") which fails in ShowNameplates' path -> nil -> addr never matched. FIX: capture now uses np:GetOwningPlayerPawn() (UMG built-in = LOCAL player's pawn, no UEHelpers), fallback localPawn(). Added driveSelf() call in the self stamina-branch of ShouldBeVisible (refresh even if HUD change-hooks don't fire) + one-shot "capture miss: owner=.. lp=.." diag. AttachedOwnerActor confirmed on NameplateWidget (dump).
- USER asked "just dont sprint in combat?" - speed cap already gates out-of-combat; the crash was the tag TOGGLING on combat flicker, not sprinting-in-combat per se. Real-sprint tag remains available IF debounced (offered).
- PARRY "needs improvement" = deferred (vague; log shows parries firing + many "scheduled but not connecting out of range/not facing"). Need specifics.
- Deployed WFQoL + ShowNameplates synced. NEEDS test: (1) no crash in combat, (2) W out of combat = faster (foot cap log once), (3) self nameplate HP tracks real + stamina shows (watch "self nameplate captured"+"self drive OK"; if "capture miss" logs, read the addrs).

## 2026-07-05 - v68: nameplate throw fixed + sprint back to tag-only (debounced)
- NAMEPLATES ROOT CAUSE (finally): SetHealthMeterVisibility/SetStaminaMeterVisibility DO NOT EXIST on the widget (grep dump = 0). The unguarded np:SetHealthMeterVisibility(true) in ShouldBeVisible THREW -> aborted the whole callback right after "firing" logged -> self-capture never ran (no "captured" NOR "capture miss" in log = proof block unreached) -> HP stuck full, no stamina. FIX: removed ALL SetHealthMeterVisibility/SetStaminaMeterVisibility calls (ShouldBeVisible x2 + driveSelf x2); rely on showWidget=raw SetVisibility(0) (the only real reveal). Capture via GetOwningPlayerPawn (exists, UMG) now reachable.
- SPRINT: MaxWalkSpeed cap = "shit" (game resets MaxWalkSpeed per-frame on foot, 300ms poll can't hold it -> jitter). Only the Sprinting-TAG injection gave real sprint (active=true). Re-enabled TAG-ONLY (dropped loco:BeginSprint/EndSprint = the likely crash - tag ran 100s of times fine over 7min before the crash). Gated on DEBOUNCED combat (loggedCombat, settled >=0.75s) so a flapping InCombat tag can't rapid-toggle the tag = anti-thrash. asc:AddUniqueGameplayTag(SPRINTING_TAG) when moving+out-of-combat, RemoveGameplayTag otherwise. If it crashes AGAIN -> tag itself is unsafe -> disable foot sprint (mount-only).
- SMARTSORT group diag healthy: keeps highest-exp per echo group, dusts dupes, respects flag=2 favorites (many low dupes stayed only because user-favorited). "719 items, 6 marked, 1 cleared" = converged. No change.
- Deployed WFQoL + ShowNameplates synced. NEEDS test: (1) NO crash in combat (tag-only+debounced), (2) W out of combat = REAL sprint speed (log "foot Sprinting tag ON"), (3) self nameplate HP tracks real + stamina shows (log "self nameplate captured"+"self drive OK"; watch for "capture miss").

## 2026-07-05 - v69: comprehensive hardening pass (plan-approved) - zero-crash + nameplate real fix
- Crash-on-close ROOT: LoopAsync loops + hooks keep firing during world teardown and touch freed pawn/ASC/inventory/widgets = native AV (pcall can't catch). Two parallel Explore audits mapped every native-access point across all 3 mods.
- CRASH GUARDS (Priority 1):
  * WFQoL: teardown hook onTeardown (ready=false + arm settling on ClientReturnToMainMenu/WithTextReason/ClientGameEnded). Added `not ready` to parry bails (onEnemyAbility + doParry - delayed doParry could outlive the 1.5s settle window). Added ready+settling gate to onReloadEnded's 250ms delayed TryActivateAbilityByClass + onReloadActivated forceWindowOpen. Wrapped Attack1_36/Attack2_40 input-hook self:get() in pcall.
  * ShowNameplates: added npTeardown hook (ClientReturnToMainMenu/ClientGameEnded -> arm settling + drop selfNameplate/hudMeters) - previously only ClientRestart armed the gate, so HUD hooks + ShouldBeVisible ran against tearing widgets on close.
  * SmartSort: added `tearing` flag (ClientReturnToMainMenu/ClientGameEnded=true, ClientRestart=false); poll + processQueue drain bail if tearing; validate pic:IsValid() before GetItemsByTag (sortOwnedItems) + before SERVER_TryApplyItemFlags (applyTagToItem).
- NAMEPLATE REAL ROOT CAUSE (Priority 2): driveSelf read shield+health+STAMINA in ONE pcall; stamina is a SEPARATE widget (HUD_PlayerStaminaMeters.PlayerStaminaMeter) that can be nil -> whole pcall threw -> ok=false -> early return -> NEITHER health NOR stamina set = HP stuck full + stamina blank. (v68 fixed the ShouldBeVisible throw; THIS driveSelf throw was the remaining cause.) FIX: split reads - health+shield guarded block, stamina its own guarded block, set health even if stamina absent, IsValid each sub-widget. resolveHUD now requires BOTH bars valid (not just shield) so a stale HUD (Percent defaults 1.0) can't peg HP full.
- CLEANUP (Priority 4): removed dead SPRINT_CLASS/getSprintClass/sprintClassCache; rewrote stale escalating-strategies comment; nil-guarded SmartSort's currentExp/startingExp typo (was nil>nil in the unused legacy RunSmartSort path).
- Sprint on-foot = tag-only + debounced combat + ready/settling-gated (unchanged, audit-confirmed compliant). Parry behavior UNCHANGED (user: good enough) - only crash gate added.
- All 3 luaparse OK, deployed (WFQoL repo==game identical), NP+SS synced game->repo. NEEDS test: play combat + change zones + RETURN TO MENU + CLOSE GAME -> no crash; self nameplate HP tracks real (not full) + stamina fills (log "self drive OK: hp=.. stam=.."); sprint W out-of-combat works; SmartSort unchanged.

## 2026-07-06 - v70: log-spam trim (reload success cue) + status confirmed clean
- Log sweep after v69: NO new crashes, 0 errors. crash_2026_07_06_00_03_04 was OptiScaler (GPU upscaler) not our mods - turned off its trace logging (OptiScaler.ini LogToFile=false + LogLevel=4, deleted stale 181MB log). Crash-on-close now writes NO dmp (v69 teardown guards working) = benign exit hiccup only.
- SmartSort confirmed working in MP/solo: "SORT: 845 items, 23 marked, 3 cleared" - self-heal (3 cleared), equipped respected (eq=true kept), favorites (flag=2) untouched, keeper=highest exp/rarity. Note: some items show flag=8 (unknown game flag, likely game's own sell/lock) - processed normally.
- IMPROVEMENT: "reload: success cue" was 327 log lines/session - K2_HandleGameplayCue on GCNA_2HR_ActiveReload_Success re-fires 2-3x per reload. Deduped: log only on first cue of a cycle (if not successThisCycle), flag side-effect still runs every fire. main.lua ~1020.
- Nameplate self drive confirmed (hp+stam both read, split-pcall fix holding). Sprint foot tag ON + mount both working. Parry unchanged.
- Deployed WFQoL. All 3 mods stable.

## 2026-07-06 - v71: parry cast-cancel + sprint manual-block fixed + nameplate change-diag
- PARRY CANCELING CASTS: doParry force-ladder called asc:ServerCurrentMontageStop(0.15) to jam a parry through mid-action - this cancelled the player's CASTS/actions (worst on caster + no-parry weapons like Breaker/2H; user confirmed Breaker has no parry). FIX: removed the montage-stop force entirely. Parry now only lands if the block ability activates cleanly (r=="ok"); if you're mid-action it retries 80ms x3 then stands down - NEVER interrupts you. (Reverses the earlier "cancel is ok" stance - user hit it hard while casting.)
- SPRINT BLOCKED MANUAL SPRINT: the loop did RemoveGameplayTag(SPRINTING) whenever not wantSprint - this stripped the tag EVEN WHEN THE PLAYER set it manually (or desynced it) -> auto-sprint stopped AND you couldn't sprint yourself. FIX: track sprintTagByUs (bool). Add only if not ascHasTag (don't claim a manual sprint); remove ONLY if sprintTagByUs (a tag WE added). Reset sprintTagByUs=false in onTeardown. Never touches the player's own sprint tag now.
- NAMEPLATE MP-HOST STUCK FULL (hp+stam always max): the clean GetHealthPercentage/GetStaminaPercentage getters DON'T exist on the widget (dump: only enemyBP_GetHealthPercentage), so HUD-scrape is the only path. v69 split-pcall didn't fix the stuck value. Root cause still unknown -> replaced the one-shot "self drive OK" log with a CHANGE-TRACKING "drive: hp=.. stam=.." log (fires when hp/stam moves >2%). Next MP test: if hp changes in log when damaged = read works (display/setPct issue); if stuck 1.00 = wrong HUD instance read. Need that data point.
- Deployed WFQoL + ShowNameplates synced. NEEDS test: cast/act freely (no more parry interrupt), manual + auto sprint both work + don't cancel each other, MP-host take damage -> watch "[ShowNameplates] drive: hp=.." lines to see if HP tracks.

## 2026-07-06 - v72: weapon/echo/mechanics memory + AutoBlock groundwork
- Saved reference-wayfinder-weapons-echoes.md (new memory): weapon types + combat styles, ECHO types (Attack/Guard/Balance/Cross/Rush) + rarity->max-rank (Common4/Uncommon6/Rare8/Epic10), affinities, AND weapon CLASS internal names from the mechanics wiki: Guardian=SnS (Latent Power, Block Counter/Shield Slam), Executioner=2H (Momentum; Breaker/Greatsword/Greatscythe), Twin Striker=Dual Blades (Blade Dance), Marksman=Rifles (Eagle Eye + per-subclass Perfect-Reload buffs: Rifle+35% firerate, Burst+50% critpow, Shotgun+50% breakpow, Runecannon+50% weappow). Guard Break/Resilience/Gloom Grasp mechanics noted.
- NEW FEATURE planned - AutoBlock (SnS/Guardian): design LOCKED via AskUserQuestion = trigger on melee+AoE, BLOCK-ONLY (no auto-counter), AUTO when SnS equipped (no toggle), stamina-gated (avoid guard-break). Reuses findGrantedBlockAbility (runtime block-ability scan) + onEnemyAbility.
- BLOCKED on API verification (mod rule: never guess): need the SnS block ability class name (likely GA_Player_Block_Guardian_C or _SnS_C) + how AoEs are tagged. Added a DIAGNOSTIC to onEnemyAbility: logs each enemy ability's full tag list once per class ("enemy GA seen X [tags: ...]") - reveals melee (Attack+Melee) vs AoE tagging. The existing "parry: block ability=X" log reveals the SnS block class.
- NEXT: user plays on SnS with PARRY ON (F8) vs AoE-using enemies -> send log. Extract SnS block class + AoE tags -> build AutoBlock v1.
- Deployed WFQoL (diagnostic only, no behavior change).

## 2026-07-06 - v73: AutoBlock v1 (Sword & Shield) shipped
- VERIFIED from live SnS log: SnS block ability = GA_Player_Block_SNS_C (folder .../GameplayAbilities/SnS). Attack tagging: MELEE = Ability.Characteristic.Melee (+Attack); RANGED/projectile = Ability.Characteristic.Ranged (e.g. GA_Turtlesaur_Atk_Spit_C); turns/dodges/taunts/aware have neither. Ground AoEs have NO Melee/Ranged characteristic -> unblockable (user-confirmed shield doesn't stop them).
- AutoBlock v1 (my-mods/WFQoL/WFQoL/Scripts/main.lua): AUTO-active when SnS equipped (detected via findGrantedBlockAbility class name contains "Block_SNS", refreshed ~3s in the sprint loop via refreshSnS). onEnemyAbility gate widened to (state.parry OR onSnS); on an incoming isBlockableAttack (Melee OR Ranged) it sets blockUntil = clock+0.9s (BLOCK_WINDOW). A driver in the 300ms sprint loop raises the shield (TryActivateAbilityByClass(GA_Player_Block_SNS_C) only if not already IsActive - no toggling) while blockUntil open + stamina > 15% (BLOCK_STAMINA_RESERVE, avoids guard-break). NO montage-stop (won't cancel your actions; mid-swing activation just gets rejected). No counter (user chose block-only), no toggle.
- Diag: "autoblock: ENABLED/disabled" on SnS equip/unequip; "autoblock: shield raised (stamina X%)" one-shot per SnS session.
- Reused mechanics wiki: SnS="Guardian" class (Block Counter is a real mechanic - counter deferred per user). Marksman perfect-reload buffs noted for future AutoReload polish.
- Deployed. NEEDS test: equip SnS -> log "autoblock: ENABLED"; get attacked by melee/projectile enemies -> shield auto-raises + negates dmg (stamina drains); ground AoEs still hit (expected); no action-cancel while attacking; watch stamina doesn't bottom out (reserve holds). If shield sticks/holds too long or drains too fast, tune BLOCK_WINDOW / add explicit end.

## 2026-07-06 - v74: AutoBlock v2 - Blocking TAG injection (class-activation was inert, like sprint)
- v73 AutoBlock "did nothing" though log showed "autoblock: shield raised" -> TryActivateAbilityByClass(GA_Player_Block_SNS_C) is INERT (block is a HOLD ability), EXACTLY like sprint. Dump confirmed: WFLocomotionComponent has BeginBlock/EndBlock/IsBlockingLocomotionStateActive + native OnBlockingTagChanged listener (same shape as sprint's OnSprintingTagChanged). EWFGameplayAbilityInput::Block[v13] (enum, like Sprint[v2]).
- FIX (mirror the working sprint solution): driver now INJECTS the Blocking state tag via asc:AddUniqueGameplayTag(BLOCKING_TAG) -> native OnBlockingTagChanged raises the shield. BLOCKING_TAG={TagName=FName("Character.State.Generic.Blocking")} - GUESSED as the analog of Sprinting tag (dump has no tag strings). own-tag tracking (blockTagByUs): add only if not already blocking (don't claim manual block), remove only our tag. stamina-gated (>15%), hold while blockUntil open.
- ADDED diagnostic hook on WFLocomotionComponent:OnBlockingTagChanged -> logs "autoblock DIAG: real Blocking tag = X" once (confirms/corrects the guessed tag from a manual OR auto block). CAVEAT: if OnBlockingTagChanged is called from C++ (not BP-VM), the hook won't fire (UE4SS hook trap) - then rely on whether the injected tag visibly blocks.
- Reset blockTagByUs in onTeardown + on SnS unequip (refreshSnS removes our tag).
- Deployed. NEEDS test: equip SnS + get hit by melee/projectile -> shield should RAISE now (dmg negated, stamina drains). Watch log: "autoblock: shield UP" (we injected) + "autoblock DIAG: real Blocking tag = X" (if hook fires - tells us if the guessed tag is right). If shield still doesn't raise = tag name wrong -> need ASC tag enumeration to find it.

## 2026-07-06 - v75: AutoBlock v3 - block-counter + smarter hold + preemptive + throttled diag
- AutoBlock CONFIRMED WORKING (v74 tag guess right): log "autoblock: shield UP (Blocking tag, stamina 67%)", user confirms shield raises. OnBlockingTagChanged hook never fired (C++ caller / UE4SS hook trap) but the guessed tag Character.State.Generic.Blocking works so it doesn't matter.
- 3 improvements (user-picked all): 
  1. BLOCK-COUNTER: after the shield absorbs a MELEE hit, auto-tap ONE M1 (block-counter) if near+facing. New helpers: nearFacing(pawn,enemy) (US->enemy facing, own check since willConnect is enemy->us), counterM1() (InpActEvt_Attack1 press+release 60ms, `injecting` guard), scheduleCounter(enemy,hitTime) (ExecuteWithDelay hitTime+COUNTER_DELAY 0.12s; gated: onSnS+ready+not settling, NOT m1Held/m2Held (don't interrupt your combo), COUNTER_COOLDOWN 0.6s, near+facing). Only scheduled for isMeleeAttack (ranged enemy is far -> nearFacing fails anyway).
  2. SMARTER HOLD: blockUntil = clock + hitTime(timings[className] or 0.8) + BLOCK_TAIL(0.55) instead of fixed 0.9 -> raises during wind-up (preemptive), holds through the hit + tail to bridge multi-hit combos. stamina reserve 15% unchanged.
  3. PREEMPTIVE: covered by #2 (raise on detection = wind-up start, hold longer).
- Diag: "shield UP (stamina X%)" throttled to 1.5s (was one-shot) so we can see block cycles + stamina over a fight to tune. "block-counter M1" one-shot.
- Deployed. NEEDS test on SnS: block still raises vs melee/projectile; after blocking a melee hit while near+facing -> auto M1 counter fires (not spammy, doesn't interrupt your own attacks); shield holds through combos without dropping; stamina doesn't bottom out. Tune BLOCK_TAIL / COUNTER_DELAY / reserve from the throttled log.

## 2026-07-06 - v75 test results + block directionality (FMC checkpoint)
- AutoBlock v3 STABLE: log shows clean ENABLE/disable on SnS equip-swap + "shield UP (stamina X%)"; NO crashes, NO errors over a ~1hr session. Block raising confirmed.
- OPEN/UNVERIFIED: block-counter M1 never logged this session ("autoblock: block-counter M1" absent) - either the near+facing+cooldown+not-attacking gate never all lined up, or scheduleCounter needs tuning. NEXT SESSION: block a melee hit while facing the enemy and confirm the counter M1 fires; if not, loosen the gate / check ExecuteWithDelay path.
- BLOCK IS DIRECTIONAL (user-confirmed mechanic, saved to reference-wayfinder-weapons-echoes): pressing block switches char to mouse-facing/aim mode (shield faces mouse-look; WASD stays free for backpedal/sidestep); attacks from BEHIND are NOT blocked, front only. AutoBlock relies on the player aiming manually (they do). Optional un-built stamina-saver: skip raising block when the attacker is directly behind (offered, user hasn't decided).
- All AutoBlock work (v1 groundwork -> v3 counter) + earlier session fixes (teardown crash guards, nameplate driveSelf split, parry no-montage-stop, sprint tag-only manual-safe, reload-cue dedupe, OptiScaler trace off) committed. Pushing at this FMC.

## 2026-07-06 - AutoReload perma-loop FIX (press early, not maxT-center)
- SYMPTOM: reload minigame re-fired every ~1s, gun never finished reloading, couldn't shoot ("perma reloading, not actually reloading"). Log: 13 activations in 17s, no fire gaps.
- ROOT CAUSE: our injected press landed AT/AFTER the real reload end -> game read it as a fresh reload-button press -> new reload -> loop. This weapon's real reload = 0.53-0.73s but MaxReloadTime reports 1.5s; we pressed at center = 0.5*1.5 = 0.75s = too late. PROOF: the one cycle we did NOT press (ended 0.53s before our 0.75s press) had a 3s gap after (no loop); every cycle we DID press re-activated same second.
- FIX (main.lua ~873, ~947): added `PRESS_EARLY = 0.18`; scheduleWindowPress now presses at PRESS_EARLY (mid-reload, before any real end) instead of the maxT-derived pressSec. window is force-open (Tolerance=1000) so early = still perfect. readWindow still runs (probes minigame live + logs maxT/window). Also DELETED the +250ms `OnTriggered_0C5309` fallback press (fired 0 times all session = dead + a stray-late-press re-trigger risk).
- Log now: "reload: window .. -> press at 0.18s (early)". Deployed + committed. NEEDS in-game test: reload completes once per mag, no loop, still lands PERFECT/active-reload buff (prior sessions PERFECT GA never activated - watch if the ActiveReloadSucceeded GA fires now).
- CLOSE-CRASH (still open, low sev): 49min session ended with log stopping clean right after ConsoleEnabler F10, NO teardown line, NO dmp. = raw process-exit (X/Alt-F4) which never calls ClientReturnToMainMenu/ClientGameEnded, so our teardown guards don't disarm the live LoopAsync loops/hooks before DLL unload. Not our Lua throwing (would log). Broad review agents running to find a safe exit guard + other hardening.

## 2026-07-06 - Robustness pass (parallel review agents) + close-crash read
- Ran 2 parallel review agents over all 3 mods (crash-risk/correctness/perf/dead-code), respecting pcall-cant-catch-AV + no-strip-helpers + MP-remote-ASC rules.
- WFQoL: added self-guarding IsValid checks INSIDE 5 helpers that did native calls but only validated at the call site (a deferred/game-thread call between check + use can free the object; pcall won't catch that AV):
  - distTo(a,b): `if not (a and a:IsValid() and b and b:IsValid()) then return math.huge end` (math.huge = "far" -> callers' `> RANGE` bail safely).
  - willConnect / nearFacing: added `pawn:IsValid()` to their existing guard (pawn was assumed valid from call site).
  - ascHasTag(asc): guards asc before lib:AbilitySystemHasTagExactly.
  - tryActivateParry(asc): guards asc at top (preloadAbilityGraph runs between the call-site check and TryActivateAbilityByClass).
- SmartSort: existsInTable() made nil/type-safe (echoRarity is a NUMBER, nil if absent -> raw string.find(nil,..) threw in the console RunSmartSort path which has no pcall). Now tostring() both sides + plain-text find (`,1,true`). String-slots path unchanged (tokens have no magic chars). Legacy/unused path but crash-proofed.
- CLOSE-CRASH verdict (both agents flagged, but LOW severity, NO code change made): raw window-X / Alt-F4 exit does NOT call ClientReturnToMainMenu/ClientGameEnded, so the settle/tearing gates never arm -> LoopAsync loops + HUD event hooks (nameplates OnHealthChanged etc.) can fire once against mid-teardown widgets. BUT all those paths are already IsValid+pcall guarded, no Lua error ever logs, and no dump is produced -> almost certainly the GPU injectors (OptiScaler/fakenvapi) tearing down, same dumpless signature as the mid-combat crash. UE4SS has no reliable Lua process-exit hook; adding a speculative shutdown hook (unverifiable, could itself crash on wrong signature) judged higher-risk than the benign close-crash. Real diagnostic = A/B test with injectors disabled (offered).
- Deployed (WFQoL via deploy script, SmartSort repo->game w/ backup) + committed.

## 2026-07-06 - ROOT FIX: identity-driven lifecycle rail (all 3 mods) - the transition-safety we kept circling on
- THE circle we kept repeating (~70 versions): every mod trusted transition HOOKS (ClientRestart etc.) to fire, then we reacted to each symptom (nameplate stuck-full, sprint dead, block stuck). Real root: the game swaps the local pawn on EVERY instance change - enter dungeon / other instance / DIE / fall-out-of-map / return-to-hub - and MOST of those do NOT fire ClientRestart. So cached pawn/asc/HUD/nameplate refs went stale + ready never re-armed.
- WFQoL specific trap found: `ready` was set true ONLY by ClientRestart, but the pawn-address transition-detect was GATED BEHIND `ready` (sprint loop `if not ready then return`). So a no-ClientRestart transition left ready stuck -> loop returned early -> never detected the new pawn -> never recovered. Circular dependency = "sprint stopped working / blocked manual sprint after a zone change".
- FIX = one IDENTITY-DRIVEN rail per mod (not event-driven). Key: the 300ms loop runs EVERY tick (NOT gated on ready) and keys off the LOCAL PAWN ADDRESS:
  - pawn nil/invalid -> ready=false (mid-transition, touch nothing)
  - pawn addr changed -> new instance: onNewInstance() wipes ALL per-pawn state, arm settle(1.5s), ready=false
  - pawn stable + past settle -> ready=true
  Every other loop/hook (chain/parry/block/reload/nameplate/sort) just READS ready/settling. Identity, not hooks, so it catches transitions no matter which engine fn fired (or didn't).
- WFQoL (main.lua ~735): new `onNewInstance()` resets pawnRef,libCache,metersCache,currentReload,m1/m2Held,sendRelease*,injecting,sprintTagByUs,blockTagByUs,blockUntil,onSnS,snsBlockAbility,lastCombat,loggedCombat,combatPendingSince,incoming. Rail is the 300ms loop head (was the sprint loop). ClientRestart hook FOLDED into rail (arm settle + onNewInstance + preload/overlay/registerAll; no longer sets ready=true - raw ClientRestart pawn can be mid-construction). onTeardown -> lastPawnAddr=nil + onNewInstance. Logs "lifecycle: new instance - per-pawn state reset, settling 1.5s".
- ShowNameplates: added 300ms pawn-address poll (cheap pointer read, NOT the old per-frame object scan) -> on pawn change OR cached ref invalid: arm settle + drop selfNameplate+hudMeters + reset diag flags -> ShouldBeVisible/driveSelf re-capture for the new pawn. Fixes "HP stuck full / stamina blank after zone change". Degrades safe: if localPawn() can't resolve, the stale-ref (IsValid) check still drops on invalidation.
- SmartSort: added pawn-address settle to the 500ms poll -> a SORT click during a transition is ignored 1.5s (avoids grabbing a loading-screen inventory). tearing flag (menu-return) kept.
- All parse-clean (luaparse 5.3), deployed (WFQoL script; nameplates/smartsort repo->game w/ backups), committed. NEEDS in-game test: enter dungeon / die / fall off map / swap instances -> nameplate HP+stamina correct on the NEW pawn, sprint works again (log "lifecycle: new instance..."), autoblock re-enables, NO stuck states, NO crash. This is the fix meant to STOP the circle.
- STILL not addressed by rail (separate): close-crash on raw window-X exit (dumpless, likely GPU injectors) - unchanged; and reload PERFECT-GA-never-fires (watch next test).

## 2026-07-06 - Freeze after rail: log verdict + nameplates off-thread fix
- User hit a FREEZE ~4min into the first lifecycle-rail session. Log check: rail did NOT thrash - "lifecycle: new instance" fired exactly 1x (clean), 4 nameplate captures, no spam, no Lua error. Log stops mid-frame right after "self nameplate captured" (06:13:51). = SAME dumpless signature as every prior crash (traced to OptiScaler/fakenvapi GPU injectors). SmartSort had done off-thread FindAllOf for 49min prior without freezing -> off-thread reads are tolerated in this UE4SS build, so that's not the smoking gun either. Strong cumulative signal: crashes/freezes persist across MANY Lua versions = the constant is the INJECTORS, not our code.
- REAL hazard I introduced + fixed: the new ShowNameplates lifecycle poll read pawn/widget UObjects OFF the async loop thread (unlike WFQoL which wraps all native reads in ExecuteInGameThread). Wrapped it in ExecuteInGameThread (torn-read/AV parity fix). 
- REVERTED the SmartSort pawn-poll rail: redundant (SmartSort re-resolves inventory + IsValid-guards per button-press; acts only on demand) and it only added more off-thread reads. Kept the WFQoL rail (game-thread-safe) + nameplates rail (now game-thread-safe).
- NEXT: the injector A/B test is now the #1 priority to break the crash cycle - hold the Lua fixed, disable OptiScaler + fakenvapi (rename proxy DLLs .off, backup first), play a session. Crashes stop -> injectors confirmed, all Lua work stands. Crashes persist -> game/EOS-side, dig there. Stop shipping speculative Lua until this is isolated.

## 2026-07-06 - FREEZE ROOT CAUSE(S) found by 2 parallel freeze-audit agents (real Lua bugs, NOT injectors)
- User hit a hard FREEZE ~4min into the first rail session (dumpless, must-kill). User chose "keep injectors, harden Lua" - correct: the audit found REAL Lua freeze vectors.
- THE freeze (nameplates): ShouldBeVisible fires EVERY frame -> driveSelf -> resolveHUD -> FindAllOf("HUD_PlayerMeters_C") = 135k-object scan ON THE GAME THREAD. resolveHUD only caches when BOTH PlayerShieldBar+PlayerHealthBar valid; during a transition the HUD exists but bars aren't both valid yet -> never caches -> scans every frame at 60-144fps on the game thread = hard freeze. My lifecycle rail AMPLIFIED it by dropping hudMeters right at transition time. Froze 14s after the transition (past settle) = exact match.
  FIX (ShowNameplates): resolveHUD throttled to ~2x/sec (lastHudScan os.clock gate; returns "unresolved" between scans). driveSelf-from-ShouldBeVisible throttled to ~10x/sec (lastVisDrive). Rail already game-thread-wrapped from last fix.
- Other real freeze vectors fixed (WFQoL, from agent 1):
  1. RELOAD FEEDBACK LOOP (was the "perma-reload" too): onReloadActivated had NO re-entrancy guard - our injected press, if late (game-thread lag), re-triggers the hook -> rebuild + forceWindowOpen FindAllOf + reschedule press = positive-feedback freeze. Added: re-entrancy guard (drop re-activation <0.4s after the last; a real reload is longer), firePress late-abort (skip if elapsed > PRESS_EARLY+0.3s = a late press that'd re-trigger), and CACHE the WFRangedWeaponAttributeSet ref (no FindAllOf per activation).
  2. getPawn FindAllOf fallback throttled to 1x/2s (lastPawnScan). The rail calls getPawn every 300ms; during a load pc.Pawn is nil -> it fell back to FindAllOf(135k) every tick = perma-load amplifier. O(1) PlayerController route stays unthrottled so a valid pawn is still caught instantly.
  3. onEnemyAbility: moved `lastScheduled = now` ABOVE the `if not state.parry` bail. Old spot (after bail) meant with parry OFF + SnS ON the AutoBlock/scheduleCounter path ran unthrottled per enemy ability (swarm = game-thread callback burst).
- SmartSort (agent 2 HIGH, latent - fires only on SORT click): findInventory()+sortOwnedItems() ran off the async LoopAsync thread (races game thread / object-array lock). Wrapped in ExecuteInGameThread + ignore SORT while a drain is flagRunning.
- STILL NOT DONE (noted, lower priority): launchOverlay does os.execute(wscript) synchronously on the game thread in the ClientRestart hook - if the VBS ever blocks = full hang (one-shot, didn't cause this freeze; verify tools/overlay/launch-overlay.vbs uses WshShell.Run(cmd,0,False) detached, then move os.execute off the game thread). SmartSort register-once for SERVER_TryApplyItemFlags (log shows per-item hook register/unregister churn - annoying, not freeze).
- All parse-clean, deployed (WFQoL script; nameplates/smartsort repo->game w/ backups), guards verified in game copies. NEEDS test: play through transitions + a reload + a SnS swarm + a SORT -> no freeze. If the watchdog logs "GAME THREAD FROZEN" it narrows further.

## 2026-07-06 - AutoChain perma-stuck-at-melee-START fix (SnS) + hot-reload note
- SYMPTOM: holding M1 (AutoChain) vs an enemy in front on Sword & Shield -> character PERMA-STUCK at the START of the melee wind-up, swing never progresses ("stuck trying to start melee perma").
- ROOT: the chain injected press(_36)/release(_37) ALTERNATING every 70ms (~140ms/tap). That re-pressed WHILE the swing was still in wind-up; SnS melee RESTARTS the swing on a fresh press -> never leaves the start. (Old design assumed the game buffers presses during a swing; SnS doesn't.)
- FIX (main.lua AutoChain): timed single tap = press -> hold CHAIN_HOLD(0.10s) -> release -> wait CHAIN_GAP(0.24s) before next press (~one tap/0.34s = natural cadence, no mid-swing re-press). State machine: chainDown/chainPressAt/chainReleaseAt. Plus flushChainRelease(): on ANY stop (ready flips false, reload, transition/settle, button release) send the outstanding release so a dangling injected press never leaves the button "held" (a second stuck-mode). Added throttled "chain: tapping (m1/m2)" diag (chain was previously invisible in logs). onNewInstance resets chainDown too.
- NEEDS in-game test on SnS: hold M1 vs an enemy -> attacks flow, no stuck. If still stuck or too slow, tune CHAIN_HOLD/CHAIN_GAP from the "chain: tapping" cadence in the log. If a DIFFERENT weapon now feels slow, the gap can drop.
- HOT-RELOAD (user asked for Ctrl+L to avoid relog): UE4SS CAN hot-reload Lua (default via the debug-console GUI "Restart All Mods" / a configurable hotkey), BUT it's UNRELIABLE for OUR mods - they RegisterHook (native hooks) + run persistent LoopAsync loops; a reload re-runs the script without cleanly unregistering the old hooks/loops -> DOUBLE hooks/loops (double attack injection, double drivers) = instability/crashes. For stability testing, close+relaunch = clean state. (No build step ever - Lua is interpreted; relaunch loads fresh.)
